# Fresh Astra review assignments — pending implementation and final validation

These reviews have not run. Recovery/implementation owners do not count as the requested final independent review. Start six new `gpt-6-astra` agents in two waves of three, once the implementation and its concrete final validation/evidence are available. Give each its explicit bounded scope below, the final commit/tree index and the original plan baseline for every applicable line. Keep the primary focus on MC 1.21.1 and require explicit comparison of affected support-line adaptations. Reviewers are read-only until a finding is independently validated by the coordinator.

Every reviewer should read the implementation plan and applicable repository instructions, inspect the actual baseline-to-final diff in its scope, and check relevant existing tests before treating a pinned tradeoff as a bug. Report actionable findings with severity, triggering conditions, file/line, consequence, and a concrete verification or test. Distinguish observed failures from hypotheses. Do not infer final acceptance from old ledger prose or historical pass counts. Report an explicit no-findings result if appropriate. The coordinator validates findings, implements justified fixes on all applicable lines, and reruns affected gates before the final handoff.

Wave 1:

1. **Settings, presets, and owner publication.** Common setting metadata, parsing/clamping/defaults, runtime batch preview/apply/undo, correlated generation publication, restart overlays, persistence failure and retry, command permission/thread ownership, scalar and per-world repush behavior, client presets and Sodium/no-Sodium surfaces. Compare both primary loaders and 26.2's world-map adaptations. Review the accepted measured preset derivation without assuming that a preset name proves safe defaults.
2. **Status and diagnostics export.** Client/server immutable snapshot provenance, no-manager and failed/OFF states, stale session/world retirement, collection cadence and bounded work, privacy allowlist, error sanitization, asynchronous export ownership and rejection, old callback suppression after disconnect/reconnect. Review native status/export fixtures and negative controls as well as production behavior.
3. **Xaero and client lifecycle.** Bindings, acquisition/debt, tile writing, committed native rebuilds and session orchestration; old-origin/reentrant callbacks, OFF versus disconnect, budget and release semantics, rendering/save/boundary regressions. Compare MC API, both Sodium generations and both 1.21.1 loaders, and preserve intentional renderer stubs on other lines. Inspect actual final map/WI5/WI6/seated/Elytra proof identity and human dispositions where relevant.

Wave 2:

4. **Compatibility, support lines, tests, and shipping.** Catalog authority and unknown states; build/shipping/render/UI/support-tier separation; source refs and classification; port resume/conflict handling; test migration accounting and impact selection; CI/release contracts, LSS/VSS byte/wire identity and exclusion of fixture classes. Inspect all five support lines and explicit Java/native-shape differences. Excluded release-promotion redesign is outside scope.
5. **Rig ownership, storage, and evidence.** Disposable paths/profile/dependency identity, inherited auth/hook stripping, WSL private display and input isolation, physical Windows storage budgeting/monitoring, owned LWJGL extraction, locks/process boot-start ownership, pre-spawn journals and cleanup receipts, failure preservation, target construction and checker closure, human-proof boundary. Exercise bounded offline/script negative controls where useful; do not operate the Windows foreground or launch competing Java/native workloads.
6. **Concurrent source correctness and performance.** Source provenance and actual Folia region overlap, fixture ownership refresh and reconnect, fixed schedule/every-edit receipt semantics, slow consumer/admission denial and debt drain; raw metric sample sufficiency/eligibility/identity, baseline calibration, fixed alternating order, floors/regression budgets and terminal failure handling. Validate the actual selected experiment and its limitations, not synthetic checker greens. Compare Fabric/Paper/Folia prerequisites and check that measured findings justify the conservative operating point.

Review outputs and coordinator dispositions belong in a new dated durable review directory and the concise final implementation ledger. Preserve full first-failure evidence and prior reviews as history. Starting a reviewer is not completion; all six reports and validated disposition are required.
