# Fresh final review 01 — settings, presets and owner publication

Date: 2026-09-14. Reviewer: independent final settings review, assignment 1 of 6. This report is new final code review, not a historical implementation or recovery review.

**Result: no actionable findings in the inspected settings/presets scope.** This is code-review completion only. Final exact-artifact native/UI/preset evidence is pending supplementation by the coordinator; no pending gate is accepted here.

## Identities and method

Read assignment 1 in `recovery-20260910/final-review-assignments.md`, primary `CLAUDE.md`, relevant line guidance and the amended implementation plan, especially P7a/P7b and A1–A5. Inspected baseline-to-final production differences and existing/new behavior pins before judging possible regressions. Compared affected common and xplat source across all five worktrees, with primary focus on Fabric and NeoForge 1.21.1 and the distinct 26.2 distance-map behavior.

| Line | Plan baseline | Inspected final commit |
| --- | --- | --- |
| 1.21.1 | `48268c7d7e8740a93fbc6c7cf60fb36f7b75d690` | `0ae20c5f5f41e9482f865b5da7faf6a1d16bfacd` |
| 1.21.10 | `948b0bf09dd852d357176577d7ccacd589aadb7d` | `8a6457bd4250b495d523f9382ff3f73a1c5e7353` |
| 1.21.11 | `8ac799a901e7eb99cb9403d512b790a0b6576190` | `b8828f5ad550c73e26de158b3c56c877cc6e5d8a` |
| 26.1 | `1e7c9a9d3507c3ba90ac511ba6b62995b8796ab2` | `f8f31bb68432db75b32908c83890587cc0ee0dd6` |
| 26.2 | `98b67abc82283417ad288cbb0cca2f4fb3343425` | `6f7081101b4fbbdb946b6160618bc71cd8d20b44` |

Final commits matched `accepted-performance-v27-20260914/docs-and-refs-integration.json` during review. Working trees contain preexisting historical/untracked evidence and documentation; no production/settings dirty diff was observed. This reviewer changed no repository files, launched no Java/native/Windows workload, and spawned no agents. The only write is this report.

## Reviewed behavior and supporting pins

- **Metadata, defaults and parsing:** `common/.../config/{SettingDescriptor,SettingBinding,ServerSerializedSettings,ClientSerializedSettings,PaperSerializedSettings,RuntimeSettings}.java`, `tools/settings/inventory.py`, and `xplat/.../config/menu/ClientOptionCatalog.java`. Runtime descriptors overlay the stored inventory; all serialized fields remain described without making advanced/internal values settable. Existing typed clamps, strict command booleans, finite command decimals, AUTO/zero/negative sentinels, legacy bandwidth adoption, new-install versus upgraded store defaults, client storage/privacy/fallback values and save hooks are retained. Read `RuntimeSettingsTest`, `SettingMetadataTest` and `SettingInventoryTest`. Executed `python3 tools/settings/inventory.py --check` read-only on all five lines; all passed.
- **Preview/apply/undo:** `SettingsPatch.java`, `RuntimeSettings.previewBatch/applyBatch/undoBatch`, and `ServerConfigBase.presetCommand`. Scratch parsing uses canonical key order, with global generation preceding its dependent per-player value. Captured validation dependencies include both generation limits and the protected far-player minimum. Scope identity and relevant inputs are rechecked before publication. Undo restores the actual changed-key patch, includes validator-derived changes, rejects conflicts and preserves compatible unrelated edits. Read `SettingsPatchTest`, `RuntimeBatchTest`, `RuntimeBatchUndoTest`, `ConservativePresetTest`, `ServerPresetTest` and `PaperConservativePresetTest`.
- **Publication:** `ServerConfigBase.GenerationLimits` is a volatile immutable pair published once at validation completion. Inspected generation readers in xplat and Paper generation services, processing services and handshake/session admission. The correlated production readers use the snapshot; the no-snapshot fallback remains the documented constructor/test seam. Controlled raise/lower interleavings in both input orders are pinned for apply and undo. Independent remaining fields retain their documented owner/publication semantics; this implementation does not claim whole-config atomicity.
- **Restart and failed persistence:** `JsonConfig` stages the generation choice in a transient restart overlay while preserving the live field. Subsequent unrelated saves serialize the staged choice, stored-value metadata reads it, and reload naturally drops preview/undo history. Failed saves leave effective changes in place and report unsaved outcomes. Real temporary-path blockers are tested. A fresh no-op conservative apply retries persistence and intentionally becomes the last application; its empty undo does not revive older history. This behavior is explicitly pinned and is not reported as a bug.
- **Command owner, permission and repush:** primary Fabric and NeoForge use the shared xplat command tree under the existing gamemaster permission root; Paper retains `plugin.yml`'s `lss.admin` command permission. Paper preset work obtains the current config within the queued owner task. Preview does not save or repush. Numeric apply/undo repushes once for an effective distance mutation, including failed saves; generation-only and no-op changes do not repush. Client commands in both loaders reach shared `ClientPresets` without requiring Sodium. Existing world/session/config identity expiration remains in the client route.
- **Client settings/UI:** shared `ClientPresets`, `ClientCommandActions`, Fabric client registration, Neo bootstrap registration, both loader copies of `SodiumStatusReturn`/`SodiumStatusEntryHook`, `ExternalOptionRefresh`, and typed `ClientOptionCatalog` hooks. Plain map-only preserves the Xaero write preference; the separately named writes preset explicitly enables persistent writes. Both explain that they cannot select recipients or disable another consumer. Publication finishes both booleans before one owner reconciliation. UI return reloads the reception binding while preserving a preexisting explicit draft. Modern/legacy reflective routes and no-Sodium command availability are inspected code paths, not claimed final native successes.
- **26.2 world scopes:** inspected adapted `RuntimeSettings`/`ServerConfigBase` and both server command surfaces against the 26.2 baseline, plus `ConservativePresetWorldScopeTest`. Only distance exposes world scope. Global batch previews reject world-form values; explicit distance operations preserve map replacement, name-before-dimension-before-global precedence and removal fallback. Map-aware `ApplyResult.repush` detects puts/removals even when the scalar is unchanged. The global conservative patch preserves existing overrides, later override edits and removals. Sibling lines do not accidentally acquire the 26.2 map API. Permission API and 26.x screen-render API adaptations remain localized; client catalog differences preserve intentional renderer availability.

## Measured 32/4/1 selection

Inspected `docs/operations/performance.md`, `status-and-presets.md`, `performance-v27-summary.json`, the private accepted `selection.json`, and `tested-effective-config.json`. The latter's computed SHA256 is `5ebd6ad1b04ba7cc220b5cb41f2b86de458178364e163519947b350f672e9ac6`, matching the public summary and selection. All nine attributed selected slots (three calibrations and six measured arms) record radius 32, global concurrency 4 and per-player concurrency 1. These equal the constants shipped in all five `ServerConfigBase` implementations. Defaults remain 512/40/40.

The measured claim is appropriately limited to a fixed four-Fabric-client/Folia-26.2 operating point. It is not a search proving these values optimal, a hardware-independent safety guarantee, or measured parity on 1.21.1. The docs preserve the corrected-reference cost exclusion, frame pacing/RSS interpretation, fixed heaps and scheduler setup, and the third-pair server tick-delay p99 excursion despite the passing median-plus-two-of-three aggregate rule. Full raw metric/eligibility adjudication belongs to review 06; this review checked settings attribution and the public claim boundary.

## Evidence and limits

Independently read the final 1.21.1 `final-validation-after-preset/1.21.1/attempt-final-integrated-20260914/manifest.json`: source SHA matches the reviewed primary commit; status and exit code are passed/0; shipping inputs stayed stable; actual unit cases are common 783, Fabric 1719 (four named opt-in experiment skips), Paper 539 and NeoForge 23, with zero failures. Fabric/Neo server summaries are 78/8; no nonexistent Tier 3 is claimed. This reviewer did not rerun Java tests or independently reproduce that native server execution.

The coordinator separately reports 1.21.10's first-attempt gate passed and 1.21.11's gate is running; those updates are not independent evidence adjudications in this report. Old UI/preset ledgers were not substituted for final exact-artifact receipts. Final modern/legacy × both-primary-loaders, no-Sodium, server failed-save/restart and 26.2 world-scope native receipts remain pending supplementation. No current runtime failure was reproduced, and no unverified hypothesis is promoted to an actionable finding.
