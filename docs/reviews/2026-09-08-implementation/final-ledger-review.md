# Final ledger versus plan — independent bounded review

Reviewed 2026-09-08 after parent confirmed all five aggregate builds/release checks green. Read-only source/evidence inspection; no Gradle, server/client launch or repository edit. Sources: primary `docs/planning/astra-system-review-fix-plan.md` and `astra-implementation-ledger.md`, plus external `remaining-runtime-matrix.md`, implementation reports, final build XML summaries, and artifact inspection. This is a completion/evidence review, not a repeat implementation audit. Parent owns the ledger.

## Verdict

No new production defect established. All 30 exact CI-versioned 0.14.0 artifacts PASS independent inspection, including 26.2. The plan is not yet fully runtime-accepted: its required genuine harness smoke, selected scenario/wrapper gates and several visual/optional-integration procedures are still unrun or explicitly deferred in the inspected evidence. Current UNIT GREEN labels avoid overstating completion. Do not upgrade all packages to DONE from build/fixture greens alone.

## Concrete ledger corrections before handoff

1. **Refresh the current matrix, retaining the historical log.** Ledger lines 9–23 still list combined builds, Neo build and generation smoke as remaining even though later paragraphs establish them. Lines 75–79 say both C2ME profiles remain red; line 85 records their subsequent greens. Label these earlier entries historical/superseded, and put one authoritative current matrix above them. Add 26.2: XML reports Fabric 2394 selected /2390 passed /4 opt-in skips, Paper 511/511, NeoForge 23/23; build log records 76/76 Tier2. Its separate runtime log was still completing during this review (Neo 8/8 present; no terminal BUILD SUCCESSFUL observed yet), so wait for that result before claiming the complete extended client command green.
2. **Add a per-line change/evidence map.** Plan Tracking requires one row per WI and affected line; ledger currently has one row per WI plus line baselines. Existing external implementation/port reports can supply the exact commits and intentional exceptions without repeating every test method. Include WI9 only on 1.21.1 Fabric/Neo and WI15 only on the two affected Fabric lines, with already-correct/stub controls elsewhere. Link the final 30-jar JSON (exact hashes), current candidate tips and any later fixture-only commits explicitly; inspection HEAD alone is not proof of the jar's build ancestry.
3. **Keep remaining obligations explicit, with gate-specific status/reason.** Link `remaining-runtime-matrix.md` from the current matrix, but update it as outcomes arrive: its opening is deliberately a prepared/unexecuted snapshot. Record selected scenario commands, checker and wrapper verdicts, current run identity and evidence path. A preflight `--validate` or shell fixture PASS is not a genuine scenario/benchmark PASS. Give omitted planned gates a named DEFERRED status and scope reason rather than merely dropping them from the matrix.
4. **Add a present-tense pointer to the plan.** Its opening still says no fixes implemented/new subagent review absent, and final boundary says implementation is future work. Those are true of the historical planning snapshot; a short dated pointer to the implementation ledger avoids presenting them as current state. Preserve historical evidence and baseline claims.

## Runtime obligations versus deterministic coverage

| Gate family | Established coverage | Remaining obligation / classification |
| --- | --- | --- |
| WI1/WI7 durable store integrity | Permanent interruption/reopen and legacy hash/size regressions; all-line build/unit suites | Selected complete migration wrapper and Paper offline-edit wrapper remain planned/unrun in the inspected evidence. They test actual persisted-world ancestry/transport, not the exact deterministic failure schedule. Do not substitute isolated wrapper phases. |
| WI2 registration/generation | Reader/generation/follower boundary controls; all-line unit suites and Fabric/Neo server gametests | Selected generation-disabled and capacity-stress scenarios, representative Paper/Folia lifecycle route remain planned. Those are complementary integration gates; they cannot replace exact stale-owner regressions. |
| WI3/WI4/WI5 composition | Receipt cancellation, delayed report, summary proof and lease controls; extended real-client OFF-at-LAN-publication/OFF-ON receipt tests green on 1.21.10/1.21.11/26.1 | Finish 26.2 client command. Selected dimension/dirty-summary scenarios remain planned. Real 1.21.1 Sodium modern/legacy Apply and Xaero backlog-disconnect/same-dimension rejoin on both loaders remain unobserved. Tier3 absence on 1.21.1 is an intentional cut, not a test failure or permission to label GUI validation complete. |
| WI6 far-player OFF/ON | Service-to-tracker and withheld-clear ordering controls | Ordinary command-driven live proxy disappearance/restoration still unrun. Exact transport withholding remains deterministic unless a controlled live injection is added. |
| WI9 seated exception | Both 1.21.1 renderers and real PoseStack sentinel regression | Controlled post-push renderer exception observation remains explicitly DEFERRED for missing isolated runtime injector. Normal boat riding does not exercise the failure. This is a required plan acceptance observation, not established by the unit fixture. |
| WI10 applied-but-unsaved | Outcome/command fixtures and runtime reapplication controls | Add selected command smoke evidence if exercised by existing gametests; controlled persistence-failure wording remains covered deterministically unless separately invoked. Never claim a rollback behavior that was intentionally not introduced. |
| WI11 disguise replacement | Production resolver with separate defining loaders and actual fixture instances | Conditional optional-plugin smoke DEFERRED: no controlled supported replacement setup. Full server restart would not test this lifetime. This need not imply ordinary plugin startup is unvalidated. |
| WI12 matching Voxy reset | Exact holder/method resolution and invocation controls; verified jar descriptors | Matching 1.21.1 profile reset/probe with ingestion resumption still unrun. Inventory/bytecode does not establish loaded mod or visible ingestion. Forced destructive wipe is a separate conditional procedure, not automatically required when ordinary reset/probe suffices. |
| WI13 harness | Actual-script isolated refusal/ownership/provenance fixtures and checker selftests | At least one genuine supervised benchmark smoke with validated current-run manifest is expressly required by plan WI13 completion. No such completed manifest was supplied in the inspected record. This is the main untested runtime boundary in the changed orchestration itself. |
| WI14 validation routes | Current profile metadata/hash inventory and operational docs; both actual 1.21.11 C2ME profiles/vanilla reported 77/77 after fixture diagnosis | Link final profile/version evidence and older-line opt-in checks when complete. Inventory certifies configuration, not GUI operation. Separate unverified Moonrise localRuntime remapping risk remains outside this repair. |
| WI15 wing animation | Real engine wing-state controls and Tier2 on both affected Fabric lines | Equipped-target stand/crouch/glide visual smoke remains unrun. Cosmetic runtime claim must remain bounded. |

## Selection limits

The plan expressly says not to run every existing scenario. The parent's prepared selection reasonably combines Paper offline-edit with representative Paper store smoke and avoids redundant store-second-join. Additional warm-rejoin-summary or wide-distance hybrid-boundary can be conditional/deferred with a stated coverage limit; small-radius/client unit greens do not establish wide-distance backpressure. Existing support-tier cuts and nonshipping renderer stubs are not missing implementation. Folia has no 1.21.1/1.21.10 lane; a representative 26.2 single-player smoke preserves the experimental label and does not establish concurrent multi-region acceptance. Optional plugin/Voxy configurations require the matching setup before their conditional live claims; their absence does not turn bounded deterministic fixes into proven runtime failures.

Merge, publication, release tags and deployment are separate later operational actions, not missing gates for this implementation review. The protected regular-map server/world must remain outside disposable test fixtures.

## Artifact result

`final-artifact-inspection.json` / `.md`: five lines, 30 exact 0.14.0 versioned jars, all PASS. LSS/VSS classes and nested libraries/common agree byte-for-byte within each loader pair; every Neo SQLite/Zstd jar matches cached stock Maven bytes. Platform Java targets are 21/25 as appropriate; common remains Java21 everywhere. Namespaces/metadata/shipping flags are recorded. No stale unversioned artifact was inspected. Artifact checks establish packaging identity, not graphics or optional-mod runtime acceptance.
