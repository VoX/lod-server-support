# Primary native Xaero map fixture staging

Primary1.21.1 Fabric remapJar and Neo named packaging compiled successfully. Sources and raw checker/terrain helpers are maintained across all five trees, but this fixture targets the primary1.21.1 native pair only. No live map assertion or user review has passed. Evidence: /home/vox/.local/state/lss-project-improvements/xaero-map-fixture-builds/1788997193910735413.

Exact native inputs inspected: Fabric Xaero World Map1.45.0 SHA256 ea84c45cb1b994a7738515670b281bcd373a9d9cc3d8d3a24b84a4baf302b9d6; NeoForge1.45.0 SHA256 d60899112e84616ba52a993c1722fd891872acc4779bfac34d9b170001451c6f. `native-methods.json` records actual method descriptors/call graphs on both; `native-save-callers.json` records Fabric native saver ownership chain. MC descriptors differ by loader; fixture native injection handlers deliberately do not name MC argument types.

`fixture` has Fabric remapped and Neo named packaging modeled on the verified external WI9 fixture. All injections are required. Real native classes are observed using @Pseudo without compile stubs. It never changes region load state, marks a map tile loaded, inserts a tile, edits native slope/color fields, or calls an LSS internal scheduler. Its sole timing intervention is a one-shot 3-second delay inside an actual targeted `MapSaveLoad.saveRegion` call, after native `MapSaveLoad.run` has pushed the region writer pause. It refuses to delay if the pause is absent or the current saver still holds the pause monitor. The native saver owns pop/recovery. Save exceptions remain native exceptions; the fixture does not fabricate a success return.

The native texture observer copies the actual direct color buffer after `MapTileChunk.updateBuffers` returns. StackWalker distinguishes actual `MapWriter.writeChunk` native production from a product bridge rebuild. Both exact target chunks31/32,z16 must have independently expected256 floor heights, native slopes and complete color buffer equal to a later real native-visited rewrite. Equality of slope bytes alone cannot establish shading; all native buffer bytes must also match. This deliberately fails if neighboring tile coverage differs; prepare all tile-chunk28..35,z16..19 and halo before capturing the baseline.

The raw checker never substitutes an expected map for a missing actual texture and never turns a missing save overlap into a skip. It requires fresh wire body observation before a committed prepared tile, native-owned writer deferral during the bounded save window, no commit in the save interval, actual successful save return and matching final edited tile recovery. Independent server oracle must include seed completion, actual native-visit start and both256-element expected height arrays. Capture world-map screenshots before emitting semantic proof. User visual review is a separate required artifact and is not supplied by this fixture.

## Recipe to finish and execute after a build slot

Run all four final primary locks: mc1211-fabric-modern, mc1211-fabric-legacy, mc1211-neo-modern and mc1211-neo-legacy-xaero, both exact native Xaero1.45.0 artifacts and the current shipping candidate. Compose the separate owned Fabric server/bootstrap already used for UI. Declare every staged fixture jar and SHA in runtime.stage_files. Runtime flags `-Dlss.xaeromap.enabled=true -Dlss.xaeromap.evidence={run_dir}/evidence/xaero-map.jsonl` apply to the client, alongside the existing runId. Receive LODs, enable Xaero writing and Xaero load-new/update settings via declared fresh run config; preserve native map shading settings for baseline/native comparison. Render distance must be pinned4; LOD distance32; generation disabled; chunks seeded through owned server console commands.

Prepare a non-tinted gold ramp over x432..591,z240..335 with height64+((x-432)%8), clear y64..96 before filling each x stripe, and keep all corresponding server chunks loaded. Each fill stripe remains below vanilla command block-volume limit. The targets are the full256 pixels of chunk31,z16 and chunk32,z16, across a native region boundary. Starting spectator atchunk16,z16 keeps them outside vanilla tracking. Independently record console receipts, exact terrain formula and target readiness times. Wait for initial wire→bridge→texture evidence for BOTH chunks, then teleport the same real client to chunk31,z16 through owning server console and require native writer snapshots matching bridge pixels/slopes/buffer. This is the independent native reference, not an injected tile comparison.

Move back to chunk16,z16, then have the owning driver create the declared run-owned arm file (lss.xaeromap.arm); this prevents an earlier natural save from consuming the timing intervention. Wait for the native saver to enter the fixture's actual pause. A run-owned asynchronous command driver watches the raw `save_pause_begin` row and edits a target ramp to diamond, recording the actual console response/time; follow with actual `save-all flush` to exercise the native chunk serializer dirty hook; the observed second body/bridge content must prove the dirty update really occurred. Keep waiting for paused DEFERRED, native successful save return and edited bridge COMMITTED. Do not manually call saveRegion or add regions to getToSave; that bypasses native scheduling. If natural save doesn't occur before the registered deadline, this attempt is inconclusive/failed, not passed.

## Explicit unresolved seams / next controls

1. Fixture compilation/remap and Neo named packaging pass; all required mixin applications still need native startup validation on both loaders. A build does not establish a native API/gate pass.
2. Exact native save cadence is inspected and recorded in xaero-native-save-cadence.json (normally60s, fast-writing10s); live selected cadence is observed from the actual processor. An actual paused DEFERRED remains unverified.
3. Scripted console mutations do not themselves fabricate Bukkit/Fabric block events. Need use a real native save/dirty route or actual client block edit and verify fresh second body. A command acknowledgment alone is not a dirty/fresh-body proof.
4. Full color-buffer equality is intentionally strict; if unrelated tile coverage differs, fix preparation/capture eligibility, never drop the RGB equality gate.
5. Raw writer shutdown hook now enqueues a bounded drain marker, waits for writer close, emits observer_closed and reports CLOSED overflow=false pending0. The raw checker requires that final close record; forced process termination cannot pass. Native validation of this hook remains pending.
6. The maintained scenario/proof adapter now binds raw oracle/evidence hashes, exact profile/run identities and semantic assertions as described below. Its native live route and final closed-stream behavior still require actual execution; it never creates human review.

## Retained user review protocol

Use maintained native_window.find(run, game_relative) and guarded XInput.focus for the actual owned Minecraft window, then capture a PNG before first proof. Semantic proof must include `review_artifacts: {visual_render: {artifact: 'xaero-map-boundary.png', artifact_sha256: '<actual PNG SHA256>'}}`. All machine evidence, assertions and artifact bindings remain frozen after first proof. Only the actual user's response goes into `reviews`; no agent screenshot inspection substitutes for that response. After normal cleanup the runner may enter awaiting-review, releasing every process and tools freeze. Later user-bound finalization uses the retained `run/tool-sources/tools/rig/rig.py review` implementation so current tool drift does not change the original checker. See maintained docs/testing/visual-review.md for exact schema/command.


## Maintained v2 execution adapter

The four-profile composer, owned controller and report dispatcher are now maintained as prepare_xaero_map.py, drive_xaero_map.py and check_xaero_map_run.py. The controller records actual commands/seed times, captures the guarded native map PNG before the native visit, waits for independent buffer equality, returns the player and arms the real native save delay. It edits only the region actually being saved; writes in another region remain legal. Server generation uses the actual enableChunkGeneration=false key.

A live proof binds a finite verified raw prefix while its writer remains open; it cannot finalize outside status=running. The supervisor then closes the native process and rebuilds the report from the complete closed stream, preserving earlier failures. Only that final closed report can become awaiting-review. No user review is generated. The ordinary retained user review flow is required afterward.

V2 Fabric remap and Neo named compilation passed at /home/vox/.local/state/lss-project-improvements/xaero-map-fixture-builds/1788997862434326848. Fabric SHA256 c22d8abf821422314699338caa6e03b26a2e4e6a6d73d0e0e3e35a2be5aa1160; Neo SHA256 14c445161567199d34827d186e4515206308bd3815c2c49f37f9ffc5003c7ff8. Nine raw/recipe unit controls pass. Actual native startup, save overlap, map content and visual review remain unverified until their owned runs execute.
