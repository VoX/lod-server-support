# Project improvements implementation ledger

Status checkpoint: 2026-09-12: all five current pairing-fix product build/test gates passed, including 90 new passing pairing cases. The corrected reference is build-qualified and six new functional recipes have READY plans; baseline run `20260912T022001Z-00789dbae475` is ACTIVE at this checkpoint, without acceptance. Performance remains unaccepted and no numeric preset is selected. Historical failures remain terminal and unchanged. **Implementation and acceptance remain in progress.**

The [implementation plan](../planning/2026-09-09-project-improvements-implementation-plan.md) governs all five isolated Minecraft lines: 1.21.1, 1.21.10, 1.21.11, 26.1 and 26.2. The implementation request supersedes the plan's original hold language. Merge, publication and deployment are outside this task. [Baseline identities](baseline.json) and the [unchanged chronological ledger](project-improvements-ledger-history-2026-09-10.md) preserve earlier decisions, attempts and evidence links.

| Package | Implemented or prepared | Remaining acceptance |
| --- | --- | --- |
| P0: baseline | Five source, artifact, ownership and testcase inventories | Bind final evidence to exact final inputs |
| P2–P3: compatibility and line maintenance | Exact catalog/resolver, generated views, explicit drift classification and isolated port workflow | Final content/ref binding, generated views and independent residual adaptation review |
| P7a: settings | Descriptor inventory, correlated effective snapshots, apply/undo and save-failure feedback | Final candidate gates and independent review |
| P4: disposable rigs | Owned display/input/process lifecycle; launch journal, immutable measured-slot claims, raw report reassembly, durable writes and physical host storage guard | Six current plans READY; corrected-reference baseline sustained ACTIVE, new functional acceptance pending |
| P6a: test migration | 67 pure suites moved; Java 21 and case-accounting controls | Final full checks; one timing pair establishes no speedup |
| P1: status and exports | Immutable status UI/commands and bounded lifecycle-safe exports | Measured impact and final candidate acceptance |
| P5: Xaero ownership | Thin facade and five ownership components | Final relevant native checks, actual human visual reviews and measured impact |
| P6b: reference measurement | Historical calibration failed terminally; new pairing-corrected reference build-qualified, six functional plans READY | New reference/candidate functional results and separately bound performance remain pending; no accepted measurements or numeric derivation |
| P7b: presets | Qualitative client/restart presets implemented; conservative command design and tests staged externally | Actual P6b numeric derivation, conservative preset implementation and native apply/undo/restart checks |

## Evidence already retained

Earlier artifact identities passed 14,724 unit cases, 436 server gametests and four available client gametest runs; see [retained build gates](final-build-gates.json). These precede later changes and do not establish final whole-plan acceptance. Historical native evidence includes server15, C2ME2, UI12, WI5 four, WI6 two, no-Sodium2 and six server preset/restart runs. The chronological ledger retains their exact records and limitations.

Both fresh export lifecycle runs passed automated proof and owned cleanup: Fabric `20260910T145008Z-9571f6a53a6e` and NeoForge `20260910T145141Z-590e96ddaba3`. Their private receipts are under `~/.local/state/lss-project-improvements/recovery-20260910/export-fix/native-evidence/`. Map four, seated two and Elytra two earlier runs passed their raw checks but still await actual human assessment; no continuation message counts as visual approval, and changed final artifacts require applicable fresh evidence.

The physical host storage guard passed 81 related Python controls, including 15 new storage controls, and five actual host preflights. See [storage protocol](physical-host-storage-guard.md) and private `storage-guard-staging-20260910/` receipts. Interrupted and failed runs remain retained, including the second-reboot create with 134 zero-byte files and no manifest or native launch.

The guarded Fabric run `20260910T154540Z-024aae6e69c8` passed 28/28 source targets and collected owned cleanup, with new-boot accelerated GPU evidence and four actual owned native-library mapping proofs. This is a historical functional pass for its recorded fixture bytes, not acceptance of the subsequently revised fixture or a performance result. Recipes and receipts remain under `~/.local/state/lss-rig/source-functional-storage-guarded-20260910/`.

Two sustained Folia attempts remain failed with complete collected cleanup. `20260910T155543Z-44252376538d` stopped at the pending-edit bound of 128. The exact-anchor early return was strongly implicated in stale owner snapshots, but failure-time coordinates were not captured and the specific cause remains unresolved. The subsequent owner-refresh derivation, `20260910T161541Z-227000bbe71a`, reached all 23,040 offers but included 12 failed edits after the disconnect boundary, plus independently rejected wrong-source and expired-original-revision commits. Its receipt is under `~/.local/state/lss-rig/source-functional-owner-refreshed-20260910/`. Reaching the offer count did not pass the workload.

The staged server repair defers an edit before mutation when its current native owner is unavailable; the staged client repair checks expected source, block and parsed original application intervals, retries eligible mismatches once per active callback, and retains expired obligations. Both repairs passed independent implementation review and their Java controls, and were integrated across all five lines. Eight fixture builds passed with corresponding candidate/baseline class and resource parity. Exact sources and artifacts are recorded in private `recovery-20260910/combined-fixture-repair/fixture-index.json` (primary source commit `e3f2411ff038d84e7b933395fea7fb09c2e6369a`). Fresh native acceptance remains pending. Cached journal snapshots can lag a successor event, so the unchanged independent full-journal interval checker remains authoritative. Neither failure is waived, and no deadline, schedule or every-edit requirement is relaxed.


The third sustained attempt, `20260910T170234Z-f47420f5afe4`, remains **FAILED** with complete owned cleanup: 972 missing original targets (D 960; B 12). Wrong-source commits, expired-revision relabeling and failed edits were absent; removing those fixture errors did not establish every-edit delivery. The following 260-second diagnostic, `20260910T173913Z-1d889744f593`, also remains **FAILED** and cannot qualify the fixed 840-second workload. Its bounded opt-in telemetry found both route-only and wrong-block rejections. D measured501 received source1 with the expected sampled gold block inside its actual original revision interval, yet the source0-only fixture rejected it. D source-only and combined source/block buckets each truncated at 32 details; these samples cannot establish aggregate rates or explain every missing target. All five recorded PREAPPLY cases arrived before actual mutation and had a mismatching sampled block, so a correct-body rejection solely from delayed journal observation was not demonstrated. Exact joins and source hashes are retained in private `recovery-20260910/sustained-owner-diagnosis/diagnostic173913-interpretation.json` (SHA-256 `52fba65b1e05744aa93fda1e9b6ddef71e26294a8f979845965810f715a33ca3`).

The measured-source policy is a **fixture/checker correction**, separate from product behavior: repeated edits explicitly permit supported loaded/disk/store sources `[0,1,3]`; controlled initial source0/1/2/3 assertions remain exact. The raw checker still requires expected content, original application/successor intervals, native receipt/session authority and every-edit/deadline proof. Normalization preserves the actual route as `delivery_source`, retains original policy fields and compares content only after raw validation. The all-five integration receipts are private `measured-source-policy-staging/integration.json` and `measured-source-normalization-staging/integration-receipt.json` under `recovery-20260910/`; normalization has 68 staged Python controls and five integrated controls passed. Earlier failed records remain unchanged and are not retroactively accepted.

A separate **production guard** addresses a deterministic stale-probe restamping hole: loaded bytes serialized before a queued dirty invalidation could previously enter a later snapshot, restore a timestamp and set the done-bit after that invalidation applied. The old-product red control is preserved. The integrated guard binds exact column/dimension/registration and one of 4096 fixed invalidation-stripe tokens before serialization, rejecting stale probes before data or all-air stamping. Collisions cause conservative extra misses; this is an invalidation-queue epoch, not immediate freshness after every block write. Native ownership, unconditional one-tick Folia release, ingestion retry cap and dirty cadence remain unchanged. Whether either native diagnostic hit this exact serialization race remains **UNPROVEN** because serialization-time evidence was not captured. Independent implementation review and staged controls passed: 94 common, 28 mapped Paper and seven mapped Fabric tests. The frozen stage manifest is private `recovery-20260910/loaded-probe-guard-staging/manifest.json`, SHA-256 `5adea8d7360323684732e0ca521d80cf02cd78604ee7b62947dd1ec7d35ad372`. All five source trees matched those integrated bytes; the later build and native outcomes are recorded below.


The subsequent **pre-performance guard gates passed on all five lines**, including applicable unit, server/client gametest, release and artifact-exclusion checks. The first full-build manual-probe setup failure and subsequent accounting-only outer-gate failure remain retained. Narrow direct-probe fixture adaptations preserve their assertions; the migration inventory now explicitly declares the one later regression test, retaining the original baseline XML and all original case/outcome checks. The successful batch is private `recovery-20260910/pre-performance-guard-build-preparation/pre-performance-loaded-probe-guard-migration-accounting-20260910/`. These are completed guard gates for their exact inputs, not final acceptance of a future measured preset or revised workload.

Eight paired fixture builds also passed with stable source/compiler inputs and independently checked full ZIP-entry parity between baseline and candidate builds. Private `recovery-20260910/post-loaded-probe-fixture-rebuild/fixture-index.json` has SHA-256 `ba002532a95d874a1fe63b2d9001171b0facfef07b951a0eed5878de26cd9197`; `root-independent-fixture-verification.json` records six source groups, all eight compiler inventories and actual product/fixture output checks. Separately, exactly one **nonfinal** WI5 compile verified the new fixture-build hook against actual source, product and compiler inputs; see private `recovery-20260910/fixture-hook-preparation/root-execution-review.json`. That hook control is not one of the final-native fixture jobs and does not establish final native acceptance.

The fourth full sustained run, `20260910T191341Z-ce650607d848`, remains **FAILED**: 21,475 valid commits and 1,581 missing original targets (D 1,551; B 30), with no invalid source, ACK-order, wire, lease or original-interval commits found by the independent audit. All 23,040 measured edits and four initial loaded-cell edits applied; no edit failed. The controlled initial four-source assertions passed. Separate terminal supervisor evidence confirms complete cleanup with zero remaining children; the original manifest still says supervisor-pending and remains unchanged. The other four current functional runs did not start. Diagnostics were disabled: body length does not identify decoded block content, and neither probe serialization timing nor the precise cause of every missing body is established. Exact original intervals, source pins and audit are retained under private `recovery-20260910/sustained-191341-diagnosis/` and `fixture-audit-191341/`.

The remaining workload issue is distinct from the deterministic product guard: pinned product behavior permits transient pre-edit **unsaved disk data** while the owner probe is suppressed; normal recovery depends on save/dirty notification or reconnect. The fixture mutates and marks dirty without establishing that each measured revision reached readable native storage before its next revision. Consequently this workload did not establish its saved-update premise. This does not turn its failures into passes or prove that each rejected native body was stale disk content. The subsequent save200 workload variant and its bounded evidence are recorded below. Earlier failed attempts retain their original configuration and obligations; no per-edit persistence guarantee is inferred from the new interval. The product retry cap, one-tick release and original failed-run obligations remain unchanged.

The reviewed `native-autosave-200-cap24` workload variant configures Paper/Folia auto-save at 200 ticks, capped at 24 chunks per tick, with region flushing disabled. The fixture observes the actual resolved settings on the legal native owner before readiness. This is a setup witness, not proof that every edit was saved within a fixed wall-clock interval. Both performance arms receive the same variant; Fabric retains its explicit vanilla-save adapter. The 23,040 measured offers, original revision intervals, source/content rules, fault schedule and every-target requirements remain unchanged. Seven Java fixture control suites, including the setup-witness suite, and the Python setup controls passed; eight newly built paired fixtures passed source stability and complete ZIP-entry parity checks. These inputs remain externally staged until final integration, with unchanged shipping product identities.

Run `20260911T171420Z-9578fcf166d3` then completed all 23,056 target obligations (23,044 applied edits plus 12 initial non-edit targets), including the disconnect, admission-pressure and slow-consumer phases. All four native clients used owned accelerated GPU contexts and independent native-library paths, and cleanup completed with no remaining children. Native execution and raw checking passed, but collection **FAILED** with `mixed-source raw report missing/stale`. Independent fresh processes with Python hash seeds 1 and 2 reproduced a sole difference: the ordering of the same 96 progress-window rows. Every other report field was equal; the producer iterated a subject set. This establishes a report-order defect, not a failed delivery or accepted collection. The failed result, original proof and raw evidence remain unchanged in private `recovery-20260910/native-save-report-order-diagnosis/`.

The minimal correction sorts subject iteration without changing any window, outcome or acceptance criterion. A fresh-process regression compares the entire normalized result under four hash seeds and pins all expected window identities/counts; it fails with the old producer and passes with the proposed correction. The ten guarded source/test placements are staged in private `recovery-20260910/report-order-staging/`. The fresh corrected-tool run `20260911T174706Z-2c6fb1e2d4bf` passed native execution, exact collection, all 23,056 target obligations and complete owned cleanup. All content, original-interval, source, progress, debt and region-ownership checks passed; measured debt drain was 1.150008634 seconds. The accepted record remains bound to its original product and tool identities. The remaining four functional lanes and performance binding were held for the subsequent product repair below.


A later source audit found that server diagnostic export completion callbacks retained native command recipients, which can retain the server, world or entity throughout blocked filesystem work. This is a source-proven lifetime violation, not a reproduced native shutdown leak. The repair was independently reviewed and integrated on all five lines: commands now acknowledge the allocated target after queue admission; static completion logging holds no recipient or live world. Completion or sanitized failure appears in the server log. Client export feedback and lifecycle behavior remain unchanged. Eight common execution controls and four bytecode controls in each Fabric/Paper module cover the new path; the new full-gate batch is private `recovery-20260910/pre-performance-guard-build-preparation/pre-performance-server-export-lifetime-20260911/`. Only actual completed receipts establish each line's result. Fresh candidate recipes and performance inputs must bind these rebuilt products; the earlier sustained pass is not relabelled.

The new server-export candidate passed full build/test gates on all five lines, including all 80 added server export tests, with stable source identities. Its sustained run `20260911T184503Z-008947200b0c` then **FAILED**: 23,055 of 23,056 targets committed, with complete owned cleanup. D3073, revision 13 at chunk (780,-7), received one allowed-source body and reported a rejection; no replacement arrived before the original successor interval ended. Exact decoded content and the callback's cached application fact were not recorded, so the rejection cause remains unknown. The independent report is private `recovery-20260910/d3073-independent-diagnosis-184503/REPORT.md`.

The bounded unknown-application fixture correction passed independent review and four pure Java control suites, including an old-policy mutation that fails as expected. It waits for real application evidence under the original deadline, then checks the original arrival time and interval; known pre-application or wrong-content responses still reject. The product retry cap is unchanged. Eight new paired fixture builds passed with stable source/compiler inputs and full baseline/candidate ZIP-entry parity; see private `recovery-20260910/deferral-only-fixture-build-preparation/fixture-index.json` (SHA-256 `bbf05683fda237536aa58cb3d3b0b455a73eda63ad3dca727002bc283771717e`). This is compiled fixture evidence, not a demonstrated explanation of D3073 or a native pass.

The subsequent sustained run `20260911T194345Z-4dbc645e46cc` **FAILED**: 21,018 of 23,056 targets committed. All 2,038 missed targets belong to D; A, B and C each committed 5,764, while D committed 3,726. No failed edits or other raw source-check errors were reported. The debt subcheck passed at 1.100005044 seconds, while the overall source/progress proof failed. Owned cleanup completed with zero remaining children. The [raw failed report](/home/vox/.local/state/lss-rig/runs/20260911T194345Z-4dbc645e46cc/evidence/source-correctness.json) and [cleanup receipt](/home/vox/.local/state/lss-rig/runs/20260911T194345Z-4dbc645e46cc/supervisor-cleanup.json) retain the authoritative result.

The [2026-09-11 active-run prefix diagnosis](/home/vox/.local/state/lss-project-improvements/recovery-20260910/d-deficit-prefix-194345/REPORT.md) is a bounded, sequential prefix analysis, not a final root-cause report. Its first missing original target, D124 at (787,4), had no same-cell wire arrival inside its original applied-to-successor interval; D125 and D126 show the same pattern. These intervals precede all scheduled faults. Two early holds had settled before D124 was offered, with no recorded overlapping hold. The first 90 seconds contain 1,800 owning ticks for D's region, with approximately 50 ms spacing; this does not support regional tick slowdown for those early misses and does not prove a per-chunk save. The evidence does not establish the cause of either this run or the earlier D3073 rejection.

The separate 260-second diagnostic `20260911T200650Z-cbe5dde85e13` completed collection and owned cleanup with zero children. All four clients emitted actual CALLBACK, REPORT and OWNER hooks with no diagnostic error or overflow. This deliberately shortened diagnostic does not satisfy the required 840-second workload. Selected cell (787,4), revisions 1–8, committed via source0 and did not reproduce the earlier missing D124. A different initial D target at (776,8) produced a sampled gold block although the oracle expected diamond; receipt209's actual report and owner records show the failure counter increasing 0→1, with later retained evidence reaching 3→4 and parking. Arrival was after the recorded edit, but serialization time was not captured: neither post-edit serialization nor product corruption is established. Two other first bodies arrived before their edits; deferred application knowledge cannot make those arrivals current. Retention is bounded and missing rows remain unknown. See the [diagnostic source report](/home/vox/.local/state/lss-project-improvements/recovery-20260910/diagnostic200650-analysis/report.json) (SHA-256 `e8269169554edd410a07f9f67b18c020f2715d519ed3bfe05f50da70cae9d0eb`) and [independent review](/home/vox/.local/state/lss-project-improvements/recovery-20260910/diagnostic200650-independent/REPORT.md) (`3bfa84fecfd5f042c118fb6092d9e25738964ba260ec4aa3b5e09b708941b3a0`).

The [passive-observer source audit](/home/vox/.local/state/lss-project-improvements/recovery-20260910/passive-observer-root-audit/REPORT.md) identifies a fixture/API mismatch: receipt failure reports mean consumer ingestion failed, while the external oracle was reporting successfully decoded older or mismatching bodies as storage failures. Product dirty-crossing contracts permit older in-flight bodies and subsequent natural refresh. The approved correction observes source/block/pre-application mismatches without target credit or product failure reports; malformed association/clock evidence still fails the fixture. Strict full-journal content, source, original interval and native authority checks, unknown-application deferral and scheduled faults remain unchanged. Five Python controls and four pure Java suites passed. Approved source manifest: `50c934bbe7ae6b8965cb8bd26c1300565118c61f33cfb39baa66b85e905ed521`.

The candidate-only passive client fixture compiled and passed its compiled-reference check: JAR SHA-256 `bf2feff1c5f0e868cfbc3b0258113df38ac592353418a9723f762bdb96b2f2a1`, 78,248 bytes. The [root build review](/home/vox/.local/state/lss-project-improvements/recovery-20260910/passive-observer-client-build-preparation/root-build-review.json) (`c679915c3ff337318bd93881a69cffcbb86b40751cc7a41221c9e9ed24567eba`) verifies stable source/compiler inputs, no report-method references in the compiled ClientProbe family, unchanged resources and no diagnostic classes. This is not a paired performance build or native acceptance.

At the retained 2026-09-11 checkpoint, all five then-current functional prerequisites **PASSED** with exact candidate target matching or separate baseline binding and complete owned cleanup. These historical runs use their recorded pre-performance products and the passive client fixture; they do not substitute for the later final candidate matrix or measured performance acceptance.

| Functional gate | Run | Target result | Exact receipt |
| --- | --- | --- | --- |
| folia-sustained | `20260911T203446Z-2c8fe6231a02` | **PASSED 23,056/23,056** | [strict receipt](/home/vox/.local/state/lss-rig/source-functional-passive-observer-20260911/folia-sustained/attempt-1/acceptance.json) (`8d40f572a204ca2bd1b97daaee0427b65ebb6182311f858c68a14a631a986593`) |
| fabric | `20260911T205059Z-ef8ad435cd5a` | **PASSED 28/28** | [strict receipt](/home/vox/.local/state/lss-rig/source-functional-passive-observer-20260911/fabric/attempt-1/acceptance.json) (`806dc17afcbad3e92ab320ce4084085480d4d271825b3787e909c33d78e3ff1d`) |
| baseline-folia-sparse | `20260911T210159Z-519e16b1ebfb` | **PASSED 28/28** | [strict receipt](/home/vox/.local/state/lss-rig/source-functional-passive-observer-20260911/baseline-folia-sparse/attempt-1/acceptance.json) (`46d97409f86bb16525970599b7f44fb853e07e0ef45b2578e63770e2079c3ffc`) |
| paper | `20260911T211259Z-6632780359f7` | **PASSED 28/28** | [strict receipt](/home/vox/.local/state/lss-rig/source-functional-passive-observer-20260911/paper/attempt-1/acceptance.json) (`cdec7a6ad8f9455391cfdec875ff684a548939c397216d8abef7f3df8cbd4db5`) |
| folia | `20260911T212340Z-f977539a6f09` | **PASSED 28/28** | [strict receipt](/home/vox/.local/state/lss-rig/source-functional-passive-observer-20260911/folia/attempt-1/acceptance.json) (`f7af589e03fceb4a92cd9ecc31d457f0acb870b2c1cd12f399b8eba2885274a3`) |

All earlier failed runs, including `184503` and `194345`, remain failed. The passive correction and new passes do not establish the cause of every historical miss. Paired fixture build/packaging evidence remains a separate requirement; accepted performance results and numeric selection, final integration/build/native/visual gates, and the six fresh final independent reviews remain pending. The separate earlier callback/report/owner hook instrumentation is excluded from the pending 203-placement integration and performance inputs. The passive client JAR retains its existing bounded opt-in rejection logging; that flag is disabled in performance and enabled only for the separate 260-second diagnosis.

## First selected baseline calibration failed (2026-09-11)

The registered v13 campaign stopped at baseline calibration slot 0, run `20260911T220257Z-1d032cea8bf8`: **FAILED**, 22,900/23,056 targets committed, with 156 missing deliveries (B: 10; D: 146) and a failed progress check. Owned cleanup completed with zero remaining children. The debt and owning-region subchecks passed, which does not pass correctness. The [first-failure summary](/home/vox/.local/state/lss-project-improvements/performance-protocol-recovery-20260910/execution-v13-passive-save200-identity-20260911/first-failure-summary.json) (`84264dbcba14724b0a8e6fbda6919eef4d003b06d1e44935409f9d577aad22d1`), [independent report](/home/vox/.local/state/lss-project-improvements/recovery-20260910/baseline-calibration0-independent-220257/REPORT.md) (`d1ff1d96130171be712917894633111129ac50ebb9800d356595418eeeaf2540`), and [root review](/home/vox/.local/state/lss-project-improvements/recovery-20260910/baseline-calibration0-root-review-220257/root-review.json) (`e302d56d03d4065725e23f9ea0d856adfa727780c7603f6c7df1faf109556f42`) retain the exact outcome. The cause remains unresolved; permitted-source bodies observed in failed intervals do not establish their decoded contents or serialization time.

This first selected failure is terminal: no replacement slot or automatic continuation is permitted. Calibration floors were not frozen and later selected slots did not run. There is no accepted performance result or measured numeric preset. The five current functional passes above remain valid for their own recorded runs; they do not replace the failed baseline calibration. Baseline identity, protocol and budgets remain unchanged.

The controller never reached metric assembly. A separate [post-failure sample diagnostic](/home/vox/.local/state/lss-project-improvements/recovery-20260910/baseline-calibration0-metric-diagnostic/raw-samples.json) (`6fbcb7578fb7764ddbbbda0d39db2883deedca08a6c99cf38afffc0f8c39d2e3`) found sufficient frame/tick and RSS observations with no detected raw sample errors, but strict target normalization still refused the missing outcomes. It supplies no valid throughput report, calibration floor or performance acceptance. A separate 260-second diagnostic `20260911T222741Z-de9131eebc2a` completed with owned cleanup and zero children. Its [independent report](/home/vox/.local/state/lss-project-improvements/recovery-20260910/diagnostic222741-independent/REPORT.md) distinguishes 147 closed-interval misses from 220 shutdown-truncated obligations; it is not a replacement selected slot or full-workload acceptance. The [root review](/home/vox/.local/state/lss-project-improvements/recovery-20260910/diagnostic222741-root-review/root-review.json) retains the bounded predicate evidence and unresolved serialization/content limits. Separately, [deterministic differential controls](/home/vox/.local/state/lss-project-improvements/recovery-20260910/baseline-correctness-control-staging/controls-attempt-1/results.json) recorded three original-baseline assertion failures and a passing fresh-probe control; the corrected control passed 103 Fabric and 28 Paper cases. These establish the tested baseline guard defects, not the cause of the original 156 native misses. The separately scoped corrected-reference comparison excludes guard costs versus the defective original; its full-build/native/preregistration gates require their own actual evidence.

## Current pairing-fix checkpoint — 2026-09-12

The paired-ingress production repair binds a released Folia declaration to its ready probes so routing cannot consume fresh ingress before the bounded probe hold or lose the ready probes when a later snapshot replaces an earlier one. Deterministic tests cover these orderings, generation/invalidation and retirement, while preserving unconditional one-tick release and legitimate disk fallback. This fixes the tested source-level races; it does not establish the cause of every historical native miss or promise a 32-second persistence deadline. The original workload, source/content/original-interval acceptance rules and ingestion cap remain unchanged.

Current five heads are `1.21.1 6bb453f5c381ba652703dd705c46a908285c4e44`, `1.21.10 408950d11a610d667865b1e84418fbeec608c491`, `1.21.11 c7f0839e84fe54771a7676c5aa1396f6a393f341`, `26.1 695e0b2c22f19f6842ff549cb2500677429d05e6`, `26.2 c6475760035777c387ccb19e5f009a716449c0f1`. Their full build/test gates passed with stable source/compiler/product inputs. All 90 added pairing cases passed exactly once under the appropriate common/Paper tasks in fresh XML (18 per line); this is separate from the 67-suite migration baseline. The independent actual gate audit is `recovery-20260910/paired-five-gate-independent-audit/report.json`, SHA-256 `50a6607a37990988ae513e6ff90772cdd89b797540e05b18e39068d46109b510`.

The separately scoped corrected reference is commit `2f94fcdf2aab72ce6f2adfe283d02f7911dd6c66`, tree `52cc7a76231ab153fdd7876ae90835355c412794`. Its actual full qualification is `folia-paired-ingress-fix-staging/qualification.json`, SHA-256 `c91cb3d534a1a9d56239dbbe055935f07cbd46aca9c94f90757719801bd83684`; Git-object receipt SHA-256 `133af26a7b693417eb995b7132723f8d77e30a873cada483f6e451a50c3fa253`. This remains a separate reference with explicit correction-cost attribution limits, not a rewrite of defective-original results.

Eight fresh paired fixture builds passed with 53 source files and complete class/resource entry equality. Raw index `pairing-corrected-phase1-staging/build-20260912T020017Z/fixture-index.json` has SHA-256 `8e537360b378ec5f4a8f3db849a5f5f3453ae0524656eafcc2822c25274b39df`; the separately derived metadata-reproduced index has SHA-256 `bc0e7985bc7ae2dbc9ff43297a9a824c7993fe24896eb1038b97f2a9d6023e11`. Raw outputs remain retained. The independent raw audit is `paired-eight-fixture-independent-audit/report.json` (`038125deae42de64e1927e201ad6c4887cba368c2d6f06356b087df368b273fc`).

Six actual recipes under `~/.local/state/lss-rig/source-functional-paired-ingress-20260912/` preserve donor workload/settings and use current identities; index SHA-256 `5112b5a0a7b2ecc20011f4ebc6850710d19fd8d762a55caf2fbdbaaafa18abec`. All six normal plans are READY in `paired-ingress-native-invocation-preparation/plans-attempt-1/summary.json` (`1967c86685ef9b1bdfd729f2e0568451111a3ac7c014dc9842628e082dbadef6`). Plans are not acceptance. Baseline sustained run `20260912T022001Z-00789dbae475` is ACTIVE at this checkpoint; no completion, cleanup or accepted result is claimed here. The remaining new functional outcomes, accepted performance, 203-placement integration, final 52-row native matrix (42 required + 4 supplemental + 6 preset runs), eight actual human reviews and six NEW Astra reviews remain pending.

## Required completion record

| Gate | Status / result to fill from accepted evidence |
| --- | --- |
| Current Fabric sparse source | **PENDING** on new pairing identities; prior 28/28 is historical |
| Current Paper sparse source | **PENDING** on new pairing identities; prior 28/28 is historical |
| Current Folia sparse source | **PENDING** on new pairing identities; prior 28/28 is historical |
| Sustained candidate Folia | **PENDING** on new pairing identities; prior 203446 pass remains historical |
| Baseline Folia functional linkage | New corrected reference: sustained 022001 **ACTIVE**, sparse **PENDING**; no acceptance yet |
| Reference performance | Original first calibration **FAILED terminally**; corrected-reference performance **UNACCEPTED/PENDING** |
| Conservative numeric preset | **PENDING** — no numeric values selected; derive only from accepted reference results |
| Final build/artifact and native gates | Current pairing gates **PASSED all five**; 203 integration and final 52-row native matrix (42 required + 4 supplemental + 6 preset runs) **PENDING** |
| Human visual acceptance | **PENDING** — eight actual human reviews of applicable final captures |
| Final documentation/catalog/ref closure | **PENDING** — bind final results without rewriting historical evidence |
| Six fresh independent Astra reviews | **PENDING** — six NEW reviews in two waves of three after implementation/gates; implementation-owner reviews do not substitute |

The prepared performance protocol fixes three baseline calibration runs, then B,C,C,B,B,C at 840 seconds each (120 warmup, 600 measurement, 120 drain). The separately scoped corrected-reference successor requires all six named functional gates and current-boot GPU/owner evidence before binding. A failed selected slot is terminal and cannot be replaced to improve the result. The protocol requires a quiet host during selected runs.

Historical candidate performance preparation distinguished the exact reproduced artifact and retained original stability assertion from a later reconstructed committed-product-input audit; its missing original before/after inventories remain unrecovered. The new pairing candidate and corrected reference instead have actual qualified source/compiler before-and-after evidence, bound to their current products in the gate receipts above. V15 is independently verified as an unbound preparation, not accepted performance. Prepared JVM/workload fingerprints explicitly include unique per-client native extraction paths in both arms; the retained world snapshot is unchanged, while the reviewed fixture repairs change fixture source and JAR identities. New recipes and both-arm workload fingerprints must bind those revised fixtures before any experiment is initialized; historical fixture evidence cannot supply the new acceptance. See the private `performance-protocol-recovery-20260910/` preparation and its unbound storage-tool provenance refresh.

Evidence acceptance always binds artifacts, dependencies, fixtures, scenario/checker, world/config, backend and complete owned cleanup. Historical failures, unsupported 1.21.1 Folia/client-gametest surfaces and pending human decisions remain explicit. No release or support-tier expansion follows from this ledger.
