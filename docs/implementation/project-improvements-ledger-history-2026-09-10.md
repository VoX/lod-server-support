# Project improvements implementation ledger

Implementation started 2026-09-09 on isolated `feat/project-improvements-mc*` branches. The reviewed plan is `docs/planning/2026-09-09-project-improvements-implementation-plan.md`; the user's implementation request supersedes its planning-only/hold language. Merge, release publication and deployment are not requested.

The full source SHAs and clean-worktree/merge-base inventory are in [baseline.json](baseline.json). Original candidate branches and normal game installations/worlds are preserved. Compatibility snapshot commits contain line-local catalog data only; later source changes do not rewrite baseline evidence.

| Package | Implementation state | Acceptance still required |
| --- | --- | --- |
| P0 | Five source inventories, Xaero ownership/baseline and test identities recorded | Keep final evidence bound to candidate inputs |
| P2 | Catalog, exact metadata importer/resolver, actual loader range bridge and generated matrix on all five | Final source snapshot and runtime validation records |
| P3 | Explicit classification, conservative drift check, isolated plan/prepare/resume; completed historical range dry-run | Final candidate blob/ref binding and independent residual-code review |
| P7a | Settings inventory/descriptors and correlated effective snapshots on all five; UI12 and six native server preset/restart runs passed | Final independent review; measured numeric settings remain separate |
| P4 | Owned private WSL runner/input/account context; native server15, C2ME two pins, UI12, WI5 four and WI6 two passed | Map four; seated and Elytra user visual responses; current concurrent acceptance |
| P6a | 67 pure suites moved; common Java21, impact controls and final case accounting passed on all five | Independent review; no performance speedup claimed from the single timing pair |
| P1 | Immutable status, screen/commands and bounded exports; UI12 and both supplemental no-Sodium standalone/client-preset controls passed | Measured frame/tick impact and final independent review; automated export/privacy controls already passed |
| P5 | Thin facade and five ownership components; all incremental/final automated gates and current WI5 four/WI6 two live profiles passed | Map four and measured frame/tick gates; seated/Elytra raw checks passed, user visual responses pending |
| P6b | Current required Folia sparse source correctness passed: 28/28 targets, exact target identity and complete cleanup; previous failure and separate diagnostic retained | Current Fabric/Paper correctness, sustained workload, baseline functional linkage, baseline calibration and exactly three measured pairs |
| P7b | Qualitative client presets passed on both primary loaders; six native Fabric/NeoForge/Paper preset/restart runs passed (server-presets-final-batch.json) | Measured conservative numeric preset and final independent review |
| Final build/artifact gates | All five final artifact identities recorded in final-build-gates.json: 14,724 unit cases, 436 server gametests and four available client gametest runs passed | Required live rows; any later measured-preset source change requires affected gates |
| Independent Astra change review | Not started | Review final changes, resolve findings and rerun affected gates |

Implementation does not mean acceptance. Raw baseline XML and exact testcase identities remain separate from subsequent outputs. Final runtime records must bind jars, dependencies, fixtures, scenario/checker, world/config and backend; historical smoke cannot close these gates.


## Recorded controls (not final acceptance)

- Compatibility tests:32 passed; line tooling:12 passed; impact selection:6 passed. All five catalogs validate. The four exact1.21.1 client dependency locks resolve with actual Fabric/Maven range semantics; they remain runtime-unverified.
- Current-package 1.21.1 attempt 3: common 708, Fabric 1695 (four explicit opt-in experiment skips), Paper 501, Neo 23; Fabric 78 and Neo 8 gametests passed. LSS/VSS release checks and fixture exclusion passed. These jars precede the two live UI corrections and any measured preset; they are not final whole-plan artifacts.
- Current-package 26.2 attempts 1/2: common 716, Fabric 1708 (four explicit opt-in experiment skips), Paper 511, Neo 23 passed; Fabric 79 server gametests, Neo 8 and explicitly invoked Tier 3 client gametests passed. Release/VSS and fixture checks passed. The first attempt's workflow contract failure is retained; the correction preserved the existing Java-version contract.
- Current-package 1.21.10 attempts 1/2: common 711, Fabric 1692 (four opt-in experiments and the unavailable modern Sodium golden skipped), Paper 501, Neo 19; Fabric 80, real client Tier 3, Neo 8, and 688-case migration accounting passed. Release/VSS and fixture checks passed. Attempt 1 stopped before client tests in native OpenAL initialization; its thread dump is retained. Attempt 2 used the null audio backend and passed without shipping source changes.
- Current-package 1.21.11 attempt 1: common 711, Fabric 1696 (four opt-in experiment skips), Paper 501, Neo 23; all JUnit tasks freshly executed with zero failures. Fabric 80, real client Tier 3, Neo 8, and 688-case migration accounting passed. Release/VSS and fixture exclusion passed; artifact inputs stayed unchanged. Evidence: `validation/1.21.11/attempt-1`.
- Current-package 26.1 attempt 2: common 711 (up-to-date from successful attempt 1), Fabric 1703 (four opt-in skips), Paper 501, Neo 23; platform JUnit tasks freshly executed. Fabric 79, real client Tier 3, Neo 8, 688-case migration, release/VSS and fixture exclusion passed with stable artifact inputs. Attempt 1's line-specific status-chat compile failure remains preserved; the native client-system feedback adapter fixed it. Evidence: `validation/26.1/attempt-2`.
- Final settings audit controls:1.21.1 common33/Fabric193/Paper21,26.2 common37; all platform compiles passed on both lines. See [settings/status audit](settings-status-acceptance-audit.md). First failure and corrected recheck are retained.
- Matched-cache migration timing: 689/689 moved cases passed in both arms, original 129.065 seconds and moved 127.233 seconds. This is one pair, not a speedup claim. An external non-golden resource change invalidated the common test task; unchanged input was UP-TO-DATE. The temporary marker was removed.
- Private XTest positive control produced actual key/button events and a private-display capture; ownership negative controls pass. This is input capability proof, not Minecraft UI acceptance.
- Folia early feasibility used two actual client registrations and full owning tick spans:1542 qualified samples with overlapping distinct regions. A one-region control with1024 samples failed the distinct-region requirement. This is not the final packaged four-client/performance gate. A later four-client diagnostic established four simultaneous v20 registrations, B reconnect, 11,228 qualified owning ticks with overlap, frame samples for every client, and 401 admission-denial probe reads. It correctly failed offered-target convergence (9/240 committed); source availability and dirty notification in the fixture are being corrected. No performance result is claimed.

- The first real private Prism UI run (`20260909T185347Z-faab6743f117`) negotiated v20 over `[::1]:25572`, received 3159 columns, exported status, and exercised the real Sodium Apply path. It failed acceptance because foreground status text was blurred and the Sodium status button overlapped its header. Both defects are corrected in source on all five lines; fresh live screenshots remain required. The failed-save control preserved configured bytes while changing effective reception, but unreadable feedback prevented a pass. The run was collected and all owned processes stopped.
- Fixture lifecycle cleanup now waits for Java shutdown hooks before terminating the private display; 91 rig controls passed before the additional Paper/bootstrap preparation controls. An actual destroyed-window XTest control verified held-key cleanup without allowing new input to an unowned window.

Private durable evidence roots: `~/.local/state/lss-project-improvements/{baseline,validation,settings-audit-control,migration-matched-timing}` and run-owned `~/.local/state/lss-rig/` directories. Launcher account files and raw launcher logs are excluded from exported results.

## Required live applicability

Required named profiles and scenario subrows from plan §15 remain required: modern/legacy Fabric and Neo 1.21.1 UI and lifecycle; Paper 1.21.1 handshake/store; 1.21.10/1.21.11 Elytra; both 1.21.11 C2ME pins; Folia 26.2 owning-region concurrency; sibling UI/platform rows. 1.21.1 Folia and client gametests are unsupported, not blocked. Native Neo Voxy 0.2.9 is a separate rejected optional trial, not a reason to omit Xaero-only required tests. The prior WSL feasibility smoke is historical evidence only.

## Dependency preparation updates

The dedicated WSL Prism cache contains the selected exact client assets and Linux native dependencies for all five lines. A stale Windows 1.21.11 asset index failed its metadata hash; the exact pinned Mojang index was acquired instead. Windows caches were read only. Paper engine locks now exist for each required server profile; sibling Paperclip builds 1.21.10/130, 1.21.11/132 and 26.1.2/74 were explicitly selected and verified against upstream SHA256s. Their complete disposable bootstrap caches were reconstructed offline from the exact cached Mojang jars and verified against Paperclip's embedded output hashes. No world or existing server configuration was copied.

Private Prism preparation supports a separately locked Fabric or Paper server candidate and rejects embedded bootstrap Minecraft/loader/Java mismatches. Each run binds the complete immutable server libraries, versions and cache file sets, as well as client dependencies. These are preparation controls, not live server acceptance.

## Current UI candidate acceptance

Run `20260909T200159Z-e5eae2ffeb00` passed modern Fabric 1.21.1 `ui-apply` and collection, with complete owned cleanup. Candidate Fabric SHA256: `63d8954f76601d8daf59d9b4a7f6c9a8f316a158c4b8c1ef1f1b1a1b1388bd85`. Real private XTest input opened `/lss status`, followed the Sodium status entry, applied the real Sodium reception checkbox, and exported the observed effective OFF state while still negotiated over IPv6. The status text and failed-save feedback are readable, and the status button occupies free space away from Sodium's header controls. A real temporary-directory save blocker left configured bytes unchanged while effective reception changed; the blocker was removed and reception restored. No WI5/map/far-render or performance acceptance is implied. This candidate predates the measured numeric preset.

The same run passed actual Fabric server preset apply/undo and filesystem save-failure controls, with independently verified command/export receipts. Generation enabled/configured-for-restart remained true after restoration, distance returned to 32, canonical server config matched its baseline, and no blocker or staged restart remained. Durable receipts are under the run's `evidence/settings-*/receipt.json`. The compatibility validation record includes their hashes alongside the UI evidence.

The next Folia loaded-target diagnostic (`20260909T194705Z-526f317da13d`) established 19,127 qualified owning tick samples and five actual sessions, but failed 238/240 independent targets. All four consumer shutdown writers closed with no overflow or held receipts. The two missing pre-reconnect targets remain a fixture publication-order investigation, not a product regression or accepted run. Subsequent acknowledged-target/body-identity fixture changes require a new live attempt.


The legacy Fabric 1.21.1 UI run `20260909T202508Z-4bfa59b04e6d` also passed and collected with complete cleanup against the same current Fabric candidate. It exercised the real legacy Sodium Apply path and actual filesystem save-failure feedback, restored the canonical config and removed the blocker. A separate UX issue was observed: changing reception in the child status screen leaves the existing Sodium parent's checkbox stale until reopening it. That issue remains under correction; this run is not final acceptance of that return path.

The acknowledged-target Folia diagnostic `20260909T201608Z-2bdc254d909b` failed 237/239 target convergence while establishing five sessions, 19,146 qualified owning ticks, five actual deferred/released receipts, and complete cleanup. The client fixture's active receipt rejection/retry handling was subsequently corrected; no passing mixed-source result is implied. The first bounded native Paper source seed `20260909T202319Z-1459be625f07` failed because its external plugin called a package-private LSS serializer across distinct plugin classloaders. It collected with complete cleanup and correctly refused snapshot creation. The remedy is confined to the external test fixture; no normal world or server was used.


The Sodium child-return correction now preserves pre-existing staged edits and refreshes the existing parent's reception binding. Focused controls passed (common 5, Fabric 5), 1.21.1 Fabric/Neo jars were refreshed, and 26.2 Fabric/Neo compiles passed. Run `20260909T204917Z-6dcbe118aaa8` passed all six legacy Fabric UI assertions against Fabric SHA256 `3d77c69eddd8ea0508b22f0df1b836206c4be064ba76102045a7ef1d0520a23e`, including the parent return path and preservation through real Apply. It collected with complete cleanup and restored the original canonical config. Neo candidate SHA256 is `3f16e1b2fc034a1cd37f0a7508a87a6cd5b784370ec5b468997fe2127843ac96`; its UI acceptance is still pending. Modern Fabric's prior pass predates this correction and needs the final relevant artifact check.

The sustained producer and indexed client oracle compile. A separate measured mode seeds 1,024 additional retained cells, offers exactly 23,040 sequenced edits over 720 seconds, and rejects a missing or delayed offer schedule. The raw offered-load checker, seed-domain checker, bounded Java schedule controls, registration-qualified interval accounting, and retrospective report assembly are implemented. Client body timing prevents an old held body from satisfying a later edit. GC and offer-to-commit latency are descriptive supplements; the frozen regression metrics remain unchanged. None of this is an actual calibration or measured-pair result.

Catalog generation now includes dated feature-specific attempts from fixed source snapshots, preserving failed latest attempts for their exact input identities. The new local `export_validation.py` derives collected-run records, checks staged candidate bytes and declared evidence hashes, and excludes raw launcher logs. The final five-line source snapshot is still pending; generated views therefore do not yet claim the new runtime records as part of that snapshot.


## Source snapshot and current runtime controls

Corrected measured-source seed `20260909T213657Z-4da17260c825` passed and collected with complete cleanup (run hash `21defa1769c3ab8acb200949b05751e0ff21048e54c49ca5321ea22fd13a3bbf`). Four initial native generation-absence observations, all 16 source facts and all 1,024 measured cells passed; the native server exited zero and the writer closed. The 77-file frozen snapshot is `measured-sources-20260909`, digest `28c9351ee28de364a82f4d737ce3a2583d398d4ea0a2aebaa7e718005df1787e`. This establishes source preparation, not multi-client delivery or performance acceptance. Earlier failed seeds remain retained.

The maintained rig Python suite passed 180 controls after the source geometry/native-path corrections, actual-game GPU observer, deterministic Xaero recipe merge, and multiple-listening-endpoint checks. Catalog controls passed 38 and line-tool controls passed 12. The newly maintained WI5 replacement fixture holds one real prepared callback, observes native pending-work retirement, and releases after same-dimension replacement negotiation; its five checker controls pass, but its new Java hooks and live behavior are still pending verification. The optional WI6 transport-close helper closes only the sole observer's actual channel in an explicitly armed owned offline server; it is a test fixture, not a shipping path.

Neo legacy run `20260909T211021Z-c1674f46321d` passed six scoped UI assertions with complete cleanup. Neo modern run `20260909T211758Z-599de5eccbe2` likewise passed the six scoped assertions, while retaining an additional Escape-return defect as an explicit limitation. Sodium closes and discards drafts on Escape release, so the status screen's earlier press-time return propagated the release to its parent. The corrected screen consumes the press and returns on the matching release. Fresh seven-assertion live verification remains required.

C2ME alpha18 original-read diagnostic `20260909T213018Z-fa997fa73586` showed both native read futures completing with an empty Optional before LSS parsing. The saved-column test setup now waits for a native FULL record before submitting its unchanged one-shot foreground/background LSS reads. That correction is not yet a passing alpha18 result; all failed attempts and the broader unresolved C2ME suite failures remain distinct.


## Subsequent verified results and retained failures

The corrected C2ME saved-FULL setup passed the three required save/read cases on both exact 1.21.11 pins: alpha18 run `20260909T220719Z-bad18ba7f84d` and alpha26 run `20260909T220813Z-83be865cabae`. Both collected with complete cleanup. This closes the selected three-case route only; earlier broader C2ME suite failures remain unresolved and retained.

Neo modern UI run `20260909T214859Z-45566b524e77` passed all seven actual assertions, including Escape return without discarding the parent's staged edits, real Apply, failed-save feedback and exact config restoration. It binds Neo candidate `2cc7d658dbc28f9d2fc5882ee720564f07338977436e6f9b17e145cb1469b7bd`, with complete collection and cleanup. Other primary profiles still need the relevant final Escape-artifact checks.

Fabric modern receive-lifecycle run `20260909T221258Z-7e64bf5cde91` passed all five assertions and collected with complete cleanup. Run hash: `20e8271a001c53c35a7fafc08fac2f1f960eefa38d2b4a2d77073dbd4d024359`; candidate: `070aa33154c4dda855879ccb852e24170899bf6382e9c5a127e00fa6b383aa75`. OFF retired acquisition while retaining committed native work; ON negotiated a fresh manager. The test held a real prepared callback, closed the sole observer's actual server channel without a disconnect packet, observed any-thread retirement with positive pending native work, joined a fresh same-dimension replacement server, and released the old receipt without enqueuing its tile in the replacement generation. Raw semantic proof was rechecked at collection. This is lifecycle evidence, not map, far-render or human visual acceptance.

Full mixed-source Folia run `20260909T215358Z-9b1909f0b432` failed strict convergence (22/28 targets) and collected with complete cleanup. It separately passed owning-region overlap with 44,011 qualified full ticks and five sessions, and bounded debt drain in 1.7486 seconds. Loaded-source ownership, bounded generation preparation, stale reconnect acknowledgement and overwritten diagnostic targets require fixture corrections and another run. No performance acceptance is inferred.

The exact 26.2 baseline source archive was verified against all 987 Git blobs at `98b67abc82283417ad288cbb0cca2f4fb3343425`. Artifact build attempt 2 passed: Fabric `7e74f7e584fe18afc4c48c6039852f395ce221b5d0a7d02243b8ddcba75798b3`, Paper `cd162864c725dca9995ab570c9a6f28b93a412dfb54afc914c71126e4e915049`. The first attempt's unavailable remap task failure remains recorded. These are baseline artifacts, not calibration or measured-pair results.

Measured-workload analysis found that alternating cell content can accidentally match an older undelivered version. Acceptance continues to require actual delivery of every offered edit; supersession will not be counted as delivery or used to erase debt. Revision intervals, wire/body provenance and fault-deadline controls are being tightened before any calibration.

All four primary 1.21.1 receive-lifecycle profiles now passed against the unchanged final-relevant candidates and collected with complete cleanup. Additional runs: legacy Fabric `20260909T222941Z-89e30c2d9953` (hash `f315881b134bba7ab3cd275eebd0b163d31938434cc6bcdd8f806845e3bcdb23`), modern Neo `20260909T223105Z-ebec0c54c91c` (hash `cb4bb3135b450526a3426c3bc64eeb3229f1a043198b2d6d5b34638b2c341c9c`), and legacy Xaero-only Neo `20260909T223303Z-563cf3f9420c` (hash `10a2c86e071bbd54f1e8e599f6d8cd5b67921f0a428dfc58169d58340914c9a2`). Each passed all five assertions and raw collection revalidation. Neo native abrupt-disconnect retirement occurred on the render thread; the Fabric observations exercised the Netty thread. Map, render and final UI rows remain separate.

Native no-LSS Paper prefill run `20260909T223913Z-fd8f62b11e83` (hash `8e1fdf10056caad41f50c59f7b6c6d8c860d2033902774913d2d0da02fbe5c85`) passed construction-only acceptance: all 19,044 promised chunks completed natively as FULL and were verified in saved MCA records; native server/writer exited cleanly and cleanup completed. A first collection invoked the wrong support tree wrapper and rejected its pre-existing tool difference; that rejection is retained separately. Collection through the original unchanged 26.2 toolchain passed. This is not source-seed or delivery acceptance: four explicit holes, native startup absence/store/proto checks and the corrected source seed remain required.


## Intermediate refactoring and corrected source preparation

All four incremental Xaero extraction stages passed the same 176 executed, unskipped baseline test identities on each of the five lines (20 intermediate gates). Retained result XML, before/after source hashes and explicit accepted attempt selections are under `p5-reconstruction`; the first 1.21.1 stage-three compilation failure remains retained. Its scratch reconstruction was corrected to preserve overload identity and the Origin import, then the exact corrected stage passed. These intermediate gates do not replace final integration or runtime acceptance.

Corrected source seed `20260909T225249Z-257b3fa01020` (run hash `d5ac671e1bd7852f3123f80d9bb7c72456ea61987c32f0bbd980e143cf0d7c52`) passed and collected with complete cleanup. Native startup verified the four generation holes absent, all 16 source facts and 1,024 measured cells were checked, and saved records retained 19,040 surrounding FULL chunks plus the four holes. The frozen 117-file snapshot `prefilled-measured-sources-20260909` has digest `ab76d3197957628fe4a8eb29b5af17b1d8e11171e0ac4c0a44fabc64691e9a55`. This is source preparation, not mixed delivery or performance acceptance.

The 1.21.10 UI attempt exposed transparent foreground text under the newer native ARGB rendering API. Opaque colors were applied on the four affected newer lines, with focused tests and builds passing; new live UI checks remain required. The separate client map-only preset receipt passed, but does not turn its enclosing failed UI attempt into a pass.


The four tested P5 extraction commits are now applied to each isolated implementation branch. Private construction checked every committed source/build byte against the actual intermediate test snapshots; expected-old-value branch updates and index-only baseline changes preserved every pending working file. Final extraction tips: 1.21.1 `42a0daea5d0cd249a41c7e61fc6ceb6a4a195ebd`, 1.21.10 `0970e12fcddd435cf7f4ab3ad621938bf22291ca`, 1.21.11 `6e32dc4c947cde4275125e737adb00aa231db108`, 26.1 `c1e8333379dada2d4a10bafab0f14f5bbe16ceed`, 26.2 `af279c99130f77a36503d2eb67089de64b7072b8`. These tips contain the extraction stages, not the remaining uncommitted final integration.

Review-flow hardening passed 279 rig controls and subsequent focused checks. Failed runtime evidence cannot be promoted by visual review; rejected reviews are terminal; finalized proof/image identities are frozen; absent ownership records cannot establish cleanup. Screenshot acceptance may remain awaiting-review after all owned processes stop. An actual user response is still required for the designated visual gates.

Seated-render setup attempts `20260909T233650Z-844ce26fabae`, `20260909T234104Z-ad5c413d7f38` and `20260909T234339Z-0a91088ebb69` failed and collected with complete cleanup. The first exposed native options migration rejecting unnecessary default-key strings without a version; subsequent attempts established all three actual clients and v20 consumers, then exposed incorrect expected English boat-command receipts in the controller. Those setup corrections do not themselves establish far-render acceptance. A pre-launch create failure involving the target-assets closure whitelist was also retained and fixed with changed-asset rejection coverage.


Further seated diagnostic runs `20260909T235530Z-dfb29fc9962c` and `20260910T000121Z-4a6bbfdde004` both produced real same-frame fault/recovery observations, with the next proxy, two completed name tags, restored sentinel/outer matrices and no crash latch. Independent raw recomputation passed those five semantics. Both enclosing attempts nevertheless failed screenshot preparation and collected with complete cleanup; neither is visual or full scenario acceptance. The first exposed an input guard that mistook an inherited private DISPLAY for the host desktop; its correction records the original host display and verifies actual owned Xvfb executable, isolation/auth arguments and creation identity. Five negative/positive controls and a real Xvfb check from both caller contexts passed. The second exposed Prism's EntryPoint launch shape lacking a command-line --gameDir; native observer discovery is being adapted without relaxing ownership/window checks.

The extended WI6 fixture compiles for both primary loaders. Twenty-five focused controls reject inconsistent participant identities, missing baseline frames, wrong successor epoch, invented retry counts, uncleared native debt and altered raw reports. Its unaffected-progress assertion concerns an independently accepted native far-player clear during observer refusal, not LOD throughput or physical Netty saturation. Live admission acceptance remains pending.


Fabric seated-draw run `20260910T000743Z-e398096b6a87` (run hash `e8f0e8808338c1499a22fb1f2bc2312229c14003290edac028278e5193480d9a`) passed all five raw automated assertions, captured the actual owned observer window, and collected with complete cleanup. Its status is **awaiting-review**, not accepted: an explicit user review of `evidence/seated-observer.png` was requested and remains pending. The native observation includes two absent-from-vanilla proxies, one real passenger dispatcher push/translate exception, a completed next-proxy draw, two completed tags, restored matrices/outer stack and no crash latch. This is the first complete runtime/capture attempt after the retained fixture setup failures.


## Current tooling verification and strict runtime correction

The current primary rig suite executed **362 tests, all passed** (`python3 -m unittest discover -s tools/rig`). On each of the five lines, compatibility controls executed 39 cases, line controls 12, and verification-selection controls 6, all passed; generated settings references also passed `--check`. These are tooling controls, not substitutes for native scenario results.

The native server-smoke catalog import initially violated branch ownership by installing sibling profiles. Actual `catalog.py validate` rejected all five trees. Only the newly imported foreign-line profiles were removed, import ownership was corrected, and actual validation plus generated compatibility `render --check` subsequently passed all five. No line-local authority requirement was relaxed. All 15 native server-smoke dependency recipes are prepared; live platform acceptance is still pending.

Strict Folia attempt `20260910T001430Z-ea41ac0456f5` failed and collected with complete cleanup. Source readiness passed, but all four client fixtures failed a redirect injection before joining: a bare method name selected the first `drainColumnQueue` overload instead of the intended eight-argument resolver-backed method. Both fixture hooks now select the exact native descriptor with required injection counts retained. Native startup verification precedes another full attempt. This failed run establishes no concurrency or delivery acceptance.


Neo seated-draw run `20260910T002739Z-41d990a1a3e8` (hash `d42668800381a578344c17e1c0f53c2566cdf068170bb0138c3000b49ac5ba79`) also passed all five native automated assertions, captured its owned observer view and collected with complete cleanup. It remains **awaiting-review**; the separate user visual assessment is pending. No visual acceptance is inferred from machine assertions.

The map observer now waits for two completed native frames with the actual Xaero fullscreen screen before capture, replacing the fixed one-second delay. The final closed map stream rejects an incomplete trailing record instead of ignoring possible failure bytes; open-stream prefix handling remains provisional. Both primary native fixture variants compiled successfully (`xaero-map-fixture-builds/1788999992349279705`); eleven focused map controls passed. These changes have not yet established a native map run. Elytra targets also omit unnecessary unversioned default key strings, preventing the same native options data-fixer failure previously observed in the seated setup.


## Primary native send-admission acceptance

Fabric run `20260910T003315Z-a362d84ef5e3` (hash `719c173a80c01f19ace81bc4b87103e4689472af9fa9bf36d4d98dc0ac143a5e`) and native NeoForge-server run `20260910T003507Z-b67f541de3a6` (hash `46f5ba3e35cf37c97881278e5aac1658d2a509d9cfa03498e8c6d3157b14536b`) passed all four strict send-admission assertions and collected with complete cleanup. Each observed repeated actual adapter refusal of one clear, an unaffected viewer's accepted native clear, successor-epoch roster/update recovery, and owner-thread retirement of the three native pending control flags after 100 observation ticks. These establish adapter-denial behavior, not physical Netty saturation or LOD-throughput fairness. The Neo run used an installed native NeoForge server and its native fixture, not merely a Neo client observing Fabric.

The first Fabric attempt `20260910T003009Z-7fa5132a2545` failed and remains retained: its controller armed against the old roster before the preceding OFF/ON baseline had settled. The checker rejected the inconsistent epoch despite the fixture's terminal success marker. The controller now waits for an initial real baseline, then a newer accepted roster and matching update after the setup toggle before arming. Two focused controls reject old or unmatched baselines. The corrected runs passed the unchanged strict epoch checker.


## Full Folia source correctness and maintained native checks

Run `20260910T004823Z-9cf2c41a5bdb` (hash `5bd6af48733757ca608156e73491263d1d776dcdad2edb8bfe897a3ba4dc2bb3`) passed the complete 550-second strict Folia source scenario and collected with complete cleanup. All 28 offered targets reached their exact current revision/content/source, including the post-reconnect edits. Genuine region proof included 44,032 qualified whole owning ticks across five sessions. The slow client deferred and released a real body; the admission lane observed 399 actual denial reads. Debt drained in 1.249848094 seconds. Sparse correctness yielded 1,594 independently useful body bytes per client; this is not a sustained throughput or performance result. Comparable Paper/Fabric correctness, the sustained workload rehearsal, calibration and fixed measured pairs remain required.

The successful source snapshot contains 121 files, digest `2a2ea66840866d55eab784161f12271302dd58489380b3f0d5a7cd545f2908bb`. It preserves all 117 accepted terrain/store bytes and adds the four actual native saved player records. Native login positions matched those records before requests began, eliminating the prior shared-spawn source contamination. The previous failed runs and snapshots remain retained.

Maintained UI v2 adds raw export/config/PNG checks, exact draft/save-failure/restoration transitions, run-bound operator inspection and an exclusive finalizer for both negotiated and no-consumer routes. The new Elytra v2 adds native session/pose journaling, real private-input phases and strict raw evidence binding. The two updated Elytra and five server-smoke fixtures built successfully; all five smoke jars' actual classfile annotations retain exact decoder selectors and `require=expect=allow=1`. The complete primary rig suite subsequently passed 412 cases. Live Elytra and native server-smoke acceptance remain pending.

UI profile mc12110-fabric-legacy run `20260910T003852Z-8f3bdc30ae12` passed its seven actual UI checks and cleanup; Voxy terrain rendering stayed explicitly disabled for this UI-only scope. Native Neo UI run `20260910T004414Z-da37339efb36` passed seven raw UI observations but its overall attempt failed because the launcher acquired three dependency files during startup. Cleanup completed, the failed attempt remains unchanged, and the retry must bind the newly acquired closure explicitly. Neither result establishes terrain rendering.

The catalog's client-gametest derivation was corrected: Loom registers the task from `enableClientGameTests`, so looking for the generated task name in the build script incorrectly marked 1.21.10, 1.21.11 and 26.1 unavailable. Four controls cover literal settings, the actual mainline boolean alias, comments and unsupported/reassigned declarations; actual checks confirm false only on 1.21.1 and true on all four newer lines. All five catalogs and generated views pass against a fresh capability snapshot. This is an intermediate snapshot; the final complete-source/evidence refs are still pending.

The final verification selector now explicitly includes client Tier 3 on all four newer lines and excludes it on 1.21.1. Linux execution requires private Xvfb through a Loom init script, removes inherited desktop display/auth variables, and enters the existing coarse lock plus owned-process Gradle supervisor. Nine focused controls passed on each line (45 total). An actual invocation while the UI rig owned the lock refused before Gradle or staging. This verifies isolation/selection, not completion of the queued final Java gates. The catalog workflow also runs for source-only changes so Java edits cannot bypass cross-line drift checking.

After removing the Elytra checker's incidental Pillow dependency, the complete primary rig suite passed 414 tests with Pillow imports actively blocked and zero skips. Evidence: `/tmp/lss-elytra-png-portability/actual-primary-without-pillow.log`. The first staged run had six explicit occupied-harness skips; the isolated actual-repository rerun executed all six controls.

The maintained verification entry point now also selects null audio for Linux client gametests, resolves relative checkout roots before launching child commands, and conservatively reports cross-line validation for unknown/shared/tooling impact. Eleven controls passed per line (55 total). The contributor commands use the owned verification wrapper; no newer-line client gametest is launched on an inherited desktop.

The 26.2 UI run `20260910T014339Z-5177284c7db5` found a real parent-binding refresh defect despite correct stored reception and preserved unrelated drafts. This line's `ClientStatusScreen.onClose` omitted `onReturn.run()` before its `setScreenAndShow` call; the other four production lines already invoked the callback. The failed run restored configuration and collected with complete cleanup. A compiled-bytecode callback-order control failed before the one-line correction, then all four screen controls passed and both 26.2 client jars rebuilt. The same regression control was added across the five lines; sibling execution remains in the final gates. The new 26.2 live UI retry is still required.


## Current UI closure and final source checkpoint

The corrected 26.2 candidate passed all seven native UI checks on Fabric (`20260910T015250Z-d9f7c5ac19cc`) and NeoForge (`20260910T015547Z-c62337e3d4fc`), including visibly refreshed parent values and preserved unrelated drafts. Both runs restored configuration and collected with complete cleanup. This closes the live retry noted above for the missing return callback; it does not establish terrain-rendering acceptance. The Fabric UI-only profile explicitly disables Voxy terrain rendering after the separately retained 1.21.11/26.1 native driver stalls.

Current version-two UI passes also exist for both 1.21.11 loaders, both 26.1 loaders, 1.21.10 NeoForge, and primary modern/legacy Fabric plus legacy NeoForge. The final 1.21.10 Fabric and primary modern NeoForge repeats remain in progress. Primary modern Fabric additionally passed independently checked server apply/undo and denied-save receipts on its actual running server.

All shipping and build inputs are committed on the five isolated improvement branches, including the callback-order regression control on every line. Full final Java gates and the remaining tooling/fixture commits are still pending. Historical native passes whose checker dependency hashes differ from the final target require fresh runs; current hashes are not stamped onto old acceptance.


## Twelve-profile UI batch and all-line tooling closure

All 12 distinct current-version UI profiles passed their seven native assertions, restored configuration and collected with complete cleanup. Exact run/profile/result identities and five preserved failed attempts are indexed in `client-ui-final-batch.json`. Current 1.21.10 Fabric additionally passed the separate six-assertion client map-only receipt; primary modern Fabric passed the separate server apply/undo and denied-save receipts. These are UI/preset gates, not terrain, transport, concurrency or performance acceptance.

The complete corrected tooling batch executed 503 tests on each line: 44 compatibility, 12 support-line, 436 rig and 11 verification controls. All 2,515 tests passed with zero skips and Pillow imports actively blocked. The first batch stopped on a 1.21.10 recipe unit test that wrongly expected primary-only catalog profiles in a sibling checkout; its failure is retained. The test now uses self-contained recipe inputs, including wrong-route and wrong-artifact rejection, while real profile validation remains a native gate. A separate absolute primary-checkout import was also corrected. Raw batches: `~/.local/state/lss-project-improvements/final-tooling/20260910T023200Z` (first failure) and `20260910T023500Z` (corrected pass).

The sustained functional checker now invokes the existing raw workload normalization and correctness checks over every 30-second progress window, including warmup. Its starvation control rejects delivery at 31 seconds even though it precedes the 32-second revisit; completed subjects leave eligibility. This is checker validation, not a sustained runtime result.

The tested tooling/fixture source closure is committed on all five improvement branches. Fresh all-line Java/build/artifact gates started under exclusive ownership; their results remain pending.


The first fresh Java attempt (`final-validation/1.21.1/attempt-20260910T023208Z-400442`) executed all 720 common tests successfully, then failed Fabric gametest compilation. The native save-read readiness control passed this line's `TestPositions.ChunkAt` wrapper directly to `ChunkMap.read`; it now passes `.pos()`. The four sibling call sites already use native `ChunkPos`. Primary test-only correction `3149a7a3` is committed; the failed attempt is retained and a fresh full batch was started. Unreached platform test reports in that first attempt are explicitly marked not-reached and are not new passes.


The next primary attempt (`20260910T023607Z-403041`) passed all 78 required Fabric server gametests, then the full Fabric unit suite detected the optional Sodium reference in shared `xplat` source through the existing `XplatLoaderPurityTest`. The rule was preserved: `SodiumStatusReturn` moved byte-for-byte to same-FQN Fabric/NeoForge source twins on all five lines. No test exemption or string obfuscation was added. The failed attempt remains retained and a fresh batch started at `20260910T024058Z-406155`. Packaged byte identity must be checked after this source-layout correction before reusing native UI evidence.


Primary full gate `20260910T024058Z-406155` passed at source `791145fd`: common 720/0/0, Fabric 1,700/0/4 (the four explicitly opt-in tools), Paper 501/0/0 and NeoForge 23/0/0; all 78 Fabric and eight NeoForge server gametests passed, with release/artifact-exclusion checks green. The batch next stopped before launching the 1.21.10 client because the Groovy init closure checked its owner instead of the actual task for `useXvfb`. Explicit task binding fixed the maintained wrapper and external gate script; an actual Loom task dry run then passed, and all 55 verification controls passed.

The primary Fabric jar differed from the earlier UI artifact only in nested SQLite/Zstd ZIP timestamps. Archive comparison verified all 319 outer paths/order and all 176 SQLite/56 Zstd entry payloads were unchanged. Exact hashes nevertheless differed, so no old record was promoted. The stripped-dependency writer now assigns ZIP-local 1980 timestamps on all five lines, including its generated descriptor; the VSS branding and publishing flows are unchanged. Two forced builds across time zones are queued to prove reproducibility and payload preservation. This packaging correction requires fresh packaged candidates and matching Fabric native runs; the earlier full gate remains evidence for its declared source, not the later packaging inputs.


The first cross-time-zone reproducibility check correctly failed: `ZipEntry.setTimeLocal` treats exactly 1980-01-01 midnight as its minimum-DOS-date sentinel and adds a timezone-dependent UT extra field. The first two builds and comparison are retained under `slim-reproducibility/1789008604875109496`. Native JDK source confirmed the branch. The fixed timestamp is now 1980-01-01T00:00:02, strictly past that sentinel.

All five lines subsequently passed two forced stripped-dependency builds each, UTC versus America/New_York: exact nested archive hashes matched; payloads, entry order and compression methods matched the preserved originals; every timestamp was fixed and no UT extras remained. Source bytes and HEADs stayed unchanged. Evidence: `slim-reproducibility/1789008731636436022/results.json`. The full final Java/build/artifact batch restarted at `20260910T025352Z-417570`; fresh Fabric runtime acceptance against its resulting hashes remains pending.


The reproducible-packaging batch passed the complete current gates on 1.21.1, 1.21.10, 1.21.11 and 26.1, including real private-Xvfb client tests on the three applicable newer lines and release/artifact checks. The 26.2 gate then exposed the same `ChunkAt` native-read adaptation missed in the earlier primary fix. Correction to the earlier note: only 1.21.10, 1.21.11 and 26.1 use native `ChunkPos` at this call site; 26.2 also requires `.pos()`. Its test-only fix is committed as `c966b1d2`; only 26.2 is rerunning at `20260910T031422Z-431391`. The four passing gates remain valid for their unchanged inputs.


## Current all-five build gates passed

All five current candidates passed their complete fresh Java/build/artifact gate. `final-build-gates.json` binds each selected manifest SHA, exact source commit, all packaged artifacts and actual case/skip identities. Totals: 14,746 discovered unit cases, 14,724 executed and passed, 22 explicitly recorded opt-in/unavailable cases skipped; 436 required server gametests passed (Fabric plus eight NeoForge on each line); all four available real client-gametest runs passed under explicit private Xvfb. Primary 1.21.1 has no client-gametest task. Every release checker and shipping fixture-exclusion check passed. The four first-line manifests use attempt `20260910T025352Z-417570`; corrected 26.2 uses `20260910T031422Z-431391`. Earlier failures remain retained.

These are full build/test/artifact gates, not completion of the native required matrix or performance plan. Final C2ME capture and native recipe rebinding are next; map/far-player/concurrent acceptance, matching Fabric UI reruns, measured presets, human visual reviews and independent final Astra review remain separate work.


## Final-artifact UI rerun and bounded snapshot freshness

Five final-artifact Fabric UI runs passed: primary modern `20260910T032452Z-b36891c6a805`, primary legacy `20260910T032819Z-aad5fcf3893b`, 1.21.10 `20260910T033112Z-4f57ef943d53`, 1.21.11 `20260910T033452Z-c0e1cdc57007`, and 26.1 `20260910T033731Z-9a25681ad297`. Primary modern also passed the separate native server apply/undo and save-failure receipts; 1.21.10 passed the separate six-assertion client map-only receipt. All restored configuration and completed cleanup. Terrain-disabled UI limitations remain explicit.

The final 26.2 attempt `20260910T034033Z-add7e9be81f4` failed a save-failure export assertion and was retained with complete cleanup and exact configuration restoration. Its exported snapshot was captured six milliseconds after the pre-action configuration copy, before the native click was processed; the helper's fixed 500 ms delay raced the deliberately bounded 2 Hz status cache. A later actual export correctly showed OFF. This was a fixture timing defect, not evidence of a stuck product setting.

The maintained helper now records native action completion and requires a later snapshot, with a bounded five-second wait. Only pre-action stale snapshots may retry; a fresh wrong value fails immediately. Actual intermediate exports and timing receipts are retained. Five new controls plus sixteen existing UI controls passed on each line (105 total). Source commits: `ca3a901f` / `5c80e778` / `70ff99a9` / `1b749457` / `7a2ff063` in line order. No shipping artifact changed. This changes all twelve UI checker identities; previous runs remain historical evidence and a fresh twelve-profile batch is required. The independent native server smoke batch can proceed against unchanged final shipping artifacts.


The first final native server batch stopped during 1.21.1 Paper staging, before any server/client launched: the maintained native-client recipe producer declared `smoke-assets`, which the immutable-tree verifier correctly rejects. Staged directory `20260910T034940Z-2996b88bd169` had no run manifest or owned processes; its preparation failure is retained rather than represented as a collected runtime result. The producer now uses canonical `assets` in stage paths, tree identity and native arguments on all five lines. Two focused controls passed per line; no verifier allowlist was widened and no product artifact changed. Commits: `5a8e14b2` / `c1abe76b` / `9ef99679` / `11e71adc` / `207b9252`. Fresh recipes preserve all dependency bytes and asset-tree contents. Native retries remain separate attempts.


Nine final native server runs passed and collected with complete cleanup (all three platforms on 1.21.1, 1.21.10 and 1.21.11). The next 26.1 Fabric attempt `20260910T040210Z-e104f91e9e92` passed its three raw store/body assertions but failed strict collection because startup extracted 39 previously unbound libraries. Cleanup completed; the overall attempt remains failed. Independent checks matched every added path/hash to the exact 26.1.2 server jar's `META-INF/libraries.list` and embedded bytes; profile, bootstrap and server log agree on 26.1.2. The external bootstrap preparation omitted this first-start extraction. Fresh recipes must bind those vendor bytes before launch; neither the verifier nor the failed result is relaxed. The remaining native attempts are still pending.


## Final native server batch and C2ME validation

All fifteen native server-smoke v2 combinations (five lines times Fabric/Paper/NeoForge) passed and collected with complete cleanup. `native-server-final-batch.json` records their exact run/profile/server/client/fixture identities and the one retained failed native attempt. The earlier prelaunch asset-root rejection remains separately recorded. The accepted final 26.1/26.2 Fabric recipes prebind the exact vendor-bundled dependencies; no closure check was weakened. These are native handshake/store/body gates, not renderer or performance acceptance.

The maintained catalog exporter initially rejected native server records because it passed only the primary client profile to final record validation. It now supplies the already hash-checked retained participant profiles, rejecting same-ID conflicts, changed participants and missing files. Nine focused controls passed on each line (45 total). Commits: `e779b655` / `b08fe3db` / `0163326b` / `aa34f1a2` / `9b839f45`. All sixteen collected attempts were exported with the maintained fix and matched the separately validated staged records exactly. The scenario checker closures exclude this exporter, so its correction does not change those native acceptance identities.

Both final C2ME v2 profiles passed their exact three native save/read cases and collected with complete cleanup: regular-map `20260910T041447Z-03043474f0d6` (run hash `38029af51290a724c83af9c6b43e8d08191b95be87cf494ce46e87a5ff51811b`) and benchmark `20260910T041522Z-93e85522dbf8` (`06fdfa459d7f4c15d136a0fb6e361a12ebbe1a2e1eb55545d7fa95ed9b5fdc06`). Earlier broader C2ME-suite failures remain distinct from this selected three-case acceptance.

The first 1.21.10 Elytra attempt `20260910T041550Z-334e328f95cf` stopped before assertions because the shared Prism dependency fingerprint was stale. Fresh preparation resolved that explicit precondition. Its retry `20260910T041909Z-de08602122a9` then exposed an actual fixture packaging defect: `TargetConsumer` was inside the Mixin-reserved root package. Both failed runs collected with complete cleanup. The two actual Elytra variants now place only mixins in a dedicated `.mixin` package, keeping helpers outside it; both rebuilt jars pass compiled metadata/class checks, and the control rejects both original broken jars. Mirrored source commits: `3544f3d7` / `03978183` / `ab0d14f6` / `e14bdd48` / `806a5965`. No shipping artifact changed. Fresh native retries and their required human visual reviews remain pending.


## Native fixture packaging and Elytra controller follow-up

Before launching the four map profiles, actual compiled-jar inspection found the same reserved Mixin-package issue in both map fixtures. Five mixins now live in a dedicated `.mixin` package and the ordinary probe remains outside it. Both corrected Fabric and Neo jars pass the compiled metadata check; both old jars fail it. Nine loader-specific controls also passed. Fresh map recipes and independent targets bind Fabric fixture `c4b79e72eeddad23b3196118ad4dc329c87246cf2b760384080f4e9429089957` and Neo fixture `1eb2fdddb9fa576171d66dbb1b677a69571ed6f4873d66627eec015a4f610b60`. This is packaging/preparation evidence, not a native map pass.

The next three 1.21.10 Elytra attempts failed and collected with complete cleanup: `20260910T042407Z-19495780c097`, `20260910T042931Z-a2c48750796a`, and `20260910T043857Z-af6ff27c00fb`. The first exposed a pre-teleport sample in the falling-phase controller. A native post-teleport barrier corrected that ordering. The second reached actual flight but used malformed server selectors and landed outside the unchanged 512-block limit. The third passed all eighteen native command readbacks after selector correction and again reached real flight, but relative mouse input did not steer the landing into bounds. Its captured sky image does not establish visible proxy rendering. Successful dispatcher submission is not a visual pass. Native input and camera diagnostics remain in progress; no failed image is submitted as accepted visual evidence and no shipping artifact changed.


## Common runner dependency fingerprint correction

A source audit found that the scoped evidence fingerprint skipped every import in `rig.py`, omitting shared endpoint, immutable-tree and toolchain verification. It now traverses common imports transitively and excludes only explicitly known unused scenario branches. The shell rig entry point, harness lock and owned-process supervisor are also mandatory roots. Changes to common validators or ownership wrappers therefore change the expected acceptance identity.

Nine new closure controls and one export-tampering control passed; the complete compatibility suite passed 58 tests on each line (290 total). The first checks exposed incomplete synthetic retained-source fixtures, which were updated to retain the required wrappers without weakening the validator. The corrected primary rig suite passed all 459 tests; the three scope controls also passed independently on all five lines. Code commits are `5f4bc7fa` / `c8d8835c` / `8b71a357` / `fbd74334` / `03d8df12`; synthetic rig-fixture follow-ups are `eea00395` / `0ee2969b` / `e5e31ac7` / `14e64c74` / `74653969`.

Previously collected attempts may only be re-exported from their original complete retained tool sources. Every newly included dependency must match its original manifest; current acceptance separately requires its hash to match the independently selected current source. The regular-map C2ME run's expanded eighteen-file dependency map matched current bytes exactly in the first comparison. Other export/target refreshes remain pending. No old result or artifact is rewritten and no semantic equivalence is inferred from a passing summary.


The strengthened dependency refresh subsequently verified all eighteen retained attempts (sixteen native server attempts including the failed 26.1 run, plus two C2ME runs). All seventeen independently intended passing targets matched, with zero differences between newly included current dependencies and their original retained manifest hashes. Expanded scopes contain 21 files for native server smokes and 18 for C2ME. Refreshed records are imported in the owning catalogs; original exports remain in documentation history. All five catalogs validate.

A further deletion control exposed an exporter-only gap: removing a transitive helper from retained sources could make scope resolution treat its import as external and omit its hash. Export now verifies the complete originally retained tool snapshot before resolving the selected scope. The control fails against the prior exporter and passes with the fix. All 59 compatibility tests passed on each line (295 total). All eighteen actual attempts were re-exported with the maintained fix and remained JSON-identical to the independently validated records. No acceptance target or shipping artifact changed. Commits: `796532a4` / `ea8bb839` / `e1432100` / `a439684c` / `1177957b`.

Elytra 1.21.10 attempt `20260910T045559Z-50c173ac648c` (run hash `80959e8dd0a968f8946c297e414a4252b484e2a2fa45486f04cb8fe052423932`) completed all six automated assertions and collected with complete cleanup. It remains **awaiting-review**: no proxy can be identified in its captured sky image. Actual mouse-grab and pitch observations now confirm landing input, and player-eye framing is within one degree of the submitted target; the actual render camera and visible draw remain under investigation. A separate camera/HUD diagnostic is staged, not yet run. This does not close the visual gate or the independently required 1.21.11 run.


## Port resumption preserves exact-patch skips

Two real-Git controls exposed a resumability defect: after resolving the first conflict, `resume` retried a later commit already present by ancestry or exact patch equivalence. Git reported an empty cherry-pick, the tool recorded a conflict with no conflict paths, and the following commit was never reached. Resumption now uses the same exact-patch check as initial preparation, persists the skip and continues to the next commit. It does not infer adapted equivalence. Both controls fail against the prior implementation; all fourteen maintained line-tooling tests passed on each support line (70 total), including assertions that the caller's HEAD, index and working tree stay unchanged. Commits: `298d47b7` / `94d923f3` / `6605a5be` / `1c3e983f` / `e8278bec`.

Follow-up tooling evidence is retained privately under `~/.local/state/lss-project-improvements/tooling-followups/20260910T051500Z`, including the original failures and corrected controls. No shipping artifact or native acceptance dependency changed in the port-resume fix.

### Required settings identity and selected failure accounting

A reproduced Paper smoke false acceptance changed only intended distance 16→17 while the old pass still matched. Required targets now bind initial configuration/world, JVM, backend and environment identity; staged source bytes are verified. Original intended recipes and retained manifests remain the authority for re-export, never the outcome of a passing run. Performance slots that fail before report assembly can now be recorded as terminal failures without manufacturing metric samples. Focused controls and actual retained-input comparisons are recorded separately; this is tooling correctness, not performance acceptance.

All twelve final settings UI profiles passed their seven assertions (84 total), restored configuration and completed cleanup. See [the final cadence-guard batch](client-ui-final-cadence-guard.json). Separate preset attempt 1 failed native input and remains preserved; attempt 2 passed with actual command/config evidence. These UI results do not close the remaining map, lifecycle, performance or independent-review gates.

The settings/failure-accounting fixes passed the complete tooling suites on every line: 59 compatibility, 14 line, 481 rig and 11 verification controls, 565 per line / 2,825 total with zero skips in selected runs. The first 1.21.11 attempt skipped five owned-run controls during a fixture build; it is preserved and the isolated rerun passed. Seventeen independent targets and eighteen original server/C2ME records matched with no settings or checker-source discrepancies; the original failed server attempt remains failed. See [runtime settings verification](runtime-settings-identity-verification.json).

### Final primary lifecycle and render batch

All four final receive-lifecycle profiles and both send-admission profiles passed and collected with complete cleanup. Independent intended settings, candidate/fixture roles and retained/current checker closure match. See [receive lifecycle](receive-lifecycle-final-batch.json) and [send admission](send-admission-final-batch.json). Both final seated-render runs passed five automated assertions and cleaned up, but remain awaiting the actual user visual response: Fabric `20260910T060828Z-0e37222dff16`, NeoForge `20260910T060931Z-989aec720720`. Their actual PNGs were displayed and a question tied to those two IDs was sent; no answer is inferred. See [seated render](seated-draw-final-batch.json).

Elytra diagnostic `20260910T061211Z-1885691a1e3d` remains awaiting review with a sky-only capture and complete cleanup. Native camera/fog evidence establishes a roughly165-block subject beyond the actual96-block fog end (configured/effective distances16/6), with proxy invisibility false. This does not prove a product renderer defect or visual acceptance. A new explicit fixture recipe uses native view distance12 and entity tracking25%, retains mandatory vanilla-player absence, and changes no fog/render implementation. The separate HUD diagnostic now focuses its owned observer window; 41 focused controls passed.

### Current Folia correctness failure retained

Run `20260910T061639Z-d1f739ae9854` failed and collected with complete cleanup. The earliest observed mismatch is D receiving actual disk-source bodies for a target requiring loaded-source evidence. C later reaches the fixture timeout shared by missing acknowledgment and missing native ownership; its final acknowledgment file contains both edit IDs without first-observation times, so the cause is not established. Region identities remain stable and ticket-retention code is present; no product regression, lost-ticket cause or benign flake is inferred. The failed exact-target export is retained. Further read-only fixture observations are required before a new attempt; no timeout or source label is relaxed.

Paper target selection also exposed a tooling false positive: a private fixture sharing the project package was treated as a production jar. Exact plugin main plus LSS/VSS name now identifies production; malformed partial identities fail closed. All ten actual Paper branded jars and the real fixture were checked, 535 staged controls and 30 maintained focused controls passed, and all four source checker closure identities remain unchanged.

Both new native-visibility Elytra runs now passed all six automated assertions, retained actual vanilla-player absence and showed a small gliding figure in the actual captures: 1.21.10 `20260910T064101Z-1d7dbe3f857c`, 1.21.11 `20260910T064330Z-9d7650011250`. Cleanup is complete, both independent targets match, and both user visual assessments remain pending. See [Elytra final batch](elytra-final-batch.json) for exact identities and seven preserved earlier attempts.


### Supplemental standalone status and client preset controls

Both primary no-Sodium/no-Voxy client controls passed six assertions each and collected with complete cleanup: Fabric `20260910T065412Z-8a9db3f8850c` and NeoForge `20260910T065732Z-dd60c1b54e8f`. Actual private-window inspection verified the standalone `/lss status` screen; the typed exports and resolved mod audit confirm Sodium and Voxy absent. Both runs exercised `map-only` and explicit `map-only-xaero-writes` preview/apply/undo, verified preview made no changes, preserved unrelated fields, demonstrated applied-but-unsaved behavior under an owned persistence blocker, and restored the original configuration. Native v20 negotiation was observed during explicit Xaero-enabled phases.

[The supplemental final batch](client-presets-no-sodium-final-batch.json) binds exact run/profile/scenario, proof, receipt and inspected image identities. These are twelve supplemental command-route assertions, not additional required-matrix rows, render approval or performance acceptance. They close the previously identified standalone/explicit-client-preset coverage gaps; server restart/preset scopes and measured conservative values remain pending. Earlier UI/preset failures and the historical audit are retained unchanged.


## Folia ACK/ownership diagnostic — 2026-09-10

The separately declared diagnostic run `20260910T071348Z-14abb3007ac7` passed all 28 target/source checks, actual region overlap and a 1.65067548-second drain, with five sessions and complete owned-process cleanup. Its 37 diagnostic rows independently recorded first acknowledgments for all 16 loaded mutations; the maximum first-ACK latency was 5.350 seconds and every mutation had current native ownership. No diagnostic failure occurred. Exact bindings and collected results are in [folia-ack-owner-diagnostic.json](folia-ack-owner-diagnostic.json).

The earlier current Folia failure remains failed and its cause remains unresolved; it did not reproduce here. This diagnostic was declared separately before launch and does not replace the required current matrix run or any performance measurement.


## Native global preset/restart acceptance — 2026-09-10

All six primary server runs passed ten native controls with independent receipt verification and complete cleanup. Paper and NeoForge each exercised preview/apply/undo, actual save failure, restart staging and readback after a separate restart. Fabric supplied its restart pair; its apply/undo/save-failure coverage remains in the final UI evidence. Each run matches its independently prepared artifact/fixture/settings/checker target. Exact results are in [server-presets-final-batch.json](server-presets-final-batch.json). These checks cover qualitative presets; the measured conservative numeric preset remains pending.


The scoped checker identity now includes exact staged Python scripts under stable run-relative targets and follows their maintained imports. Both original no-Sodium runs were re-exported from retained scripts and full original tool snapshots; their expanded23-file scopes exactly match independently checked current intended bytes. Missing/changed script or helper controls fail closed. Original results, proofs and the original final-batch snapshot remain unchanged. See [expanded supplemental identity](client-presets-no-sodium-expanded-identity.json). This does not infer arbitrary external launch provenance; the existing exporter checkout-origin assumption remains an explicit final-review question.

## Current required Folia source acceptance

Run `20260910T075349Z-cea267b50bcd` passed the unchanged required 550-second sparse source scenario and collected with complete owned cleanup. The independent required target matches exactly; [summary](folia-source-final-batch.json) and [exported record](folia-source-final-record.json) bind both production candidates, all five fixtures, settings and checker. All 28 offered targets committed with exact payload sources; five sessions include the real reconnect, and drain took 1.650 seconds. This closes this Folia correctness case only. The failed `20260910T061639Z-d1f739ae9854` and diagnostic-only `20260910T071348Z-14abb3007ac7` remain separate retained evidence; their history is not rewritten. Paper/Fabric, sustained every-edit delivery and baseline functional linkage remain unrun on these fresh recipes. No calibration or measured acceptance is claimed.


### Accepted runtime scope refresh — staged Python

An independent original-recipe audit verified35accepted cases against current scoped dependencies: UI12, four WI5 and two WI6 profiles are unchanged; fifteen native server and two C2ME profiles have equal expanded identities. Only exact staged Python keys were added for those17cases; no previously scoped dependency changed, all target fields match, and original recipe/run/proof/result files remain unchanged. The previous server/C2ME batch identity snapshots are preserved under `identity-history/pre-staged-python-scope-20260910`; current batches point to independently matched fresh targets and retained-source exports. See [scope summary](accepted-scope-audit-summary.json) and [full audit index](accepted-scope-audit-index.json). Full reports have a durable file/hash manifest. No catalog import or new native pass occurred, and pending seated/Elytra human reviews are excluded.
