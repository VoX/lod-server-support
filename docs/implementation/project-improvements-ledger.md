# Project improvements implementation ledger

**In progress — 2026-09-14.** The V27 Minecraft 26.2 comparison is accepted: three calibrations and three measured pairs passed all 30 calibrated comparisons. The measured opt-in conservative preset is 32/4/1; defaults remain unchanged. The 41 numeric placements and final classification updates are committed across all five lines; their exact commit map is recorded in the checkpoint. Final all-five builds, 52 native rows, 8 human assessments and 6 fresh final Astra reviews remain unfinished.

The [implementation plan](../planning/2026-09-09-project-improvements-implementation-plan.md) and [original baseline](baseline.json) remain authoritative. The corrected reference and candidate share correctness fixes; V27 measures remaining improvement costs, not the total original-to-final change. See [performance scope and the third-pair tick-delay exception](../operations/performance.md), the [public summary](../operations/performance-v27-summary.json), and [current checkpoint](current-checkpoint-2026-09-12.json).

The original 67-suite migration is unchanged. Its retained matched 689-case comparison and prior source/test evidence are historical qualification, not new executions. The 162 nonnumeric placements and later 75 fixture/tool placements are committed; 110 maintained Python checks and 12 maintained pure Java mains passed separately. The two UUID classification entries were completed; the final nine classification actions and 41 numeric placements are now committed. The 82 new JUnit identities remain expected, not yet executed.

Six functional prerequisites retain their actual product/fixture identities, full 23,056/23,056 and sparse 28/28 outcomes, cleanup and earlier CPU-placement scope. V27 adds its own nine validated automatic setup/affinity/heap receipts; earlier functionals do not prove the newer placement. Prior startup failures, calibration-inconclusive attempts, coverage misses and diagnostic limitations remain preserved. No failed historical experiment is relabeled as accepted.

Whole-client 30 FPS cadence includes pacing; RSS under fixed 1500M client/2G server pre-touch is resident memory, not live heap. Xaero completed-write intervals are descriptive, not isolated pump cost or a new threshold. V27 is measured 26.2 evidence, not final integrated artifact qualification or a cross-line performance claim.

The [original ledger](project-improvements-ledger-history-2026-09-10.md), [recovery history](project-improvements-ledger-history-2026-09-12.md) and preserved pre-V27 dated snapshot retain prior decisions. Historical pending labels describe those checkpoints. No merge, push, publication or deployment is included. Existing unrelated dirty/untracked evidence must be preserved.
