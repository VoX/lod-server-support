# Project improvements implementation ledger

**Visual review found map defects; correction and acceptance remain open — 2026-09-20.** P1–P7 of the [implementation plan](../planning/2026-09-09-project-improvements-implementation-plan.md) are implemented on Minecraft 1.21.1, 1.21.10, 1.21.11, 26.1 and 26.2. The conservative preset remains opt-in at 32/4/1; shipping defaults are unchanged. V27 is accepted for its recorded Minecraft 26.2 workload: three calibrations, three measured pairs and all 30 calibrated comparisons under the preregistered rule, comprising 25 nonduplicate comparisons, not 25 statistically independent observations. Its limits remain in [performance](../operations/performance.md). The [original baseline](baseline.json) remains the comparison basis.

The subsequent [visual follow-up](visual-review-followup-2026-09-20.md) establishes a Xaero region-boundary shading limitation and an LSS/native-map acquisition gap at eight chunks. Map product corrections remain pending. The seated fixture now captures a closer healthy pose before its intentional fault, with separate recovery captures on Fabric and NeoForge. The original failed setup attempt is retained. These findings supersede the earlier “only sign-off remains” interpretation; the dated 54-case progress index is historical.

## What is established

- **Build provenance.** Full Fabric, Paper and NeoForge builds, `:common:test`, Tier 1, Tier 2, Tier 3 where available, packaging and release checks passed on all five lines on September 14, including the 82 new preset/undo JUnit identities. Original build commits remain 1.21.1 `0ae20c5f`, 1.21.10 `8a6457bd`, 1.21.11 `b8828f5a`, 26.1 `f8f31bb6` and 26.2 `6f708110`. Reuse is justified by byte equality of the recorded shipping source, resources, Gradle/build and license inputs, including physical worktree inputs; it is not inferred solely from an empty Java diff. Later tooling, catalog and documentation heads are recorded separately and do not relabel those builds. The [checkpoint reconciliation](current-checkpoint-2026-09-12.json) retains the exact evidence references and their scope.
- **Final artifact checks.** Test-move validation, shipping LSS/VSS test-class exclusion and rig-fixture exclusion passed on all five lines. All 15 local candidate JAR hashes equal their retained September14 artifacts; no new shipping build is claimed.
- **Fixture qualification.** The final selection covers 14 fixture families and 20 artifacts. Three original successful receipts were retained; recovery produced 12 successful fresh builds, including one replacement, for 15 successful compile attempts overall. The first 26.1 smoke fixture used the support-line label as its engine parameter and honestly declared exact Minecraft 26.1; its native profile requires 26.1.2, so it did not qualify that runtime. A fresh build derived 26.1.2 from the owning gradle.properties and recorded the actual compiler, metadata and unchanged final product inputs. The successor index replaces only that fixture family and retains the old index/receipt as historical evidence. Original failures remain recorded; no shipping product or metadata admission guard changed.
- **Correctness.** The production fixes cover diagnostic-export lifecycle retention, reconnect receipts, bounded per-player Folia loaded-versus-disk correction ordering, header freshness, rig consumer refusal and retention after store/empty replies. Reviews retained the epoch, registration, ownership, ordering and tscache/store/header contracts.
- **Reviews and harness repairs.** The six [September final reviews](../reviews/2026-09-14-final/README.md) remain attributed to four Astra and two Claude Code reviews. Fable's September 16 completion preserved the R04 loader-route and R05 cleanup, display, environment and metric-schema fixes. Three [Astra completion reviews](../reviews/2026-09-19-completion/README.md) subsequently checked rig ownership, concurrency/performance conclusions and the native Connector/Elytra follow-ups. The reaped-process signalling guard and manifest main-section repair are applied on all five lines. The bounded Elytra recapture still requires fresh genuine mouse evidence; a source review is separate from live or user visual acceptance.
- **Performance.** V27 remains limited to four Fabric clients with a Folia server on 26.2, fixed heaps and 30 FPS pacing. Third-pair server tick-delay p99 exceeded its individual bound; the unchanged median-plus-two-of-three rule passed. Frame cadence is pacing-saturated, tick noise is comparable to the 10% budget, and 32/4/1 was shared by both arms rather than compared with defaults. The corrected reference excludes the shared correctness-fix costs. Earlier Fabric/Paper prerequisites and measured Folia arms retain their original artifact identities.

## Automated results and remaining acceptance

- **Native execution.** The [dated validation progress index](native-validation-progress-2026-09-20.json) records 46 collected passes and eight cases whose automated checks are green but await actual user image decisions. The contract has 54 distinct runs: 42 required matrix rows, four supplemental cases, six preset runs and two additional Fabric/Paper source checks; Folia is already counted in the required 42. Fabric and Folia each passed 28/28 source obligations, with Folia owning-region evidence. The first final Paper attempt failed at 27/28; a bounded diagnostic replay passed 28/28. Both attempts retain their exact identities. That replay closes the final-byte Paper functional gate for its recorded diagnostic configuration; it does not prove the first failure’s body content or turn it into a pass. That was the pre-feedback execution snapshot. The visual follow-up adds one map diagnostic, one failed seated setup attempt and two successful seated recaptures; none supplies user acceptance.
- **Eight user visual assessments.** The four Xaero, two seated-draw and two Elytra cases require the user's real disposition bound to the captured images through the [visual-review workflow](../testing/visual-review.md). Automated proof or a code review cannot supply that disposition.
- **Final harness and handoff checks.** All five complete rig suites passed 628 tests each with zero skips; the non-rig suites passed 735 tests total. The cleanup observation covered 57 retained run inventories and 319 recorded process identities; game cleanup passed, loopback bindings were available and the coarse harness lock was free. The owned review gallery remains intentionally live for the user, so overall plan cleanup is not complete. The separately requested Windows usability/resource-coexistence observation remains unanswered. Input isolation does not by itself establish absence of resource interference. Native Windows remains observation-only/unverified.
- **Evidence and catalog closure.** All 46 passed cases completed exact-target export into external partial-review-only staging. Its matrix evaluation reports 34 passed and eight unverified rows per line; the exact unverified reason is “missing complete explicit acceptance target,” because the eight user-reviewed cases have not yet been exported. This is not final maintained matrix publication. The full 54-case record/matrix import remains gated by the eight actual user image decisions; the partial-apply guard stays intact. Export only collected passes with complete cleanup and exact candidate/fixture/scenario bindings. Reconcile the 42 required targets using committed line-owned validation records and pinned source refs before recording overall acceptance as complete. The visual follow-up is the latest status record; the progress index preserves the earlier selection; a reserved final-acceptance filename is not completed evidence.
- **Retained follow-ups.** R06-1's unmeasured redundant correction bodies, R06-2's own-region correction scope, and the launch-environment allowlist follow-up remain recorded. Final acceptance does not remove them or extend performance claims.

## Retained history and operation

The [original ledger](project-improvements-ledger-history-2026-09-10.md), [recovery history](project-improvements-ledger-history-2026-09-12.md) and historical checkpoint fields preserve dated decisions and evidence. Failed and inconclusive experiments remain such. The September 14 implementing session ended at a usage limit after review 4; Fable's Claude Code completion on September 16 supplied the R04-1 commit, reviews 5–6, their fixes and consolidation. Subsequent Astra work supplements that history.

Support tiers are unchanged: Fabric/Paper on main 26.2 retain full support; 26.1/1.21.11 retain their documented tier; NeoForge and the 1.21.1 line retain best-effort support. Folia remains experimental, including after successful native checks. Minecraft 1.21.1 has no Folia build or Tier 3 client game-test task; unavailable tasks are not silent passes. No merge, publication or deployment is included.

See [disposable rigs](../operations/disposable-rigs.md), [storage guard](physical-host-storage-guard.md), [failure catalog](../operations/test-flakes.md) and [ownership](../architecture/ownership.md). Automated clients use private WSL displays; normal Windows clients, personal worlds and the user's desktop remain outside the rigs.
