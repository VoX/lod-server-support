# Project improvements implementation ledger

**In progress — 2026-09-14.** P1–P7 implementation is present across Minecraft 1.21.1, 1.21.10, 1.21.11, 26.1 and 26.2, except the measured conservative preset and its final integration. The latest store/all-air correction is committed on all five lines and the corrected comparison reference; 113 targeted cases passed on 26.2. The measured 26.2 candidate is qualified by its retained full build plus an inventory-only supplement; the corrected reference passed full qualification and release checks. The six V20 functional prerequisites passed for their recorded fixture. The reviewed Folia UUID fixture repair has fresh paired builds and verified V21 preparation; all four fresh Folia gates passed, with exact unchanged Fabric/Paper evidence retained. Performance remains unaccepted, and final native, human and six new independent Astra reviews remain pending.

The [implementation plan](../planning/2026-09-09-project-improvements-implementation-plan.md) remains authoritative. [Original baselines](baseline.json) remain the starting point for final source review. A corrected reference is used for the performance comparison so both arms include the automatic correctness fixes; their cost is excluded from the feature comparison. No merge, publication or deployment is included.

| Package | Implemented behavior | Acceptance still required |
| --- | --- | --- |
| P1: status and exports | Immutable status snapshots; UI and commands read cached state; bounded exports capture an allowlist and retire feedback with its world/session | Final artifact-bound UI/export checks and measured impact |
| P2–P3: compatibility and support lines | Catalog and resolver, generated views, exact file classification and isolated port preparation; line-specific APIs and unavailable tasks remain explicit | Final source/blob references and inventory checks after remaining integration |
| P4: disposable rigs | Private displays/input, owned process cleanup, physical host storage guards, exact artifact/run association and independent raw checks | Applicable final native matrix and actual human assessment |
| P5: Xaero ownership | Facade delegates to session, bindings, acquisition queue, tile writer and rebuild scheduler; lifecycle, lock order and bounded frame work are pinned | Final map/lifecycle evidence and measured frame/work interpretation |
| P6a: test organization | 67 original pure suites moved to common, Java21 retained, test identities and external corpus inputs accounted for | Final exact case accounting; new regressions remain separate from the migration baseline |
| P6b: concurrent correctness/performance | Four subjects, owning-region checks, original revision intervals, delayed-consumer/reconnect faults and fixed calibration/pair laws | Fresh qualified inputs and an accepted complete performance experiment |
| P7a–P7b: settings and presets | Shared descriptors, coherent publication, preview/apply/undo, persistence feedback, qualitative presets and operating guides | Derive conservative values from accepted evidence, integrate remaining measured placements, then validate final artifacts |

## What the evidence establishes

Historical all-five build campaigns passed their recorded platform, JUnit, available game-test, packaging and release gates. They do not qualify later source changes. The six header/refusal functional prerequisites completed by September14: both sustained runs committed all 23,056 original targets, and all four sparse runs committed 28 of 28. Their raw results matched retained proofs and owned cleanup completed. These are product/fixture correctness prerequisites, not a completed performance comparison. Exact receipts and current pending bindings are in [the checkpoint](current-checkpoint-2026-09-12.json).

Test migration has a real matched comparison: 689 identical case/outcome identities passed in both arms, with separate untimed warmups and forced selected execution. Original Fabric invocation time was 129.065s versus 127.233s in common; summed case times were 115.114s versus 114.671s. This is one pair, not evidence of a general speedup. See [test-placement validation](xaero-test-validation.md) and the unchanged [migration inventory](test-moves.json).

The supporting integration applied 162 nonnumeric placements across the five lines. Its 3,065 distinct Python cases passed; a separate later compatibility campaign passed 91 per line. The 75 reviewed fixture/tool placements are now applied and locally committed across all five lines, together with the two exact UUID helper/test classification entries per line. The actual maintained Python checks passed 22 cases per line, 110 total. The maintained 26.2 pure controls also passed eight server and four client Java mains; these 12 mains are separate from the Python case count. Only 41 measured product/test placements and nine final classification actions remain pending. The original eleven-action plan and its guards remain immutable; separate completion receipts account for the two finished entries. Exact applied paths, before/proposed hashes and commit scope were checked; this does not mean the worktrees are clean. These placement counts are not feature counts or proof that final source classification is complete after subsequent corrections.

## Correctness fixes and current qualification

Implementation and native investigation exposed real lifecycle and ordering defects: stale loaded captures and registration guards; an old client worker polling a replacement session's receipt; Folia probe publication races and the first 512-position coverage gap; and cancellation of admitted work by later frontier or empty backpressure declarations. The corrected lifecycle retains one bounded owner opportunity for each admitted disk attempt, without extending its original lifetime or holding the request open. A full uninstrumented confirmation and later qualified sustained passes established their own exact input outcomes. Short diagnostic successes did not necessarily exercise the corrective-send branch.

The fixture now refuses excess deferred receipts explicitly and reports ingest failure before releasing them, preserving its 128-hold bound. V18 subsequently exercised 30 actual refusals without recorder overflow or report/release exceptions, but still missed eight D commitments. All eight had acknowledged/applied targets and three received wires within their original intervals. The retained trace does not identify which receipts were refused or decode the missing block predicates; body lengths are not content proof.

Source review then found a separate store/all-air gap: a store body or freshness response could discard the already-admitted late loaded opportunity; an empty disk/store fallback could do the same. Eight focused old-production sender tests failed at the missing correction assertion. The fix preserves that exact opportunity after eligible resolution, including an authoritative empty clear, while rejecting ghost, edit-stale and failed-enqueue paths. All 113 cases in the targeted six-suite campaign passed, including 12 new cases. The three changed paths are committed across all five lines and the corrected reference. This proves the tested router/sender paths, not the complete cause of every native miss. See [ownership and the linked tests](../architecture/ownership.md).

The retained 26.2 full build executed at `84f63361`; its manifest remains failed for the migration inventory mismatch. The exact metadata-only supplement qualifies product inputs at `b7f1b277` without relabelling that build or changing its products. Reference `c2283b03` passed full qualification and release checks. Other lines still require their final full gates after remaining integration.

The first fresh Paper sparse run delivered 28/28 targets but missed the scheduled D pause entirely. Its one explicitly authorized unchanged retry also missed that premise, was stopped early and completed owned cleanup. Both failures remain preserved; neither qualifies. The externally staged sparse-only correction arms an original target and holds its actual receipt for 20 seconds, with exact trigger/wire/receipt checking and target commitment after the hold. Measured 840-second runs retain their fixed 160–180-second pause. Eight fresh paired builds and full ZIP payload comparisons passed. All six new functional runs then passed: both Folia sustained runs committed 23,056/23,056, and Paper, Fabric and the two Folia sparse runs each committed 28/28. Each completed owned cleanup without ownership errors. Exact run identities and acceptance receipts are in the checkpoint. These passes cover their current qualified products, fixture bytes and checker; they do not constitute performance acceptance or replace the two earlier failures.

## Performance and remaining completion

V17 ended inconclusively during baseline calibration: the first two accepted baselines already had tick-delay and server-RSS spreads that no third sample could bring within the frozen 10%-of-median rule. V18 used explicitly reviewed isolation settings in both arms, then terminated on the eight-target correctness failure in its second calibration. Neither experiment ran a candidate measurement or selected a conservative preset. V19 remained unused after the functional blocker. V20 preparation is verified and its six functional prerequisites are complete, using the same qualified products and isolation settings with the new fixture/checker identities. V20 then terminated in calibration slot 0: `20260914T101725Z-acf06df8c58a` timed out waiting for joint readiness before any workload origin, accepted calibration or measured slot. Owned cleanup completed with no remaining children or ownership errors. C had product registration and a native owner observation but no first oracle-session handoff; the exact handoff guard outcome was not observed, so no causal diagnosis is established. The six earlier functional passes remain valid only for their recorded inputs. The UUID correction does not retroactively establish the failed run’s unobserved lookup result. V20 postprocessing remains unexecuted, and no conservative selection exists. Failed and interrupted slots remain preserved; they are not retried or relabelled as accepted slots.

The integrated Folia fixture now records subject UUIDs and resolves the current player by UUID instead of relying on the unsafe name-map lookup. Eight existing pure control mains passed, including the helper lifecycle checks; the fresh eight paired fixture builds and full entry comparisons passed. Only the Folia archive changed, so the accepted Fabric/Paper gates retain their original recipes and exact unchanged archives. The paired build inventory is honestly 54 sources: prior 53 plus the runtime helper. The final six-group inventory is expected to contain 55 sources with the separate helper test now integrated; final inventory derivation remains pending. V21 unbound preparation passed with unchanged products, 2G/pre-touch, EDF4, 30-FPS/minimized settings and original fixed workload laws. All four fresh Folia gates passed independent audit: both sustained runs committed 23,056/23,056 and both sparse runs committed 28/28, with owned cleanup complete. The candidate sustained run exercised 32 capacity refusals and reached 128 held receipts while still meeting its strict source and debt checks; this is not a claim about unobserved receipt content. The mixed six prerequisites retain the two exact original Fabric/Paper runs. V21 then ended terminally inconclusive during baseline calibration. Its first two original baseline runs were accepted, but their tick-delay p95/p99 spreads made the frozen 10%-of-median rule unreachable for any third sample. The original third claim, `20260914T123254Z-401c984192e1`, was stopped during startup and its incomplete result preserved. Owned cleanup left zero processes. No candidate measurement, accepted complete performance comparison or numeric selection exists; the functional qualifications remain valid for their exact inputs. Postprocessing remains unexecuted, and no retry or replacement is authorized.

Xaero was active in these profiles. Actual bridge-written counters progressed during the measured windows, and the existing assembler checks that progress. Frame timings include native pacing; coarse counter increments are not per-frame bridge CPU costs or map-content correctness. The earlier inference that Xaero was absent was withdrawn, and separate speculative comparison drafts were abandoned.

Completion still requires an accepted fixed nine-slot experiment (three baseline calibrations, then three ordered baseline/candidate pairs), with the original workload, source laws and budgets. The tested 32/4/1 settings remain inputs, not accepted conservative values. After accepted derivation and integration, complete final build/tool and accounting checks, 42 required native rows plus four supplemental rows and six preset runs, and eight actual human visual assessments. Earlier evidence can cover only independently matched unchanged inputs. Finally run six **new** Astra reviews over original-baseline-to-final changes, validate findings and apply justified corrections. Implementation and recovery agents do not count as those reviewers.

## Retained history and operation

[The original ledger](project-improvements-ledger-history-2026-09-10.md) and [the subsequent recovery history](project-improvements-ledger-history-2026-09-12.md) preserve dated decisions and evidence. Their active/pending labels are historical. Minecraft 1.21.1 has no Folia build or Tier 3 client game-test task; unavailable tasks are not silent passes.

See [disposable rigs](../operations/disposable-rigs.md), [the storage guard](physical-host-storage-guard.md), [the failure catalog](../operations/test-flakes.md) and [current ownership](../architecture/ownership.md). Automated clients use private WSL displays; normal Windows clients, personal worlds and the user's desktop remain outside the rigs.
