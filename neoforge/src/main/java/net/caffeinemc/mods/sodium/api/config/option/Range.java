package net.caffeinemc.mods.sodium.api.config.option;

/** COMPILE-ONLY STUB — see {@link net.caffeinemc.mods.sodium.api.config.ConfigEntryPoint}.
 *  The real type is a record; callers only touch the (min, max, step) constructor,
 *  whose descriptor a plain final class reproduces exactly. */
public final class Range {
    public Range(int min, int max, int step) {
        throw new AssertionError("compile-only stub");
    }
}
