# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Support branch: `support/mc1.21.1`** — the v0.11.0 FRESH CUT (stage G-3, off
> main @ e4c0a4cc) targeting **Minecraft 1.21.1** on Fabric + Paper + NeoForge
> (the ONLY line SHIPPING NeoForge at v0.11.0 — LINE_SHIP_NEOFORGE=true, user
> decision 2026-08-15; the working client pairing is the community Voxy port),
> **Java 21** (build with a Java 21 JDK — paperweight's codebook cannot parse
> Java 25 class files). **BEST-EFFORT tier, whole line** (user decision
> 2026-08-14): the spike's pre-authorized feature-drop list applies
> (docs/planning/pre-authorized-cuts.md), further cuts need a dated decisions-log
> entry. **Cut actually TAKEN: Tier 3 client gametests only.** The other two
> pre-authorized headline cuts were NOT needed: the Fabric far-player RENDER path
> is LIVE (reworked to immediate-mode `dispatcher.render` on
> `WorldRenderEvents.AFTER_ENTITIES`) and, since v0.14.0, the NeoForge renderer is
> LIVE too (the same-FQN twin renders immediate-mode on
> `RenderLevelStageEvent.AFTER_ENTITIES` + `EntityJoinLevelEvent`; it was a no-op
> stub through v0.13.x — `RENDER_AVAILABLE` is now true on both loaders here) and the Sodium config
> screen is LIVE (ported, registered under `sodium:config_api_user`; since the
> options-page-generations port below it renders on BOTH Sodium generations). Release
> notes must describe the shipped set, not the pre-authorization. **Folia does
> NOT exist on this line** (no 1.21.1 build — the 1.21 family starts at 1.21.4):
> `folia-supported` is ABSENT from plugin.yml with the pins INVERTED to absence
> (PluginYmlContractTest + release_check), the Modrinth loaders list drops folia,
> and `run-folia`/`SOAK_PLATFORM=folia` are unavailable here — the mainline prose
> below about Folia describes 26.2, not this line. Do NOT merge to `main`; releases tag
> `v<x.y.z>+mc1.21.1` (make_latest false). Per-line surfaces live in
> **docs/planning/per-version-surfaces.md** (canonical — this banner is a
> POINTER) and the line identity in `.github/line.env` + `gradle.properties`
> (loom-remap toolchain, intermediary mapping namespace, accessWidener "named").
> Line status at the cut: `NativeSectionShape` carries the FULL flavor —
> NATIVE_COUNT_SHORTS = 1, the three folds, and **NATIVE_LONG_ARRAY_PREFIXED =
> true** (the fourth axis, found AT this port: 1.21.1's vanilla container write
> VarInt-prefixes the long array; 26.x/1.21.11 write bare words; V20 is
> prefix-free everywhere by wire spec); `ScopedCarrier` is the pass-through
> variant; `SectionConstruction` twins carry the Registry-of-Biome-param ctor
> flavor; the dirty hook targets `ChunkSerializer.write` RETURN (no
> SerializableChunkData on this line — vanilla/Moonrise/C2ME routing
> bytecode-verified, see the mixin javadoc); goldens line-regenerated per the
> two-stage T1 rule (duplicate-air.bin + xray-masked.bin are hand/lever rows,
> production-verified by the golden byte-match). V-3 seams landed branch-first
> here (surfaces rows 1 + 11): `BackgroundIoSubmit` (S4 — this file carries the
> ProcessorMailbox/tell flavor; ChunkDiskReader's signatures are line-invariant)
> and the gametest `TestPositions` (T2 — ctor/field/asLong/addRegionTicket
> flavors concentrated in one file).
> **v0.13.0 port (2026-08-26):** the region-scan / hybrid-walk / Xaero-backpressure
> stack (main `feat/hybrid-scan` 31cfefe6..de34c616, 18 picks + version scrub —
> §18 heal DELETED with it, `enableXaeroMapBackpressure` replaces the heal key).
> **ONE line adaptation**: the §12 pick's buildConsumer keeps this line's
> `getMinBuildHeight()/getMaxBuildHeight()` (max already EXCLUSIVE) inside the
> incoming anonymous-consumer structure — contract-pinned by
> `XaeroWiringContractTest.theConsumerPassesThisLinesWorldHeightExpression`.
> Zero new MC-API surface otherwise; NeoForge needs no new wiring (xplat
> singletons via the already-pinned ClientNetGlue seams; both new keys are
> JSON-only). 2-Opus port review: code clean both lenses. Gates: T1 2081/0, T2,
> :neoforge:build, selftest 270 + validate, pre-flight + release_check 0.13.0
> OK, fresh-backfill smoke (hybrid-boundary itself runs on MAIN per the plan —
> accepted under this line's best-effort tier; `./scripts/soak.sh
> hybrid-boundary` is available here if the full 30-min gate is wanted, its
> sizing is MAIN-measured). NOTE: §12 makes the still-owed NeoForge Xaero
> 1.45.0 live check MORE load-bearing — an unverified NeoForge Xaero build now
> governs the LOD download rate, not just the map.
> **v0.12.0 port (2026-08-21):** the region-summary/stamped-up_to_date/quadtree stack (main 79e49951..e0cdf6f2, 20 picks + the #215 checker floor) — adaptations: ResourceLocation/location(), template=/timeoutTicks= gametest annotation, split-dir resolver re-root, panel folds (c2f4374b); wrs green both platforms; NeoForge gametest smoke repaired (was a silent no-op since the v0.11.0 cut — see neoforge/build.gradle).
> **Options-page-generations port (2026-08-23, main PR #236 per sodium-options-page-generations-plan.md §12):** this is the DUAL line — Sodium 0.8.13-beta.2 (the m3t4f1v3/Create+ path, `LSSConfigMenu` + `sodium:config_api_user`, ModMenu 11.0.3) AND 0.6.13 (the NeoForge Voxy-fork pairing — `LegacySodiumPage` + the `@Pseudo` `SodiumLegacyOptionsHook`, both loaders) light up. Line adaptations: `Identifier` → `ResourceLocation` in `LSSConfigMenu`, `LSSConfigMenuTest` and the api/config recording stubs (the stubs must carry the REAL 0.8.13 descriptors, which use ResourceLocation here); JAVA_21 legacy mixin configs (both trees, twin-pinned); `suggests.sodium` `>=0.8.13-beta.2` → `*` (0.6.13 is a supported client now); surfaces row 15 (= main's row 18); `sodium_version=mc1.21.1-0.8.13-beta.2-fabric` feeds the modern golden arm only. The live gates matter most here (both generations × both loaders).
> **NeoForge Sodium 0.8+ page (2026-08-26):** the NATIVE NeoForge jar now registers the page too — sodium-neoforge's `ConfigLoaderForge` reads the same `sodium:config_api_user` key from `[modproperties.lss]` in neoforge.mods.toml and reflectively instantiates the same-FQN `dev.vox.lss.config.LSSConfigMenu` twin (ModList/IModInfo metadata; catalog walk identical to Fabric's). The `net.caffeinemc.mods.sodium.api.config` stubs are COMPILE-ONLY: shadowJar excludes `net/caffeinemc/**` and release_check's NEOFORGE_FORBIDDEN pins their absence; `SodiumConfigApiContractTest` pins the TOML property + the walker's interface/no-arg-ctor at the CLASSFILE level (reflection would link MC types absent from the plain-JUnit classpath).
> **Xaero map bridge port (2026-08-23):** the issue #223 stack (main PRs #229/#230/#231 + the #232 default-OFF flip) — line adaptations: `Identifier` → `ResourceLocation` (tests + menu id), `getMinY()/getMaxY()+1` → `getMinBuildHeight()/getMaxBuildHeight()` (max already EXCLUSIVE — the +1 drops), `getLightDampening()` → 2-arg `getLightBlock(EmptyBlockGetter.INSTANCE, BlockPos.ZERO)` (this line's only overload; constant-opacity approximation), the `Registry<Biome>` section family via the line's SectionConstruction seam, `BLUE_/RED_STAINED_GLASS`. 2-Opus port review: no MAJORs. The bridge is live on the NeoForge client too (the shared ClientNetGlue pump) — configurable from the NeoForge Sodium 0.8+ page too since 2026-08-26 (the entry above); the config key remains the only surface on page-less stacks. Still owed: one live check per LOADER of the 1.21.1 Xaero 1.45.0 jars in the test instances — the FABRIC jar was one of the two member-verified ones (lowest risk of the four), but the NEOFORGE Xaero build was never inspected.
> **v0.11.1 port (2026-08-18):** the stutter-fix pair (scan prefix retention +
> acquisition frontier, main PRs #203/#204 + folds) cherry-picked from main —
> pure xplat/common, no line flavor points; the body below is main's CLAUDE.md
> as of that port, with the hand-mirror corrections noted inline.

## Project

LOD Server Support (LSS) — distributes LOD chunk data from servers to clients over a custom networking protocol. Clients request distant chunks individually; the server reads them from disk or memory and streams serialized sections back, enabling LOD rendering mods (Voxy) to display terrain beyond vanilla render distance. Supports Fabric (client + server), Paper (server only), and NeoForge (see tiers below). The one plugin jar also serves Folia (regionized probing + lifecycle mailbox code paths; `folia-supported` is declared on lines where a Folia build exists upstream — on THIS line its ABSENCE is the hand-maintained fact, hard-asserted by PluginYmlContractTest's inverted pins (no 1.21.1 Folia exists; the R2-5 line.env derivation is main-only)). **Folia support is experimental on every line**: all six Folia soak scenarios pass single-player, but the stated exit criterion — concurrent MULTI-REGION ingress — has never been produced by the harness. Do not retire the experimental label on soak greens alone.

## Support tiers (v0.11.0+; docs/planning/neoforge-support-plan.md is normative)

**Full** — Fabric + Paper on main (26.2): complete gauntlets (T1/T2/T3), 23-scenario soaks ×3 platforms, live-rig burn-in, first-priority triage. **Correct, not
perfect** — the 26.1/1.21.11 lines: full builds + T1/T2 and representative smoke
soaks, no live rig, no exhaustive gauntlets. **Best-effort** — NeoForge and the
whole MC 1.21.1 line: they track the mainline feature set, but feature cuts are
acceptable where the platform/version fights (cuts beyond the plan's
pre-authorized list need a dated decisions-log entry — the §6.2 cut protocol; the
release notes must name the tier and any cut). Best-effort is a SUPPORT-commitment
axis, distinct from Folia's *experimental* (a correctness-confidence axis — see
below). **v0.11.0 ships NeoForge on the 1.21.1 line ONLY** (the one line with a
working client pairing — the community Voxy port; other lines' NeoForge clients
have no Voxy route). Gated by `LINE_SHIP_NEOFORGE` in `.github/line.env`, which on THIS branch release_check.py MIRRORS by hand (`SHIP_NEOFORGE` — flip BOTH together, per its comment; the R2-5 derivation chain is main-only); build.yml still builds + tests the NeoForge
module on EVERY line so the port stays maintained. **Wire compatibility is NEVER
tiered** — every jar speaks the same protocol at full fidelity, and every
never-tiered claim names a test that reds when violated (plan §1.2).

## Project Structure

Multi-project Gradle build:

```
lod-server-support/
├── common/   Pure Java utilities (no MC deps) — shared by all loaders
├── xplat/    Shared MC-logic SOURCE SET (not a Gradle project) — compiled by
│             fabric/ AND neoforge/ via srcDir; loader-pure (XplatLoaderPurityTest
│             whole-source pin; XplatJava21SurfaceTest constant-pool tripwire);
│             per-loader seams via dev.vox.lss.platform.LoaderServices + same-FQN
│             twins (LSSServerNetworking/LSSClientNetworking/FarPlayerRenderer/
│             LSSNetworking + the ChunkSaveDataHook mixin shim)
├── fabric/   Fabric mod (client + server), Minecraft 26.2 — hosts ALL test tiers
├── neoforge/ NeoForge mod (client + server), Minecraft 26.2 — BEST-EFFORT tier;
│             MDG 2, shaded EXCEPT sqlite (nested jarJar for module dedupe —
│             neoforge-jarjar-sqlite-plan.md), contract tests + 8-test gametest smoke
└── paper/    Paper/Folia plugin (server only), Minecraft 26.2 (unchanged by N)
```

## Build Commands

```bash
./gradlew :fabric:build                       # Build Fabric mod + Tier 1 & 2 tests (Tier 3 is CUT on this line — the task does not exist)
./gradlew :paper:shadowJar                    # Build Paper plugin JAR
./gradlew :neoforge:build                     # Build NeoForge mod (shadowJar + contract tests + vssJar)
./gradlew :neoforge:runGameTestServer         # NeoForge 8-test gametest smoke (exit = failed count)
./gradlew clean                               # Clean build artifacts
```

Output JARs:
- `fabric/build/libs/lod-server-support-fabric.jar` — Fabric mod (client + server)
- `paper/build/libs/lod-server-support-paper.jar` — Paper plugin (server only, shadow JAR; serves Paper and Purpur — `folia-supported` is declared again on the 26.2 line, so Folia loads this jar)
- `neoforge/build/libs/lod-server-support-neoforge.jar` — NeoForge mod (client + server, shadow JAR: Paper-style shading for common+zstd, but **sqlite-jdbc rides as a STOCK jarJar nested library** (`META-INF/jarjar/`, generated metadata — neoforge-jarjar-sqlite-plan.md): flat shading collided at FML module resolution with any mod nesting the same artifact (the community Voxy port); `check_store_natives_neoforge` pins the shape. The plain jar task is DISABLED (no `-slim` artifact can match release globs). The VSS variant (TOML+brand rewrite, class-set byte-identical) joins `vssJars`.
- `fabric/build/libs/voxy-server-side-fabric*.jar` / `paper/.../voxy-server-side-paper*.jar` — the **Voxy Server Side** branded pair (`./gradlew vssJars` builds all three loaders' variants). Near-byte-copies of the LSS jars rewriting **display + LOCAL surfaces only, never wire**: mod/plugin display metadata + icons, the command key (`lsslod`→`vsslod`) + admin node (`vss.admin`), `lss-brand.properties`, brand-preferred config filenames (`vss-client-config.json`/`vss-server-config.json`) and brand-routed local paths (`<world>/vss-lod`, `.vss/cache`) — ALL with cross-brand adoption so a same-folder jar swap keeps config/store/cache. The far-player privacy node stays dual-declared (`lss.farplayers.hidden` AND `vss.farplayers.hidden`, both enforced — Bukkit resolves an undeclared node to the op default). What stays verbatim (the **wire-compatibility contract** — LSS client ↔ VSS server and vice versa): mod id `lss`, package/class names, the entire `lss:*` wire surface. `release_check.py` pins the pair (nested-common SHA equality on Fabric, class-digest identity on NeoForge, TOML/plugin.yml line-diffs). Runtime branding flows through `common/Brand` (defaults to LSS when the properties resource is absent). **VSS publishes to Modrinth `84zcagOb` LOCALLY, never via release.yml** — the process (token in `~/.bot.env` `MODRINTH_PAT`, bare version numbers, no changelog, unversioned filenames, per-line loader/game-version splits) is recorded in project memory `vss-publish-discipline`.

CI builds (env `CI=true`) name the jars `lod-server-support-<platform>-<mod_version>+<minecraft_version>.jar` (e.g. `lod-server-support-fabric-0.6.0+26.2.jar`); the release workflow feeds `mod_version` from the tag. Local dev builds keep the stable unversioned names.

## Test Commands

```bash
./gradlew :fabric:test -x runGameTest                        # Tier 1: JUnit unit tests only (~40s)
./gradlew :fabric:runGameTest                                # Tier 2: server gametests (starts dedicated server, ~13s)
# NOTE: `:fabric:test` alone runs Tier 1 ONLY — runGameTest hangs off check/build, not test.
# For Tier 1 + 2 run both commands above, or `:fabric:build`.
# Tier 3 (runClientGameTest) is CUT on this line (pre-authorized-cuts.md) — the task
# is not registered (enableClientGameTests = false) and `-x runClientGameTest` FAILS.
./gradlew :fabric:build                                      # Full build + Tier 1 + 2 tests
./gradlew :paper:test                                       # Paper JUnit tests (wire parity, NBT serialization, config)
```

Most tests live in the Fabric module. Paper has its own Tier 1 JUnit suite (~330 tests, run by CI): Fabric/Paper wire parity, NBT serialization (golden cross-module corpus + transcode fuzz), the platform twins of the filter/manager/service/broadcaster suites via injection seams, plugin enable/dispatch/handshake glue, plugin.yml + release-workflow contracts (per-line flavors hand-mirrored on this branch; the R2-5 line.env derivations are main-only), the Folia seams (lifecycle mailbox, regionized probe scheduling incl. the `republishHeldBatch` CAS/offer-generation guards, `FoliaWiringContractTest`'s constant-pool scan), exporter schema parity, and command output. Paper runtime behavior is validated live by the soak harness on the Paper platform (`SOAK_PLATFORM=paper` — seven scenarios against a real server, store-second-join included since the 2026-08-27 Folia review; no 1.21.1 Folia exists, so the `folia` soak platform is not runnable on this line).

### Test Architecture

- **Tier 1** (`fabric/src/test/java/dev/vox/lss/`, ~1250 tests): JUnit 5 via `fabric-loader-junit`. Covers every subsystem — the LOD store family, position packing, bandwidth limiter, config validation, column caches (timestamp tiles + miss memo, column cache store), the want-set pipeline (scanner/tracker/state-map/request-manager incl. the adaptive-cadence and backpressure pins), payload codecs + wire parity + the NBT transcode goldens, the palette memo, the router ladder (duplicate-serve grace, probe suppression, miss-memo rung, generation pacing/spread gates, drain rotation), OffThreadProcessor lifecycle + stale guards, the compat rungs (v16/v18/v19, Via guard), x-ray masking, the reflective bridges (Voxy, Moonrise, AntiXray — against real-package-name stubs), far players (wire/tracker/motion/broadcast), the transfer-rate pair + send pacing + transport yield, the move-desync tracer, and the contract tests (release workflow, plugin.yml, toolchain, entrypoint listing, xplat purity, save hook, LAN hook, workflow contracts — hand-mirrored line facts on this branch). The suite class names are discoverable; each test class's javadoc records its pins and the decisions they protect — READ THE TEST before "fixing" behavior it pins (many findings that look like bugs are deliberate, test-pinned tradeoffs; see memory `review-findings-vs-pinned-decisions`).
- **Tier 2** (`fabric/src/gametest/java/dev/vox/lss/test/`): 71 Fabric server gametests across 8 classes — service lifecycle, serializer byte-parity (disk vs live, incl. masked), generation lifecycle, commands (incl. the `set` apply-clamp-restore), two-player dedup/fairness/fan-out, corrupt-region containment, move-trace hooks, plus the crafted-frame handshake pins. Every class MUST be listed in the `fabric-gametest` entrypoint of `fabric/src/gametest/resources/fabric.mod.json` (compiles-but-never-runs otherwise; `GameTestEntrypointContractTest` derives the listing from source both ways). Seeding goes through `GameTestSeeding` — the mailbox is latest-wins, so multi-position seeds must be ONE batch. Mock-player gotchas: `primeListenerForMoves` before movement packets; the stock mock hard-codes CREATIVE (hand-roll a survival player for survival-gated paths). Gametest JVMs run with `-Dlss.test.integratedServer=true`; timing tests must never race the product's WALL-clock windows with tick-denominated waits (an unthrottled gametest server ticks at ~0.2-0.4 ms — see the fan-out entry in the flake catalog).
- **Tier 3** (`fabric/src/gametest/java/dev/vox/lss/test/LSSClientGameTests.java`): End-to-end client-server flow test. Validates handshake, session config, spiral scanning, chunk section receipt, decoded section content asserted at a registered `LSSApi` consumer (flat-world block layers, off-render-thread dispatch), and the ingest-failure recovery loop (a consumer rejecting a column — by `LSSApi.reportIngestFailure` or by throwing — gets it re-served end-to-end). Fabric Loom handles headless rendering automatically.

The system property `-Dlss.test.integratedServer=true` (set in fabric/build.gradle for gametest JVMs) allows `RequestProcessingService` to activate on integrated servers during testing.

### Known Test Flakes & Environmental Failures

**This section is the canonical home for known CI test flakes and environment-specific test failures — keep it up to date, and record all similar findings here.** When a test failure is diagnosed as a flake or an environment quirk (not a code regression), add an entry here so the next contributor/session **re-runs rather than chases a phantom regression**. Each entry gives the test, the symptom, why it's benign, and the action. (These are also mirrored in the assistant's project memory, but this file is the in-repo source of truth.)

Since 2026-07 `build.yml` contains the flakes automatically: docs-only changes (`**.md`, `docs/**`) skip the workflow entirely, the Tier 2 and Tier 3 gametest steps **retry once** on failure (each retry emits a `::warning::` annotation in the run summary — a retried-then-green run means a flake fired, not that nothing happened), and the first attempt's logs/crash-reports are uploaded as `gametest-flake-evidence-*` artifacts (failures upload `gametest-failure-evidence-*`, retention 30 days — GitHub's raw job logs for this repo expire within days, the artifacts are the durable record). A job that is red even after its retry is very unlikely to be one of the entries below — diagnose it from the artifacts before re-running.

- **`RegionFaultGameTests` (Tier 2) — HARDENED 2026-08-14 (V-1/T3b; was environmental on some dev boxes).** The corrupt-region test used to fail (`errors=1 ... not_found=0 on tick 1202`) on constrained/WSL2 machines regardless of load, because it pinned vanilla's not-found LABEL. The assertion now accepts either label (`not_found + errors == 1` — the containment + pool-survival contract is the pin; the classification is vanilla-internal and genuinely differs per MC line AND per box state), which absorbs that environmental shape. **A failure of this test is now a real containment/pool regression — diagnose, don't re-run.**
- **`IncomingRequestRouterTest.loadedProbeBeatsAFreshMissMemo` (Tier 1) — polling-deadline flake, hardened 2026-08-03.** Failed once on a GitHub runner at `IncomingRequestRouterTest.java:188`, which is the `fail("timed out waiting for")` inside `waitFor`, NOT a logic assertion. Content-identical code passed the same commit's PR run, the release run (which builds Tier 1 too) and 5/5 locally — three passes to one failure. The helpers' deadline was 5 s for work that takes milliseconds, so it was measuring runner contention; raised to 30 s (`POLL_DEADLINE_NANOS`), which costs nothing on the passing path. **Note `build.yml` retries the GAMETEST tiers once but NOT Tier 1**, so a timing flake in a JUnit test fails the run outright with no second attempt — worth remembering when a green-everywhere-else commit reds only on the main Build. A failure at a DIFFERENT assertion in this test is a real miss-memo/probe-ordering regression, not this entry. **Same family, second suite (2026-08-13):** `OffThreadProcessorDiskResultTest.generationPermanentFailureClearsTheMemo` fired the identical waitFor-timeout shape during a full-T1 run that overlapped a running soak server (self-inflicted box contention — soaks get the box to themselves, and that includes not running T1 beside them); its helpers carried the same 5 s deadline and got the same 30 s hardening. A timeout-shaped failure in ANY suite using these polling helpers is this entry; a logic-assertion failure is real. **Third sighting (2026-08-21, 1.21.11 v0.12.0 port CI):** `RegionSummaryServiceTest.aDeclinedSendIsNotCountedAsAFrame` timed out at `awaitAssembly` (line 414, the helper's fail) at the ALREADY-hardened 30 s deadline on a shared runner — the frame assembler is a MIN_PRIORITY daemon, the starvation-prone shape; identical bytes green same-hour on main/26.1/1.21.1 CI and locally. Re-run the job; if a SECOND CI timeout appears in this suite, raise its deadline to 60 s rather than re-running again.
- **`LSSClientGameTests` superflat section count (Tier 3) — hardened in v0.6.1 (was a serialization-window flake).** `assertDecodedFlatWorldContent` used to fail with *"superflat columns must decode to exactly the one non-air section, got 24"* when a column was serialized during unsettled spawn-prep (its air sections not yet culled by `SectionSerializer`'s `hasOnlyAir` / block-light filter) and that stale delivery lingered in `recorder.snapshot()`. It flaked on the v0.6.0 and v0.6.1 PR CI (twice in a row on the latter — re-running did not clear it). The assertion now checks the content section (sectionY -4) and requires any extra sections to be **air-only** (sampled block states — the extras' blocks are genuinely air, only cull metadata/light differed), tolerating the transient un-culled sections while still catching real decode bugs. If a *new* "expected exactly one content section" or "must be air-only" variant appears, treat it as a real decode regression, not a flake. (`release.yml` runs `-x runClientGameTest`, so Tier 3 never gated the publish regardless.)
- **`TwoPlayerGameTests` fan-out (Tier 2) — hardened 2026-07 (was a one-shot re-ask race, twice).** `editedColumnPropagatesToBothHoldersThroughBroadcastFanout` failed as "A=3 B=1 on tick 1202" when a step-2 re-ask routed before the broadcaster's dirty-clear mailbox event applied on the processing thread and terminally resolved up_to_date off the stale done-bit. Step 1's version of the same bug was fixed in PR #19; step 2 flaked again 2026-07-10 and now retries its ts=1L re-asks under the same exactly-once guards (mirroring the real client's rescan retry, without weakening the pin — an undelivered clear still freezes the counts and fails at maxTicks). If this test fails again, treat it as a real fan-out regression, not a re-run.
- **`TwoPlayerGameTests` fan-out on the 1.21.1 line — LOCAL-box flake, elevated rate (sighted 2026-08-15, v0.11.0 release pre-flight).** `editedColumnPropagatesToBothHoldersThroughBroadcastFanout` failed 4 of 8 local Tier 2 runs on WSL2 (always the same shape: "waiting for A's post-edit re-serve" at maxTicks — the hardened one-shot-race family above) while the SAME commits passed the suite first-attempt on all of that day's CI runs (no retry annotations). Flag combinations were ruled out by A/B (CI=true / -Pmod_version / both / neither all produced both outcomes). The mainline entry above says a recurrence is a real regression — that judgement stands for CI failures and for the 26.x lines; on 1.21.1 (different engine timing, fresh stage-G port) a LOCAL red with this exact waiting-timeout shape is this entry: re-run isolated (`./gradlew :fabric:runGameTest`), and only a CI red or a persistent isolated-run red is a real fan-out regression. DIAGNOSED + FIXED same day: the test's tick-denominated timeout raced the product's WALL-denominated windows — an unthrottled gametest server ticks at ~0.2-0.4 ms, so all 1200 timeout ticks fit inside the 500 ms duplicate-serve grace, where every ts<=0 retry is the DESIGNED silent absorption (`grace_skipped`, done-bit kept) and the test wedges; CI's slower ticks outlive the window and heal, which is why it never fired there. Fix (all four trees): step 1 waits out grace + probe-suppress (~2.2 s) via a 50 ms sleep per waiting tick (wall-denominated, tick-rate-independent), then the retry exercises the honest probe re-resolution the step pins. Validated 8/8 on the box that redded 4/8. The decisive instrument was one diagnostic line in the assert (sent=1 reqs=1199: every retry consumed, none served). A future failure of this test is NOT this entry — the wall race is closed.
- **`TwoPlayerGameTests.overlappingRequestsFromTwoPlayersDedupeDiskReadsAndBothConverge` — the SAME tick-vs-wall race family, FIXED 2026-08-21 (was one sighting on the 1.21.1 v0.12.0 port gates).** Failed once at maxTicks with "waiting for BOTH players to receive all three columns (fan-out delivery), A=0 B=0" under heavy box contention (eight concurrent review agents), and the full Tier 2 suite passed on one isolated re-run minutes later (n=1 for this test — the suite ran once, not 72 trials). Root cause was the family mechanism, not the port: the test's wait sits on three REAL disk reads + fan-out (wall-bound work) while an unthrottled gametest server burns all 1200 timeout ticks in ~0.3-0.5 s of wall time. Fix (all four trees): the convergence wait sleeps >=50 ms per waiting tick — the same wall-denomination as the fan-out step's fix above — so the ceiling is tick-rate-independent and the passing path pays only the handful of ticks the reads take. A future failure of this test is a real dedup/fan-out regression, not this entry.
- **`warm-rejoin-summary` (Fabric) run-2 red at 649 validated / 11 clean — cause UNRESOLVED, one red one pass (2026-08-21, v0.12.0 port smoke).** One red (validated=649, clean=11, THREE NEVER_CLEAN tiles) immediately after an auto-run fresh-backfill; one pass (985/13) minutes later. The judgement audit measured IDENTICAL system load for both runs (~0.25 sys-util — 'isolation' did not actually distinguish them) and both sat above the 26.x baselines' ~0.15, so neither 'engine save timing' nor load is established; the line's own PAPER wrs and sibling 1.21.11 both land at 26.x-typical 1313/15, so a line-wide attribution is contradicted. What IS established: the doubt is whole-tile quantized in the NEVER_CLEAN bucket (not a uniform depression), run-1 dispositions are identical to every other line, and tiles_no_region stays exactly 9 — the resolver/sweep-coverage failure shapes are excluded. Discipline: a red here is judged by the CHECKER, never by prose — re-run; a checker-green re-run closes it. On a SECOND red: capture per-tile NEVER_CLEAN attribution and a ~0.15-load baseline run before touching anything; if the line genuinely runs lower, the fix is a derived platform-aware floor in check_soak.py (the call-A pattern: derive, change the check, pin the boundary in selftest) — never a doc that admits sub-floor values.
  Reframed after the D-prep last-line audit: this is NOT plain variance — BOTH Fabric samples on this line sit below EVERY sibling sample (red 11/649, pass 13/985, vs 15/1313-1377 on 26.1, 1.21.11, and this line's own Paper run), n=2. A future first red here is not noise to wave off — apply the escalation below on the SECOND red of any run pair. Same shape, second scenario (2026-08-21): `stamp-heal-rejoin` first run redded residue-7 (stale=6/unknown=1, validated=473), repeat run landed EXACTLY the 26.x cluster (1313/clean=12/residue-4, digit-identical to main's) — one red one pass, n=2, same discipline applies. The `dirty-while-offline-summary` false-clean canary PASSES on this line (probe leg race-immune), so the stale-residue variance is confirmed safe-direction.
- **`GenerationLifecycleGameTests` (Tier 2) — cold-gen timing, budgets raised 2026-07.** The four tests that wait on real cold chunk generation (piggybacked, same-tick re-request, End-void sentinel, generation-serve seed) ran at 600 maxTicks, which first-run generation can exceed on starved runners; all now run at 1200 (FP-033 needed the same bump earlier). A timeout beyond 1200 ticks is worth investigating, not re-running.
- **Tier 3 silent client-JVM hang → 35-min step timeout (3 sightings).** The Client GameTests job fails with NO assertion, NO crash report: the client log simply stops (typically right after an `LSS-CacheIO` save line) and GitHub kills the step, while byte-identical trees pass Tier 3 elsewhere the same day. Environment-class headless freeze on a shared runner. Decisive signature: silent log stop + EMPTY crash-reports dir in the artifact (a real regression fails a test or crashes; it does not go mute mid-teardown). Action: `gh run rerun <id> --failed` after confirming the signature. `release.yml` skips Tier 3, so this never blocks a publish.
- **Vanilla `TicketStorage.purgeStaleTickets` NPE (Tier 3) — upstream MC 26.2 fragility.** 26.2's `removeTicketIf` fires callbacks mid-iteration of the fastutil ticket map; Tier 3's ticket churn can trip it. Investigated: every LSS ticket call is main-thread-only and unaffected — not fixable in LSS; the CI retry contains it. If it recurs, confirm the crash stack is still vanilla-only before suspecting LSS.
- **Soak checker (`check_soak.py`) — benign timing / light-settle drift on constrained boxes.** Occasional dirty-resave / quiescence drift is tolerated by design (the checker carries settle tolerances); treat as a re-run, not a rewrite.
  - **`paper-store-unfired-event` premise red — ambient dirty marks in `base-paper`.** Two diagnosed mechanisms: world-gen pack animals grazing (structurally fixed: t+2 entity kill + windowed premise) and the residual grass-regrowth drip on the bare-dirt patches the original grazing left (~1/min for the world's lifetime; a drip inside the edit window is this red). Action: re-run; a base reroll would re-open the first mechanism, so live with the drip. Diagnostic lever: `SOAK_DIRTY_TRACE=true` logs every mark-driving Bukkit event — reach for it FIRST on any Paper ambient-dirty mystery.
  - **`A7` "disk.errors increased" on a memo-enabled fresh-backfill — the miss memo's generation acceleration starving background reads (A/B-diagnosed).** Decisive signature: `disk.errors` increased, `grep -c "Failed to read chunk" server.log` == 0 (all timeouts — they log as throttled WARNs, so count via the counter, not lines), and **A5 exact**. It is the documented memo tradeoff (miss-memo-design.md), not a regression; verify with the `missMemoTtlSeconds: 0` kill-switch A/B if in doubt. If A5 ALSO fires it is a different problem. **The second look that settles a repeat red is a same-day BRANCH A/B on the same box, not another re-run** — box IO day-state has exonerated the branch three times (base branch redding harder minutes later). The NBT transcode and the SOAK_DIALECT=19 lever are mild intensifiers of the same mechanism on a degraded box (kill-switch A/B settles those too; a transcoder byte bug would fire A1-A6, never A7). Post-stage-B note: the storm signature is "+K" (`maxConcurrentDiskReads`), not "+pool", on stores-armed configs.
  - **`store-save-storm` law A2 red in MIRRORED adjacent-window pairs — the SAME WSL2 clock-step artifact, cross-JVM flavor (diagnosed 2026-08-24 after a 2nd red on an idle box).** Shape: A2 `columns_sent != received_columns` (+ the bytes twin) in two ADJACENT windows whose values are mirror images — the client "receives before the server sends" in window N, the server "sends unreceived" in window N+1 — and the merged pair conserves EXACTLY (columns and bytes both). Mechanism (measured): the box wall clock stepped BACKWARD ~2.5 s between the server's snapshot sample and the client's, so the stable ~+2.2 s client-after-server row phase inverted for one boundary (the client row stamped BEFORE the server row it pairs with) and the checker attributed one send burst to the wrong window on one side only. Decisive three-part signature: (1) violations come in adjacent mirrored pairs that sum to zero imbalance; (2) the client-vs-server snapshot phase jumps backward by the step size at exactly the violating boundary, then restores; (3) the server's own d_wall/d_tick shows one compressed wall interval (e.g. 2518 ms) at a constant 100-tick cadence — the tick clock is the step-immune referee, same as the B2 entry above. Action: artifact — accept-with-record (a WSL restart clears the steps). A REAL A2 loss leaves a residual imbalance after merging the adjacent windows — never explain THAT away with this entry. Not scenario-specific: any two-JVM soak can wear this shape; store-save-storm just runs the most send-burst boundaries.
  - **`bandwidth-throttle` / `dirty-during-backfill` law B2 red — WSL2 clock-step artifact (2026-08-21; tick-clock cross-check is the decisive test).** Shape: B2 per-window violations only, each on a ~2.7-2.9 s wallMs window recurring every ~30 s all run. Mechanism (measured wall-vs-/proc/uptime): the box wall clock advances at the correct rate and is STEP-CORRECTED BACKWARD ~2.2 s every ~30 s (net ~-7%; no forward drift) — any window bracketing a step under-counts wall time and B2's 1.3 headroom absorbs 30% while the step eats ~44%. **Decisive test — recompute the window against the snapshot's `tick` field** (monotonic, step-immune): an artifact window shows tick_dt ≈ 5.00 s vs wall_dt ≈ 2.7-2.9 s with traffic ≤1.0× cap on the tick clock; a REAL pacing regression reds at tick_dt ≈ wall_dt ≈ 5.0. The neighbor-quantum clause is the second discriminator and it is NOT decoration: a real 1.8× burst confined to one short window is numerically identical to the artifact, so require the violating window's byte delta to EQUAL its 5.0 s neighbors' steady quantum. The whole-run cumulative 5% bound is NOT a discriminator here — at these scenarios' ~22% duty cycle it stays green under regressions up to ~4.5×. Action: tick-clock recompute → artifact = accept-with-record (a WSL restart clears the steps); real = stop. Do not widen B2's headroom for this.
  - **`A7` + `law-coverage` together — environmental read-timeout storm on a loaded box.** Heavy box IO makes LOD reads exceed the 10 s timeout (`disk.errors` up, coverage floor tripped). Decisive check: `disk.errors` increased while `grep -c "Failed to read chunk" server.log` == 0. If A5 ALSO fires it is a DIFFERENT problem (the timeout-triage path changed). Environmental → re-run on an idle box — and "idle" includes no leftover test servers (`pgrep -af 'lss.multitest\|test-server'` first; six forgotten JVMs once produced two consecutive reds).
  - **`warm-rejoin-summary` / `dirty-while-offline-summary` suppression-pin red with HIGH validated — a late summary frame, re-run.** The run-2 `requested_total >= columns.known` pin assumes the frame beats the first scans; on a loaded box the frame (or the cache load it buffers behind) can land after several 1 Hz declares already re-asked the disc, and the revocation round adds more. Decisive signature: `summary.columns_validated >= 800` AND `tiles_clean` near its floor while ONLY the suppression/re-declare legs red — validation worked, it was just late. Action: re-run on an idle box. `columns_validated == 0` (or a `tiles_no_region`/honesty-leg red) is NOT this entry — that is a real summary regression.
  - **Summary-scenario tile legs sit ~1 tile from their floors — a single-tile boundary red is a re-run first, a re-derive second.** The ceiling/floor pairs are exactly tight against the 25-tile window geometry (see `check_stamp_heal_rejoin`'s docstring), and reference runs land within one tile of three legs. Action: one re-run; a second identical boundary red means re-derive the PAIR against the observed geometry (the `tiles_no_region >= 5` premise leg localizes which half moved — this line's runs read exactly 9) — never widen one leg in isolation. This is about single-tile BOUNDARY reds; the line's catalogued "wrs runs LOW" entry is a different signature.
  - **`dimension-trip` "segment does not end verified-quiescent" — snapshot phase-drift artifact.** `find_quiescent` files QPoints only at the client row nearest a server snapshot; when the two 5 s cadences drift out of phase, the segment's final row is STRUCTURALLY unable to be marked quiescent however idle the system is. Decisive check: is the failing row index ever `min(|client.wallMs - server.wallMs|)` for any server snapshot? If never, artifact — re-run.
  - **Laws A5/A1 — documented latent false positives, never yet seen live.** A5: permanent generation failures double-count the RHS while the client is connected; near-boundary ticket drops under-count it — an A5 red today is a code regression FIRST, but check whether the imbalance equals a small count of gen errors/boundary drops before chasing conservation (also noted in `law_A5`'s comment). A1: three lost-send shapes (throwing client transport, server batch-frame send failure, post-/reload unregistered-window declarations) — an A1 red whose imbalance equals one batch or frame is one of these; full list in `law_A1`'s docstring. Related observability hole: a dirty NOTICE whose send fails after its clear applied is invisible to every counter.

## Background-task discipline (assistant workflow)

**Always put a time-based fallback on background tasks** (user rule, 2026-08-14 —
added after a readiness-poller wedged the session): every background command that
waits on a condition MUST carry its own deadline — wrap the wait in `timeout <s>`
or bound the loop (`for i in $(seq 1 N); do ... sleep S; done` + a final
TIMED-OUT line) so the task always EXITS and fires its completion notification,
even when the condition can never become true. Never launch an unbounded
`until`/`while true` poller: if the watched thing wedges (or the predicate is
subtly wrong — the sighting was a log-freshness conjunct that became permanently
false once an idle server stopped writing), the task wedges, and the session
wedges waiting on it. Long-running SERVICES (test servers, proxies) are exempt
from the deadline but their READINESS checks are not.

## Local Test Servers (manual play)

```bash
./test-server.sh              # set up + run all three: Fabric :25564, Paper :25566, Folia :25567
                              # (:25565 stays free for the soak/benchmark harness — a test server
                              #  there is indistinguishable in the server list and an accidental
                              #  join contaminates a running soak)
./test-server.sh run-folia    # one platform only (also run-fabric, run-paper)
./test-server.sh run-neoforge # NeoForge server :25569 (stage-N best-effort loader; version
                              #  pinned to gradle.properties neoforge_version, installer cached
                              #  under test-server/neoforge/. Fabric/vanilla clients JOIN FINE —
                              #  LSS channels are .optional() and add no registry content)
./test-server.sh run-fabric-no-c2me  # Fabric with c2me*.jar parked as .disabled (A/B vs C2ME; run-fabric re-enables)
./test-server.sh run-fabric-store    # run-fabric with the LOD store ON (lodStore=on) + the background
                                     # backfill (lodStoreBackfill=true auto-walks regions) — warm rejoins
./test-server.sh run-paper-store     # run-paper with the LOD store ON; store DB at <world>/lss-lod/
                                     # (no backfill on Paper — Fabric-only; warms from serves)
./test-server.sh update       # rebuild + reinstall the LSS jars into all three (restart to apply)
./test-server.sh clean        # delete test-server/
# The staged lss-server-config.json now carries the SHIPPED DEFAULTS and nothing else —
# it used to hand-write nine tuned values (distance 64, 8 MiB/player, diskReaderThreads 8,
# sendQueueLimitPerPlayer 9600, generation caps 40/40) that predated the config review, so
# the rig tested a configuration no player runs. Only genuinely rig-specific keys are
# written; everything else falls through to the mod's defaults.
# Knobs (the staging REWRITES the config each run, so hand-edits don't survive — these are
# the supported way): LSS_LODSTORE=off|on|full (default on — matching a fresh install; on==full),
# LSS_LODSTORE_BACKFILL=true|false
# (default true) — both follow the shipped defaults; LSS_LODSTORE_BACKFILL_CPS (unset =
# server default 500); LSS_MOVE_TRACE=1 stages config/lss-move-trace.enable on the Fabric
# server (the tracer's vanilla-rung environment; rows land in
# test-server/fabric/logs/lss-move-trace.jsonl — default 0, marker removed when unset);
# LSS_LOD_DISTANCE (unset = shipped default 512, restored 2026-08-13 after stage A's brief
# 300 — the rig used to hardcode 64, and 512 is ~64x that area, so set 64/96
# on a small box or when running all three servers); LSS_VIA=1 stages ViaFabric+ViaBackwards (Fabric) / ViaVersion+ViaBackwards
# (Paper) via Modrinth-API resolution — the C5 Via mismatch guard's live rig (run-fabric-via
# = the convenience case; old-MC clients live in the lss-multi-test Prism profiles), with
# LSS_VIA_GUARD=0 as the guard's kill-switch A/B (writes enableViaMismatchGuard=false into
# the staged config — hand-edits get clobbered, use the knob; default 1). Every non-via run
# parks the Via jars so the baseline stays Via-free.
# run-fabric-store / run-paper-store FORCE the store on (immune to
# LSS_LODSTORE=off) and otherwise match plain run-fabric / run-paper now that on is default.
```

Downloads real server jars (Fabric launcher; Paper/Folia latest stable via fill.papermc.io;
NeoForge via the maven.neoforged.net installer), installs the built LSS jars (Folia gets
the same Paper plugin jar; NeoForge gets the shadowJar), and stages offline-mode config
under `test-server/<platform>/`. For joining with a real client and
eyeballing LOD behavior — the automated gates are the test tiers and the soak harness above.

### The SoakPlayer dummy rig (far-player live gates)

`./gradlew :fabric:runSoakClient -Psoak.server=localhost:25564` launches the headless
soak client as a **far-player TARGET dummy**: its `-Dlss.soak=true` property-gates the
far-player capability bit OFF (it never becomes a viewer and never shifts baselines),
but its player entity is an ordinary target the real GUI clients can see as a proxy.
Stable name `SoakPlayer` (pinned via `--username` — rejoins keep identity/playerdata).
`-Psoak.server` defaults to `localhost:25565` (the harness port), so soak.sh behavior
is unchanged; point it at the test-server.sh Fabric server (`:25564`) for live-gate
sessions. The E2/E3 live gates use TWO real GUI clients + this dummy (elytra
extrapolation, tracking-boundary handoff, vanish/spectator via RCON, the mounted
scenario) — user-scheduled, tracked on the manual-verification checklist.

## Architecture

### Entry Points

- **Fabric:** `LSSMod` — server initializer: registers payloads, starts `RequestProcessingService` on dedicated servers
- **Fabric:** `LSSClient` — client initializer: sets up client networking, commands, mod compatibility
- **Paper:** `LSSPaperPlugin` — plugin entry point: registers Plugin Messaging channels, starts `PaperRequestProcessingService`

### Networking Protocol (wire v20, the v17 design line — declarative want-set, server-owned generation)

Batch model with 6 core payload types plus the `lss:client_info` sidecar, the three
`lss:far_player_*` channels (E1 — raw `FarPlayerWire` byte[] bodies, version-neutral, no
protocol bump), and the two `lss:region_summary*` channels (region-summary-sync-plan.md
§4 — raw `RegionSummaryWire` byte[] bodies, same discipline: version byte, zigzag-delta
stamp windows, hard decode caps at `MAX_SUMMARY_TILE_RADIUS` 65 + the
`MAX_SUMMARY_TILE_ABS` 2M center-domain bound in both compact ctors with long tile
arithmetic, NO capability bit and NO canSend — the fire-and-forget REQUEST is the
capability declaration; explicit dimension echoed both ways + drop-on-mismatch is the
whole anti-stale binding), and the `lss:col_stamps` S2C channel (stamped up_to_date —
stamped-up-to-date-plan.md: raw `ColumnStampsWire` byte[] verification-stamp frames
sent ONLY to sessions that sent a summary request this session, semantic stamp bounds
at decode; both WireParityTests' channel census pins the full 13-channel surface).
Fabric uses `LSSNetworking` with Fabric `StreamCodec`;
Paper uses `PaperPayloadHandler` with raw `FriendlyByteBuf` encoding. Both produce identical wire format.

**C2S:** `HandshakeC2SPayload` (capabilities bitmask) → `BatchChunkRequestC2SPayload` (batch of packed position + clientTimestamp pairs — the client's **complete want-set**, not an increment; ts `>0` = resync "send if newer", `<=0` = "I have nothing" — there is no generate-request shape, the client never classifies)

**S2C:** `SessionConfigS2CPayload` (4 fields: protocolVersion/enabled/LOD distance/generation toggle — the concurrency caps are server-internal and left the wire) → `VoxelColumnS2CPayload` (coords + dimension + columnTimestamp + one-byte serve-source tag `COLUMN_SOURCE_*` in_memory/disk/generation/store — diagnostic attribution for the client trace; unknown values pass through verbatim; added in protocol 18, still the v17 design line — + one-byte codec tag `COLUMN_CODEC_RAW/ZSTD` added in protocol 19 (zstd-1 column frames for capability sessions; stripped at egress for v18-compat sessions) + MC-native sections) → `BatchResponseS2CPayload` (batch of responseType + packed position pairs — up-to-date and not-generated) → `DirtyColumnsS2CPayload` (server-pushed change notifications)

**The want-set model.** Each scan the client declares *every position it still wants*, closest-first, in one batch; the server **replaces** that player's backlog with it. The scan cadence is **adaptive** (docs/planning/adaptive-scan-cadence-design.md): a 1 Hz fallback (also the self-heal cadence) plus a completion-triggered fast re-scan up to 4 Hz when the last batch is ≥95% answered AND the predicted walk cost fits `FAST_RESCAN_MAX_WALK_COST` (a PREDICTION, deliberately not a memory of the last walk — and in the MOVEMENT WINDOW (a crossing with no walk since) deliberately the bare from-zero cost even though scan prefix RETENTION (docs/planning/scanner-reopened-rings-plan.md, 2026-08-18: `recenter(d)` decrements the prefix and reopens a ring bitset instead of zeroing — the render-thread-hitch fix; kill switch `enableScanPrefixRetention`) keeps the walk itself cheap, so under sustained movement the fast path still covers only rings 0-127 and a moving client returns to 1 Hz past ring 128; a PARTIAL, accepted fix to the elytra wall — do not claim "4 Hz while flying" unqualified, and raising the constant only moves the cliff; full analysis in docs/planning/elytra-chunk-wall-investigation-2026-08-01.md). Pressure gates (decode queue + consumer ingest backlog below ¼ of halt) and v16 sessions exclude the fast path. **Since 2026-08-24 the walk itself is HYBRID by default** (region-scan-plan.md + hybrid-scan-plan.md; client `enableRegionScan`): rings out to `HYBRID_NEAR_RADIUS` (64) walk in the legacy closest-first ring order (clean concentric fill where the player is looking), then the remainder switches to a spiral of 32×32-chunk regions, each emitting its RESIDUE (the region minus the near square, clipped to the lod square) ring-ascending — the declared set is a complete prefix of the combined order plus at most one partial tail, and the server-visible FAR working set collapses from ~95 scattered regions to 1-2 active ones (the far-radius Xaero-bridge drop fix, plus .mca/store/tscache locality for free via the router's declaration-order drain); at lod ≤ 64 the walk IS the legacy order exactly (the FRESH from-scratch enumeration — the legacy arm's prefix-retention shortcuts aside), and the legacy ring-major walk remains the fallback arm, selected at manager construction. Consequences that are load-bearing, not incidental:
- **Re-declaration is the single self-healing mechanism.** There is no client drip-feed, no in-flight timeout sweep, and no rate-limit backoff — all deleted. The server may silently drop any not-yet-admitted ask (backlog replace, mailbox overwrite, Folia hold CAS loss, residual disk saturation); the next declaration (≤1 s away — the fallback bounds it) heals it. Silent drops never remove positions from the client's awaiting set, so churn-dominated phases (drops above the 5% completion threshold) keep outstanding high and hold the cadence at 1 Hz — the profile the soak churn ceilings were measured against; a sub-5% drop rate legitimately runs fast. Every such drop is counted (`service.superseded` / `service.range_filtered`).
- **The scanner must NOT suppress awaited positions at send.** An awaited position is an ordinary unsatisfied want-set member and blocks its own ring's confirmation until data lands. Re-introducing send-time suppression re-opens a 10s-class stall against the server's silent drops.
- **`RESPONSE_UP_TO_DATE` / `RESPONSE_NOT_GENERATED` are the only things that empty the want-set.** They are what makes `ColumnStateMap.classify` return SATISFIED. Never delete them. `NOT_GENERATED` is **permanent for the session**: the client adds the position to `sessionSatisfied` (no 0-stamp — legacy cache 0-stamps re-declare as -1) and never re-asks, even on movement; the ONLY revival is a dirty broadcast. It fires ONLY on permanent unservability (generation disabled, or a hard generation failure) — **never for transient pressure**: a full gen slot, gen-service capacity, a generation timeout, a submit no-op, or a ghost delivery all drop silently (counted `superseded`, disk-miss flavors also `miss_dropped`) and heal by re-declaration. On a gen-enabled server it never fires at all (pinned by the `generation-capacity-stress` soak: `not_generated == 0`).
- **At convergence the client sends NOTHING** — not an empty heartbeat. Soak quiescence depends on `service.requests_received` going still. The one exception is a single **edge-triggered empty batch** when the client crosses into backpressure halt — its decode queue mostly full (count or bytes), OR a consumer-reported ingest backlog at `INGEST_BACKLOG_HALT_SECTIONS` (issue #71: Voxy's unbounded ingest queue is invisible to the decode-queue signal; consumers report depth via `VoxelColumnConsumer.pendingIngestBacklog`, docs/planning/ingest-backpressure-design.md): it clears the server backlog so no work is done for a client that cannot ingest it.
- **Byte 0 is retired and reserved.** It was `RESPONSE_RATE_LIMITED` through v16; UP_TO_DATE(1)/NOT_GENERATED(2) were deliberately *not* renumbered, and the codec ships unknown types unaltered (pinned) so byte 0 stays forward-safe.
- **Legacy shims, both directions, all default true.** Server-side rungs through the shared `WireDialect` gate: **v19** (v0.9.x clients — memoized enqueue-time translation), **v18** (v0.7.x-v0.8.x — membership-only, the egress strips exactly the codec byte; identity marked before registerPlayer, survives dimension change, dropped at disconnect/quit-race Remove), **v16** (v0.4.x-v0.6.x — 6-field SessionConfig echo, synthetic want-set ingress at 1 Hz, source-byte strip; docs/planning/v16-compat-design.md). Client-side: the v16 discovery fallback for old SERVERS (`enableV16ServerCompat` + Tier B `enableV16Generation`; docs/planning/v16-client-compat-design.md) — there is deliberately NO client v18/v19 rung. `/lsslod diag` shows per-dialect membership (`Dialects: v20=…, v19=…, v18=…`). v16 compat is PERMANENT by user decision — never propose its sunset.
- **Protocol 20 (v0.10.0, cross-MC-version columns — XVER)**: column bodies carry an identity dictionary (block-state/biome identity strings) + per-section palettes of dictionary indices, so a column decodes on ANY supported MC version (`common/wire/` — IdentityCodec, WireSectionCursor, the translators, all corpus-golden-pinned). Live path translates at the producer boundary; the disk transcode path emits v20 directly from memo-resolved descriptors. Unresolvable identities render via the client fallbacks and report through ingest-failure containment. The **Via mismatch guard** (`enableViaMismatchGuard`) declines LOD sessions for Via-translated older-LSS clients; a proxy-side Via is invisible to it (documented hole). Store schema 4 holds v20 bodies (3→4 migrates via a paced walk, never a rebuild).
- **The server owns generation.** The disk miss IS the generation trigger: a cold column always reads disk first (one route, one SYNC slot), and on not-found the server generates if generation is enabled and a GENERATION slot is free — no request shape can force or forbid it. `ts == 0` (the retired v16 generate-request) routes identically to `-1`, as inert as the retired wire byte 0.
- **No client budget derives from any server cap.** The scanner declares the constant `WANT_SET_BUDGET` (800) per scan; the old Global Constraint #28 cross-clamp is gone. Its successor is a static inequality pinned by `WantSetBudgetInvariantTest`: `SYNC_ON_LOAD_SLOT_CAP (200) + MAX_CONCURRENT_GENERATIONS (512) + WANT_SET_FRONTIER_RESERVE (64) <= WANT_SET_BUDGET <= MAX_BATCH_CHUNK_REQUESTS (1024)` — the want-set dominates the worst-case in-flight set with frontier headroom, re-derived at build time if any constant drifts.

Backpressure lives in three places now, none of them on the wire: the bounded want-set (the constant `WANT_SET_BUDGET` = 800, inside the `MAX_BATCH_CHUNK_REQUESTS` = 1024 wire cap), the server's per-player dequeue gates (a full slot cap or disk pool *retains the entry in the backlog* — it never bounces), and the client's edge-triggered clear above.

Requests are keyed by packed chunk position end-to-end (no request ids): every response is a statement about a position, so late or duplicate responses are idempotent, and a column the client no longer tracks is still ingested. Delivery honesty: the server's per-player served-set (diskReadDone) answers re-declarations with up_to_date only when the client claims data (ts>0); a ts<=0 re-declaration declares "I have nothing" and re-resolves honestly (skipped silently while the payload is still in the send pipeline, deliberately without clearing the done-bit — see `IncomingRequestRouter.resolvedAsDuplicate`), so any post-resolution loss — send-queue drop, decode failure, consumer rejection — self-heals instead of becoming a permanent hole. The **duplicate-serve grace** (docs/planning/duplicate-serve-grace.md) extends the silent-skip window ~500 ms past the payload's send-success departure (`SEND_DEPARTURE_GRACE_MILLIS`; stamps live in the state, so they die with it on removal/dimension change): a ts<=0 re-ask that crossed its own delivery in flight (~7-8% of cold-backfill asks) is absorbed (counted `duplicate_skips` — law A1's disposition term — plus the `grace_skipped` observability subset) instead of costing a redundant read+send; past the grace the clear-and-re-resolve applies unchanged, so a genuine post-send loss heals at most one scan later. Send-FAILURE drops never stamp — their done-bits are proactively cleared, keeping that loss class instant-heal. Consumers report rejected columns via `LSSApi.reportIngestFailure`, which forgets the client's received-stamp so the next scan re-declares it.

Flow: handshake (capabilities bitmask; CAPABILITY_VOXEL_COLUMNS iff LSSApi has consumers) → 4-field session config → the client spirals its want-set with clientTimestamps (≤0 "I have nothing", >0 resync) → latest-wins mailbox → backlog replace → the router drains in declaration order: up-to-date checks, loaded-chunk probe, then `tryAdmitAndSubmit` (SYNC slot + disk read; full slot cap RETAINS and continues, no disk headroom RETAINS and stops — saturation never reaches the wire) → a disk miss escalates server-side (gen enabled + slot free → GENERATION ticket; slot full → silent drop; gen disabled → session-permanent `NOT_GENERATED`) → lightweight responses batch per tick, column payloads send individually → dirty broadcasts re-enter positions into the want-set (the ONE revival path for a NOT_GENERATED-parked position; on Paper walk-in generation does not mark dirty by default, so a parked position there heals on reconnect). Delivery honesty: the per-player served-set answers ts>0 re-declarations up_to_date; ts≤0 re-resolves honestly, with the **duplicate-serve grace** (~500 ms past send-success; `SEND_DEPARTURE_GRACE_MILLIS`) absorbing re-asks that crossed their own delivery, and **probe suppression** (1.5 s post-serve) skipping redundant re-serialization — both windows are WALL-clock (tick-denominated test waits must never race them). Consumers report rejections via `LSSApi.reportIngestFailure`, which forgets the received-stamp so the next scan re-declares. **Stamped up_to_date** (stamped-up-to-date-plan.md v1.3 — §9/§10/§11 are the 1-Fable, 3-Opus, and final-panel folds, all normative; built after the live rig measured a PERMANENT ~500-tile summary-stale set that re-ran identically on every rejoin): exactly TWO compare-backed up_to_date rungs — the router's tscache rung and the header-fresh delivery — emit (position, verification-second) stamps on `lss:col_stamps` to summary-requesting CURRENT-dialect sessions (the request is the eligibility declaration, re-checked at the sink against a legacy re-handshake), and the client RATCHETS its cached acquisition stamps forward (`ColumnStateMap.ratchetStamp` — monotonic, leaf-gated, ts>0-only, dirty/retry/sessionSatisfied-skipping, pure ts) so the next summary frame validates columns a later header write once passed (stale → stamped → clean; `stamp_heal.sh` = `stamp-heal-prime` → `stamp-heal-rejoin` is the live gate, before-state PINNED). The stamping doctrine: a rung may stamp ONLY if its staleness bound at disposition is provably inside the freshness margin — the STORE-stamp conversion never stamps (its own delivery-honesty doctrine: a re-serve hands the STORED second; a `stampedSiteCensusHoldsTheNarrowing` ARITY census — recursive over all five modules, balanced-paren 5-arg count + stampSecond call count — enforces exactly two sites), the done-bit rung never stamps (range-filtered invalidation channel — stamping it would make the F2 lost-CLEAR ghost hole permanent), the cannot-improve flavors never stamp, and the platform predicate — eligibility short-circuit FIRST, then `DirtyColumnTracker.isPending` (TWO-PHASE: mark → drain → invalidation-APPLIED, released by the broadcasters' `confirmInvalidated` callback, BATCH-COUNTED per position with a once-guarded callback so overlapping drains and requeue replays cannot release a later batch's guard early — the drain-to-apply gap sealed permanently at any dirtyBroadcastIntervalSeconds > 15), then the READ-ONLY `RegionStampTable.isClaimSuppressed` latch probe — refuses toward unstamped. Paper residual (accepted-open, UNCANARIED — pinned deliberate in `theEventBlindStateStampsByAcceptedDesign`): an event-blind content change can be stamps-sealed until the chunk's next save. Frames are fire-and-forget with the writability DROP (NOT_WRITABLE → uncounted, never retained — loss heals next rejoin, unlike the once-per-session summary frame's RETRY), flushed BEFORE the batch response (load-bearing: FIFO applies the ratchet while same-tick marks are still set — pinned), split at 1024 entries never truncated, counted `summary.stamps_{frames,entries,bytes}` server-side / `summary.stamps_{applied,ignored}` client-side. One accepted corner: gen-disabled + a loaded-but-never-saved chunk whose probe was deferred parks NOT_GENERATED despite being live — healed by its first save's broadcast or reconnect.

### Backfill visual ordering — settled model, do NOT re-investigate (2026-07-16/17)

"LOD chunks load far-before-near / rings missing" was root-caused across a full day of
src-tagged live traces and is CLOSED. The observable is three layers: (1) **arc-fill
geometry** (irreducible: ring r has 8r positions vs the per-player gen cap, so far
rings fill as sequential arcs, scrambled by non-FIFO schedulers, bounded to ~2 rings
by the order-spread gate); (2) **trailing view-edge crescents** (movement only:
positions exiting the vanilla view circle become LOD-needing for the first time and
pop in near — correct first-time service); (3) **the platform generating ahead of
LSS** (C2ME saves terrain beyond LSS's tickets; LSS correctly serves it on the next
declaration — the lever is C2ME's config, not LSS code). What WAS LSS's fault is
already fixed and test-pinned — do not un-fix under performance pressure: the
movement cadence debounce, the spread-gate anchor ladder, the disconnect bulk ticket
release (`DeferredTicketReleases`), the retired vanilla-load scan budget scale.
Diagnosis instruments if something NEW appears: `/lss trace` (`col` events carry
`src` 0=probe/1=disk/2=generation) and `/lsslod diag`'s `order_gated`/`inversions`.
A far `src:2` beyond frontier+2 on a fresh world is an LSS gate bug; far
`src:1`/`src:0` is the platform.

Chunk coordinates are packed as `((long)chunkX << 32) | (chunkZ & 0xFFFFFFFFL)`.

### Common Module (`common/`)

- `LSSConstants` — protocol version, channel IDs, wire format limits, status codes
- `LSSLogger` — SLF4J logging wrapper
- `SharedBandwidthLimiter` — fair per-tick bandwidth allocation across active players (global + per-player caps)
- `PositionUtil` — chunk coordinate packing/unpacking
- `DiagnosticsFormatter` — formats diagnostic data for `/lsslod diag` output
- `ColumnTimestampCache` — per-dimension up-to-date checks without disk IO. Tile-based (D0): one `int[1024]` per 32×32 region of epoch-second offsets (~4.25 B/column dense); every clamp rounds UP or refuses to fabricate freshness — the safe direction is a redundant serve, never a false up_to_date. Tile-granular LRU eviction under a byte budget; record-format v2 persistence with in-place v1 migration. Also hosts the **miss memo** (docs/planning/miss-memo-design.md): TTL-bounded authoritative disk misses so gen-waiting positions skip per-scan re-reads; `put()`/`invalidate()` clear entries, `invalidateStamps()` is the disk-drain's stamps-only variant; misses never persist.
- `store/` — the **LOD store** (docs/planning/lod-store-implementation-plan.md): `SqliteLodStore` — per-dimension SQLite tables of served wire bytes (zstd-1; WAL; thread-confined readers; single batcher writer; tombstoned deletes with immediately-committed DeleteRows; STAT-DRIVEN freshness sweeps against region mtimes with `src_stamp` = byte-ACQUISITION second; opt-in size-cap eviction with looped incremental_vacuum; drop-and-rebuild on any schema/wire/mask/registry drift EXCEPT the 3→4 step, which migrates in place via a paced background walk, AND — since v0.13.1 — a provably PURE registry-id permutation on an all-v20 store (order-insensitive `registry_content_fingerprint` beside the ordered key + identity-derived mask fingerprints; VisualWorkbench-class per-boot registry shuffles no longer rebuild, drop messages name the failing key — docs/planning/store-registry-permutation-plan.md). `LodStores` factory degrades full→null (a failed SQLite boot runs store-less, `store=unavailable` in diag). `StoreBackfill` — opt-in nearest-spawn region walk, restraint-gated, cap-aware, resumes across restarts. The serve rung lives in `AbstractChunkDiskReader` (hits excluded from disk.* counters); deposits ride the delivery choke ONLY (the Fabric save hook is DELETE-only); the dirty→store invalidation fan-out is the correctness backstop on both platforms. `/lsslod store status|invalidate all` on both platforms (backfill verbs not on Paper); `state=ok|sweeping|latched` — a latched store must LOOK dead.
- `region/RegionStampTable` — per-region freshness stamps (region-summary-sync-plan.md P1, 2026-08-19, reshaped by the 3-Opus fold): memoized region-header save-second tables (the store sweep's 8 KiB `readHeaderTimestamps` shape — z-major indexing, NOT `tileSlotOf` which is x-major; mtime-`!=`-invalidated with a 5 s stat horizon and the raced-mtime settle discipline; `int[1024]` snapshots capped at 4096 with strip-and-relearn enforced OUTSIDE the entry monitors — one monitor at a time, no lock-order inversion; EVERY doubt state incl. unreadable/unresolvable is a horizon-cached sentinel, never per-ask IO) + a per-region `liveSaveMark` (atomic monotonic max) bumped from `DirtyColumnTracker.markDirty` — the platform-neutral change choke point, listener fired BEFORE the mark becomes drainable. **The mark is a LATCH, never a stamp** (fold MAJOR): while `liveSaveMark > maxHeaderSecond` (a marked change no examined header reflects yet) the whole region answers NEVER_CLEAN — comparing client stamps against mark TIME is unsound because a read racing the pending write hands out stamps NEWER than the mark carrying PRE-change bytes; the latch self-clears when the write lands, and the FIRST observation of the clear condition still answers NEVER_CLEAN for one STAT_HORIZON (the clear-side grace, final-panel MAJOR: a sibling chunk's same-second landing can clear the latch while the marked write is still pending; mark==0 never pays it, so the evicted-tscache path is unaffected). Consumers: (1) the disk reader's **header freshness rung** — a ts>0 ask that missed the tscache answers `up_to_date` when the client stamp clears the header second by `HEADER_FRESH_MARGIN_SECONDS` (= read timeout + 5; client stamps are issued at read COMPLETION, so a bare strict compare would validate exactly the raced reads — fold MAJOR), skipping the read AND the send (`disk.header_hits`; excluded from submitted/completed like store hits; the headerFresh result carries the MARGINED bound, and delivery refreshes the tscache at bound+1 under a MONOTONIC MAX — a header-derived bound must never lower a higher observed acquisition stamp, fold MAJOR); (2) the **store freshness rung** at delivery — a store hit whose STORED acquisition stamp ≤ the recipient's stamp answers `up_to_date` instead of re-sending the bytes (the tscache doctrine's non-strict compare; all-air store hits still send the clearing column — the `columnBytes != null` guard is the ghost-terrain pin). Both are per-RECIPIENT at delivery (`PendingRequest` now carries `clientTimestamp`; a dedup member whose stamp fails takes the standard transient drop and re-declares) and stale-guarded (an overtaking edit voids the proof — no done-bit, no stamp); both count `service.up_to_date`. Every doubt path fails toward SERVING. This closes the tscache-miss GB-class warm-rejoin re-download. The region-dir resolver is HOISTED out of the store branch on both services (per-level throw belt — an exotic dimension degrades to UNKNOWN, never fails start) — the rungs work store-less.
- `region/RegionSummaryService` + `RegionSummaryWire` + `RegionSummaryDiagnostics` — the P2 summary server core (plan §5, reshaped by the changeset-2 3-Opus fold): per-player LATEST-WINS request mailboxes → tick-thread `pump` (the ONLY place player state is read — dimension-matched admission with a 5 s TTL retention for requests racing re-registration; RADIUS **and** CENTER clamped to the SERVER's own window `ceil(lodDistanceChunks/32)+1` tiles, never the protocol max 65, counting `summary.range_filtered` — fold I-M1: a ~30-byte request must not buy a 17k-tile sweep; plus a 5 s per-player re-sweep COOLDOWN = the stat horizon, held requests RETAINED not dropped — the three-layer flood bound: coalescing/window/rate) → a dedicated MIN_PRIORITY sweeper daemon (`tileStampSeconds` windows over the stamp table — readdir-listed existence, one per dimension per horizon; strict `r.<int>.<int>.mca` parse; degenerate-second regions poison to NEVER at TILE level; **no-evidence = DOUBT** (fold H-M1/H-M2): unlistable/not-a-directory region path, region deleted-after-observed, and all-absent headers (`maxHeaderSecond==0`) all answer NEVER_CLEAN — `STAMP_NO_REGION` (0, "validate your stamps") is ONLY for never-observed absence on a listable dir; tile sweeps store array-FREE lite header memos so a summary window can never evict the P1 chunk rung's retained arrays; reported stamps carry `FRESH_CLAIM_MARGIN_SECONDS`) → ready frames drained onto a DEDICATED send lane on the next tick (`summary.*` counters, NOT in `bytes_sent`; `frames`/`bytes` count only sender-confirmed sends; `tiles_no_region` is its own disposition — a whole-window no_region reading is the resolver-gone-wrong signal). The sender is TRI-STATE (SENT/RETRY/DROP — live-diagnosed 2026-08-20, rig reqs=7/frames=5): an unwritable channel (the F2 writability guard) answers RETRY and the service RETAINS the frame under a 10 s TTL — the frame drains at exactly the join/portal serve-flood moment, and the fire-and-forget client never re-requests, so a drop-on-unwritable eats the session's whole exchange; only a vanished player (or a mid-send throw, or a legacy re-handshake — the sink re-checks CURRENT dialect) DROPs. Retention is PER-PLAYER FIFO (final panel MAJOR: the client apply has no recency guard — its correctness rests on stale-then-fresh order, so a retained frame must never send after a fresher same-player frame; RETRY holds the frame at its own queue's head and stops only that player's drain), swept at removePlayer + the pump's anchor-less eligibility sweep (the Folia quit-race belt). Kill switch `enableRegionSummaries` (server, default true, HANDLER-checked — pinned on Paper's twin; boot-set, not a `set` row). Ingress: Fabric receiver → `RequestProcessingService.handleRegionSummaryRequest` (contained decode); Paper `dispatchPluginMessage` 5th case; NeoForge twins registered. Cleanup rides the NETWORK disconnect (`RegionSummaryService.removePlayer` beside `removeViewer` on all three loaders — sweeps the cooldown mark; TTL + vanished-send belts cover a miss), never the dim-change cycle. Server diag gains a `Summary:` line (present only once a request arrived). The Tier-2 `regionFreshnessServesRealRegionHeaders` gametest runs header rung + tile stamps + the whole summary pipeline against REAL game-written region files (terminal-pass shutdown — the static-mask-manager stomping hazard).
- `OffThreadProcessor` — main-thread → processing-thread handoff (probes/serializes on main, prepares payloads off-thread, routes via `IncomingRequestRouter`). `handleDiskNotFound` owns server-side generation: an AUTHORITATIVE disk miss escalates into a GENERATION slot when gen is enabled + a slot is free (full slot = silent drop, `superseded` + `miss_dropped`); timeouts are transient; only permanent failures answer `NOT_GENERATED`. ALL generation admission (memo rung and delivery path) runs the **pacing rules** — "generation never overtakes nearer in-flight work": nearer-pending-SYNC hold + `MAX_GENERATION_COHORT_SPAN` (1) beyond the nearest outstanding ticket (`gen_order_gated`), plus the **order-spread gate** (`MAX_GENERATION_RING_SPREAD` 2) anchored on the client-declared ACQUISITION frontier (the router-stamped first unsatisfied ts≤0 entry; unsatisfied ts>0 revalidations stamp only when a pass has no acquisition entry — gen-frontier-acquisition-anchor-plan.md, so dirty re-asks no longer collapse the window) with outward damping (333 ms/ring; inward instant) — the anchor history (batch[0] wedge, straggler leak, undamped movement inversion) is pinned in tests; the outstanding set is a RESTRICTION, never the window's anchor. Holds the invalidation-overtake stale guards (disk dedup-group + per-player generation, swept on removal; `requeueLosslessEvents` re-queues only incomplete phases so a consumed generation outcome is never re-delivered un-tainted). Timestamp saves ride `TimestampSaveScheduler` (latest-wins slot, one drain per empty→full transition — issue #62). Diagnosis instruments: `-Dlss.admissionTrace=true` (`[lss-adm]` per escalation decision), `/lsslod diag` `order_gated`/`inversions`, `/lss trace` client-side (`col` events carry src 0=probe/1=disk/2=generation).
- `IncomingBatch` — immutable `record IncomingBatch(IncomingRequest[])`; the unit the client declares, the mailbox holds, and the backlog is replaced with
- `AbstractPlayerRequestState` — per-player state. Under v17 it owns the want-set path: a latest-wins `pendingBatch` mailbox (`offerIncomingBatch`/`takeIncomingBatch`, an `AtomicReference` — network/pump/processing safe; an overwritten batch is counted superseded), the processing-thread-only `ArrayDeque` **backlog** (`replaceBacklogWith`/`pollBacklog`/`restoreBacklog`, with a volatile `getBacklogSize` mirror for diagnostics), and `publishedWantSet`/`peekWantSet` — the probe's fallback source, which survives `takeIncomingBatch` and clears only when the backlog drains (`restoreBacklog` republishes; without that the steady state is ~zero probe coverage). Also hosts the duplicate-serve grace's departure-stamp map (`departedColumns` — stamped on send success in `flushSendQueue`, swept once per grace interval, dies with the state) and the served-set sweep (`sweepDiskReadDoneOutsideRange` — M3: culls `diskReadDone` beyond the ingress radius + margin, keeps in-pipeline/grace entries, trims the backing array; without it the set grew monotonically all session)
- `IncomingRequestRouter` — replaces each player's backlog with the newest declared want-set, then drains it in declaration (proximity) order. The PLAYER drain order rotates once per cycle (M4 — the snapshot map iterates in identical UUID-hash order every tick, and against the GLOBAL disk-headroom gate an unrotated order let the first player starve the rest of disk reads under saturation; within-player order unchanged): checks timestamps for up-to-date, probes loaded chunks, dispatches disk reads (every entry — the client no longer classifies; generation is decided at the disk miss), with cross-player dedup via `DedupTracker`. Nothing is bounced: a full slot cap **retains the entry and keeps scanning** (the cap means "dequeue at most N concurrently", not "reject above N" — entries behind a full slot can still resolve via the ladder/probe), no disk headroom **retains and stops the pass** (saturation never reaches the wire — issue #32 at its source), and a full send queue retains and stops. A submit no-op (level not yet registered) drops silently — transient conditions never answer the session-permanent `NOT_GENERATED`. Retained entries are re-prepended in order by `restoreBacklog`, which also republishes the want-set
- `PendingRequest` / `SlotType` — derived admission: a pending-map entry IS the held slot (no separate limiter object). Every entry holds `SYNC_ON_LOAD` until a disk miss escalates it to `GENERATION`
- `DirtyColumnTracker` — thread-safe tracker of per-dimension dirty chunk positions, drained by broadcasters
- `SendActionBatcher` — reusable per-tick accumulator for batching `SendAction` responses by player
- `HandshakeGate` — pure decision ladder for the C2S handshake (version mismatch → no reply; no consumer → reply without registering; disabled → advertise disabled; else register), shared by `LSSServerNetworking` and `LSSPaperPlugin` so reply/registration policy cannot drift between platforms
- `JsonConfig` / `ServerConfigBase` — GSON config base classes; `ServerConfigBase` holds the server config fields, defaults, and clamps shared verbatim by Fabric and Paper. **Since v0.11.0 stage C the per-key clamps are STATIC HELPERS** (`clampLodDistance`, `clampMbPerPlayer/Global`, `clampGenGlobal/PerPlayer`, `clampDirtyBroadcastInterval`, `clampMaxConcurrentDiskReads`) shared by `validate()` AND the `/lsslod set` registry (`common/config/RuntimeSettings`) — the R-2 rule: a registry row can never clamp differently from boot validation, so `set dirtyBroadcastIntervalSeconds 0` stays 0 and `set maxConcurrentDiskReads 0` stays AUTO. The advisory validate() INFO lines (dirty-0 mode, Paper's Folia store warn) are log-on-CHANGE (transient last-advised fields) since validate() re-runs on every `set`. **The tick-poll pattern** (SET plan): formerly capture-at-construction consumers re-apply config at the top of their owning tick — `SharedBandwidthLimiter.reconfigure`, both gen services' `updateCaps` (plain fields, same-thread), the per-player `genSlotCap` (VOLATILE — the processing thread reads it cross-thread), `OffThreadProcessor.updateSweepRadius` (monotonic max — an under-radius sweep is free, redundant serves not corruption), and `AbstractChunkDiskReader.reapplyGateCapacity` (the reader owns the pool size + post-degrade store facts). The AUTO timestamp-cache budget deliberately does NOT rebuild at runtime (boot-sized until restart; the `set` reply says so)
- `CommandHelp` — the shared `/lsslod help` line builder (both platforms render identical lines; root label flows from Brand, so VSS prints `/vsslod`)
- `farplayers/` — **far players** (v0.11.0 E1-E3, docs/planning/far-player-proxies-plan.md as amended by the mega plan's R-3/R-5/R-7/R-9/R-10): distant players render as simple player models in the LOD terrain. `FarPlayerWire` (version-neutral byte[] bodies on the three `lss:far_player_*` channels — identity-string per-payload dictionary, epoch'd roster, quantized pos/angles/velocity, the R-10 vehicle block, bounded hostile decode); `FarPlayerBroadcastService` (single-threaded on the owning tick: snapshots built ONCE per tick, SeeU's filter ladder + server-authoritative privacy — mode `on`/`opt-in`/`off`, exclude list, `lss.farplayers.hidden` permission, Paper "vanished"-metadata vanish bridge, shareSelf opt-outs — epoch'd roster with **commit-on-success** mutation (a withheld frame mutates NOTHING), nearest-first visible cap + dictionary-bound update chunking, delta suppression with the one-shot rest frame, tiered cadence, a dedicated send lane with own counters — `far_players` exporter group, law-audited as `bytes_sent + far_players.bytes`); `FarPlayerClientTracker` (epoch armor — unknown-epoch updates and stale incremental rosters drop wholesale; sticky equipment; the 4096 identity cap; one `FarPlayerMotion` per player); `FarPlayerMotion` (declared-cadence lerp ×1.2 margin, measured correction ≤2× declared — the delta-suppression armor — velocity dead-reckoning ≤1.5 windows, monotonic clock). **The capability bit is the PREFS CARRIER** (E2 review): it composes from arm+harness-properties ONLY, never config — a disabled client still subscribes to deliver its shareSelf opt-out; the server skips serving disabled subscribers before frame work. Soak/benchmark clients are property-gated off (`far_players` counters must read zero on every soak — the all-snapshots inert check in check_soak). Subscription identity: handshake beside the dialect mark (CURRENT dialect only; Paper post-dialectFlip in the Register drain), dropped at disconnect/quit-race Remove/departed-player sweep/re-handshake-without-the-bit, survives dimension change
- `RuntimeSettings` — the `/lsslod set` key registry (12 keys incl. the requireServicePermission gate row: the 7 from stage C — distance, both gen caps, both mb caps, dirty interval, gate K — the two R-9 far-player rows from E1, and the two BOOLEAN rows `enablePingBackstop` + `enableSendPacing` (strict true/false parse — adaptive-transfer-rate-plan.md / send-pacing-plan.md; the `outboundBufferCeilingKB` row died with the ceiling, 2026-08-13); `applyAndPersist` = parse → per-key clamp → assign once → validate() → save(); apply-notes carried per key for the command reply)

### Fabric Server-Side (`fabric/`)

- `RequestProcessingService` — orchestrates per-player processing, ticks on server thread, broadcasts dirty columns
- `PlayerRequestState` — per-player request state, pending-request slot admission, send queue, metrics
- `ChunkDiskReader` — async region reader. **Disk-read gate** (`DiskReadGate`, stage B as amended): at most K = `effectiveMaxConcurrentDiskReads(pool, store)` threads run the expensive phase; a permit-less store miss PARKS (bounded, drained on release — pure fail-fast starved permit holders); sustained saturation is answered by ROUTER RETENTION (`disk.gate_stops`), park overflow is race armor only (`disk.gated`, ~0, log-free). `hasHeadroom()` is deliberately NOT narrowed by the gate; backfill bypasses. **Read-path ladder** (`useBackgroundReadPriority`, default true): (1) the Moonrise rung — reflective `MoonriseRegionFileIO.loadDataAsync(..., Priority.LOW)` when Moonrise-Fabric is present (`read_path=moonrise-low` in diag; typed linkage-throw latch → warn-once → inline fallback); (2) the vanilla IOWorker path at BACKGROUND priority — since `useBackgroundReadSplit` the executor task is FETCH-ONLY (raw record) with inflate+parse on the LSS pool (the A7 timeout-storm fix; false restores full-read-on-executor); gives up read-your-writes (self-heals via dirty broadcast). Chunk-IO-overhaul mods (C2ME) null the vanilla IOWorker internals — `backgroundReaderOrFallback` latches `backgroundIncompatible`, warns once, falls back to `chunkMap.read` + the `AdaptiveReadThrottle` (yields to gameplay by narrowing `hasHeadroom()`). These branches are live-only (gametests/soaks run plain vanilla IO) — `ChunkDiskReaderTest`/`MoonriseReadCompatTest` pin them via injected seams and real-package-name stubs. The DIRTY hook is C2ME/Moonrise-proof since #69: `SerializableChunkData.copyOf` is the choke point every chunk system funnels through. See docs/planning/read-scheduler-design.md §10.4.
- `FabricOffThreadProcessor` — extends `OffThreadProcessor`; main thread probes loaded chunks via `SectionSerializer`, processing thread prepares payloads and schedules sends
- `SectionSerializer` — serializes loaded MC `LevelChunkSection` + light data into wire-format bytes using MC's native `section.write(buf)`
- `NbtSectionSerializer` — region-NBT → wire bytes (used by `ChunkDiskReader`). Since round 2 (2026-07-30, `useNbtTranscode` default true) the default path TRANSCODES: palette global ids via the extended `MemoizedNbtCodec` memo + the disk long array verbatim, no `PalettedContainer`/`LevelChunkSection` ever constructed — byte-identical by the unpack-verbatim invariant, pinned by the object-path-generated goldens + a transcode-vs-object fuzz. Exotic shapes (>256-entry block / >8-entry biome palettes, malformed data, x-ray-mask-needing sections — the pre-gate mirrors `needsMasking` via `MaskSet.containsId`) fall back PER SECTION to the object path (codec parse, the permanent rung and the flag-off rollback)
- `ChunkGenerationService` — ticket-based chunk generation, triggered by the server on any disk-read "not found" (gen enabled + slot free). Timeouts are TRANSIENT outcomes (silent drop, retried via re-declaration); only extraction errors are permanent. Fabric generation has no priority hand-off (vanilla pins worldgen priority to ticket level 33) and no latency throttle (worldgen's variable baseline makes latency a poor congestion signal) — the concurrency caps are its honest limiter; a future adaptive backoff should key on tick health (MSPT), not latency
- `DirtyColumnBroadcaster` — periodically pushes dirty column notifications to connected players
- `DirtyContentFilter` — FNV-1a hash of served column bytes per position; chunk saves mark a column dirty only when LOD-visible content actually changed (filters vanilla's ~10s metadata-only re-saves, e.g. inhabitedTime). Fabric only — Paper's dirty detection is event-driven.
- `LSSNetworking` — `PayloadTypeRegistry` registration for all C2S and S2C payload types
- `LSSServerNetworking` — server-side event listeners, `RequestProcessingService` lifecycle (started via `ServerLifecycleEvents.SERVER_STARTED`)
- `AccessorServerChunkCache` (mixin) — exposes `ChunkMap` for disk reads
- `ChunkSaveDataHook` (mixin) — dirty-detection source: static @Inject at `SerializableChunkData.copyOf` RETURN with `require = 0` (issue #69 — the retarget from `ChunkMap.save`, which Moonrise @Overwrites into a throw-stub; that made LSS's injector match zero targets and fatally crash Moonrise servers at world load). `copyOf(level, chunk)` is the snapshot-for-saving choke point EVERY chunk system funnels through — vanilla's `ChunkMap.save`, Moonrise's `NewChunkHolder.saveChunk`, and BOTH of C2ME's save paths (whose rewritten chunk system never invokes `ChunkMap.save` at all — the old hook silently never fired there) — verified against decompiled vanilla 26.2 + Moonrise-Fabric 1.1.0 + C2ME 0.4.2 bytecode. Body in `LSSServerNetworking.onChunkSaveData` (mixin classes refuse classloading under fabric-loader-junit); may run OFF-main under C2ME/Moonrise — safe wherever `copyOf` itself is legal, `DirtyContentFilter`'s synchronization is load-bearing for it. Pinned by `SaveHookContractTest` (copyOf target + require=0 + vanilla shape)
- `IntegratedServerLanHook` (client mixin) — starts `RequestProcessingService` when an integrated server is published to LAN
- `dev.vox.lss.trace` — the **move-desync tracer** (docs/planning/move-desync-tracer-plan.md; ships in the release jar, fully inert by default): server-side observability for 26.2's three movement-rejection paths (`too_quickly`, `wrongly`, and the previously-invisible SILENT rejection) plus armed-player flight telemetry (1 Hz rows + a 5 Hz in-memory ring flushed ahead of events). Activation is `-Dlss.moveTrace=true` OR the marker file `config/lss-move-trace.enable`, read once at SERVER_STARTING — no config key, deliberately absent from the documented config surface (user decision); output `logs/lss-move-trace.jsonl` (rotate once at 128 MiB, 256 MiB total cap), validated by `scripts/check_move_trace.py`. Mixins live in their own **non-required** `lss-trace.mixins.json` (`@Accessor` cannot soft-fail — a required config would crash tracer-DISABLED servers on MC field drift); hook bodies delegate to `MoveDesyncHooks` and every capture is catch-all-contained. Chunk-delivery state resolves per JVM through `ChunkSendState` (Moonrise rung via reflective loader reads, vanilla rung via public `chunkSender.isPending`; moonrise-present-but-unresolvable = rung `none`, never a wrong answer). `/lsslod diag` shows a `MoveTrace:` line ONLY while active (rung + row/drop/event counters — the post-deploy verification and the silent-rejection rate in one call). Lifecycle hangs off `LSSMod`, never the service — control-arm rows (LSS disabled/unregistered, `lss:null`) are the point. The probe-snapshot refactor rode this change (mega plan R-2): `ChannelPressureProbe.snapshot()` default method + public `FabricChannelPressure`/`PaperChannelPressure` factories.

### Paper Server-Side (`paper/`)

- `PaperRequestProcessingService` — same orchestration as Fabric via Plugin Messaging; ticks on the GlobalRegionScheduler pump and drains a cross-thread lifecycle mailbox at the top of `tick()` (a REGISTERING handshake's SessionConfig reply is DEFERRED through the mailbox and sent right after `registerPlayer` — an inline reply invited the first want-set into the pre-registration gap). On Folia only, loaded-chunk probing runs on the chunk's owning region thread with a one-tick batch hold-release: the pump releases last tick's held batch via `republishHeldBatch` (CAS + an offer-generation counter) BEFORE taking anything new — release-then-take, never take-then-release, or a superseded batch resurrects. `RegionTaskScheduler`/`RegionOwnershipCheck` are the test seams.
- `PaperPlayerRequestState` — per-player state adapted for `byte[]` payloads instead of `CustomPacketPayload`
- `PaperChunkDiskReader` — async disk reader using direct NMS `ChunkMap` access (no mixin needed). With `useBackgroundReadPriority` (default true) reads route through `MoonriseRegionFileIO.loadDataAsync(..., intendingToBlock=false, Priority.LOW)` instead: Paper/Folia gameplay loads run on Moonrise's own prioritised IO pool rather than the vanilla `IOWorker`, so IOWorker priority would be a no-op here and LOW is what actually defers to gameplay's NORMAL. Unlike Fabric's vanilla-IOWorker path this keeps read-your-writes (`loadDataAsync` serves in-progress writes — Fabric's Moonrise rung keeps it too). Flag false restores `chunkMap.read`. The live LOW path has no cheap unit test — the soak harness on real Paper/Folia is its gate (no `read_path` diag token here, deliberately: on Paper "flag on" IS the Moonrise path; the token exists for Fabric's live-only rung)
- `PaperOffThreadProcessor` — extends `OffThreadProcessor`; same main-thread/off-thread split as Fabric
- `PaperSectionSerializer` — Paper-side serializer using NMS `LevelChunkSection.write(buf)` + light data
- `PaperNbtSectionSerializer` — region-NBT → wire bytes (used by `PaperChunkDiskReader`); textual twin of `NbtSectionSerializer` incl. the round-2 transcode path + per-section object fallback (`useNbtTranscode`)
- `PaperChunkGenerationService` — async chunk generation via Moonrise `PlatformHooks.get().scheduleChunkLoad(..., ChunkStatus.FULL, addTicket=true, Priority.LOW, consumer)`, triggered by the server on any disk-read "not found": LOD generation DEFERS to player-driven NORMAL generation (the generation analogue of the `Priority.LOW` read path). The consumer delivers an NMS `ChunkAccess` with no throwable channel — null is the (permanent) failure outcome; timeouts stay transient. Serializes the generated chunk inside the completion (`completeAsyncLoad` — on Folia that is the owning region thread and the chunk can unload before a hop to the global tick) and hands only the immutable `LoadedColumnData` to the pump. The live LOW path has no cheap unit test — the soak harness on real Paper/Folia is its gate (the `Priority.LOW` argument is unit-pinned via the `launchAsyncLoad` seam)
- `FoliaSupport` — platform probe (`Class.forName` on Folia's regionizer class); used only where Folia removes functionality outright (e.g. the soak driver's save-all mapping)
- `PaperPayloadHandler` — encodes S2C / decodes C2S payloads using same wire format as Fabric
- `PaperCommands` — Bukkit `CommandExecutor` for `/lsslod stats|diag|store|set|help` (v0.11.0 stage C: `set` marshals the mutation through the pump via `enqueueRuntimeTask` — drained AFTER the lifecycle mailbox, so the SessionConfig re-push can never enumerate a registered-but-flip-pending legacy player as CURRENT (the SET review's ordering MAJOR, pinned); bare `/lsslod` = the shared `CommandHelp` lines)
- `PaperConfig` — GSON JSON config (same defaults/validation as Fabric)
- `PaperDirtyColumnBroadcaster` — broadcasts dirty column notifications to connected players (same as Fabric's `DirtyColumnBroadcaster`)
- `PaperWorldHandler` — registers configurable Bukkit event listeners for dirty chunk detection via reflection-based position extraction
- `PaperSoakBridge` — reflective gate from `LSSPaperPlugin` into the dev-only `dev.vox.lss.paper.soak` package (soak scenario driver + metrics exporter twins; excluded from the release shadowJar, retained by `soakShadowJar` for `SOAK_PLATFORM=paper` and `SOAK_PLATFORM=folia` runs)

### Client-Side

- `LodRequestManager` — orchestrates the client request loop: each scan the scanner writes the whole want-set into its send buffers and it ships in the same tick; handles dirty column re-requests, pruning on movement, and the one edge-triggered empty batch that clears the server backlog on backpressure halt (decode queue OR consumer-reported ingest backlog). No drip-feed, no timeout sweep, no rate-limit retry — re-declaration replaces all three. Also owns the client half of **region summaries** (region-summary-sync-plan.md §6): a fire-and-forget request at BOTH dimension-entry sites (initial load + change; center = the player's tile, radius = min(65, ceil(effective lod/32)+1)), gated on `enableRegionSummarySync` + the CURRENT dialect + the harness property gate (`-Dlss.soak`/`-Dlss.benchmark` opt back in via `-Dlss.soak.summary`); the S2C frame decodes contained, drops on dimension mismatch (the entire anti-stale binding), BUFFERS latest-wins while this dimension's cache load is in flight and applies right after `adoptLoaded` + the ingest-failure re-applies (a rejected column must never be sealed validated), then validates per tile via `ColumnStateMap.applyTileValidation` with provenance-scoped revocation (final-panel MAJORs: fresher frames revoke ONLY summary-provenance bits — per-column server proofs are never downgraded — and each revoked position's scanner ring is REOPENED, or positions below the confirmed prefix would orphan). BOTH sentinels are skipped (STAMP_NO_REGION = no evidence — the table's never-observed scope is server-lifetime, so a server-off deletion reads never-observed; validating against it would seal deleted terrain forever): NEVER_CLEAN counts `tiles_unknown`, NO_REGION counts `tiles_no_region` (own counter so the harness honesty legs stay sharp — a window legitimately spans many never-generated regions). Dirty notices during a cache load queue in `dirtyDuringCacheLoad` and re-mark (+ ring reopen) after adoptLoaded/failure re-applies, before the buffered frame. Counters `summary.tiles_{clean,stale,unknown,no_region}`/`columns_validated` (client exporter group + conditional `/lss diag` Summary line)
- `SpiralScanner` — expanding Chebyshev ring scan (adaptive cadence: 20-tick fallback + the completion-triggered fast re-scan, `fastRescanDue` — 250 ms floor, ≥95%-answered threshold with geometric tightening, **predicted**-walk-cost + ¼-halt-threshold pressure gates, v16 sessions excluded; armed only via `noteDeclared` from `tickScanPhase`) that produces the want-set closest-first; owns scan policy: the constant `WANT_SET_BUDGET` (800 — no client budget derives from any server cap) with pressure scaling — the MIN of the decode-queue factor and the consumer-reported ingest-backlog factor (issue #71; <=0 backlog = no signal, bit-identical to pre-#71; the vanilla-load scale is retired — server-side read/generation priority owns that protection). Does NOT suppress awaited positions — an awaited position blocks its own ring's confirmation (the fast trigger reads the awaiting-set SIZE as a cadence input, never as a send filter). Since 2026-08-18 the walk runs two intervals — a reopened-ring bitset below the retained confirmed prefix, then the frontier — so crossings/dirty marks cost O(affected rings), not O(disc) (scanner-reopened-rings-plan.md; `reopenRing`, the Euclidean crescent band, 64-bit overflow valve, `reopened=` diag field). Since 2026-08-19 (quadtree-client-state-plan.md §12) a ring whose crossed section LEAVES all have clear needs-masks confirms in O(perimeter/8) leaf checks without touching positions (`ColumnStateMap.ringNeedsFree`; kill switch `enableQuadtreeScan`, gates the WALK only) — satisfied-disc reset walks (teleport/LOD-shrink/valve) drop ~30-90 ms → ~1.5-3 ms (the post-cache-load hitch is the off-thread leaf build's fix instead — adopted stamps are revalidation needs, never fast-skipped); emission/cadence bit-identical BY CONSTRUCTION (the prefix/valve machinery runs unchanged — QuadtreeWalkDifferentialTest pins it); `ring_skips=`/`valve=` diag, `scan.quad_ring_skips`/`scan.valve_trips` exporter fields
- `RegionScanner` — the HYBRID want-set walk (region-scan-plan.md + hybrid-scan-plan.md, 2026-08-24; the default arm via client `enableRegionScan`, chosen at `LodRequestManager` CONSTRUCTION — a flip applies at the next join/SessionConfig re-push, never mid-session). Extends `SpiralScanner` (every cadence/budget/latch mechanism inherited verbatim); overrides the walk with TWO PHASES. Phase 1 walks rings r₀+1..N (N = min(`HYBRID_NEAR_RADIUS` 64, lod); r₀ = `wholeExcludedRings(vd)`, the outermost ring wholly inside the 1-buffered Euclidean vanilla exclusion — brute-pinned) in the legacy closest-first ring order: each ring probes `ColumnStateMap.ringNeedsFree` FIRST (gate-independent — counted into `ring_skips` on both quad variants), needy rings pay the 8r position pass, and a budget break inside phase 1 sets the truncation latch and SKIPS phase 2 entirely (nearest-first preserved under pressure scaling). Phase 2 spirals 32×32-chunk regions (`rx = cx >> 5`), each visited region emitting the still-wanted positions of its residue — `(region ∩ lodSquare) \ nearSquare`, decomposed into ≤4 disjoint rects (`residueRects`; contained-in-near regions are skipped outright) probed leaf-wise via `ColumnStateMap.rectNeedsFree` — ring-ascending via a counting sort (region ring span ≤31 — 1-Lipschitz). The declared set is a COMPLETE PREFIX of the combined order + at most one partial group across BOTH phases (`region_span=` ≤2 on dense interior fill; legitimately wider on lod-edge CLIPPED residues — the invariant is the complete prefix, not a span number); at lod ≤ 64 phase 2 is empty and the emission ORDER equals the fresh legacy walk exactly (differential-pinned under both quad variants). Stateless over the live player position: `recenter`/`reopenRing` are no-ops (needs bits carry every invalidation; the phase-2 `rectNeedsFree` leaf-mask probe skips satisfied residues — its rect bounds ARE the near+lod clamp, conservative toward walking; `regionNeedsFree` is RETIRED), `confirmedRing` = min OBSERVED unresolved ring, lod+1 converged (the soak law `scan.confirmed > 24` reads identically AT CONVERGENCE — mid-fill the region value is live-recomputed each walk, more volatile than legacy's retained prefix). Cadence is the review-panel-repriced deviation from plan §8 (§14.1; see `predictedWalkCost`'s javadoc): the MOVEMENT WINDOW prices legacy's from-zero formula over the TRUNCATED frontier (`4·s(s+1)`, s = scanRing when truncated else lod — the pinned elytra unlock preserved below ring ~96-128, 1 Hz flight above), and STATIONARY prices the LAST walk's measured observe work — 4 Hz dense backfill at any frontier depth, 1 Hz on a mass dirty scatter (each needy region costs an emit pass). An audit rung re-derives one region's leaf needs per periodic fire (`audit_heals`, expected 0 — a stuck-OFF needs bit inside an otherwise-clear region is otherwise a PERMANENT orphan; runs even on a phase-1 break). Diag `near_rings=` (phase-1 rings that emitted or observed needy work this walk — active during near fill, ~0 at convergence) beside `region_span=`/`region_skips=`/`audit_heals=`; exporter `scan.near_rings`/`region_span`/`region_skips`/`audit_heals`; the client trace `scan` event carries `near_rings`. `RegionScanDifferentialTest` pins want-SET equality (positions + timestamps + confirmed ring) against BOTH legacy quad variants of the fresh walk at far lods, plus ORDERED-sequence equality at lod ≤ 64; the four reopen/prefix mechanics pins in the manager suites are legacy-arm constructed (plan §10 policy), with region twins (the summary revocation + the dirty-frame pair) asserting the needs-bit re-declaration path, the governor window-limited latch pinned on both arms, and the arm selection itself pinned against the local-config hazard
- `ColumnStateMap` — single owner of per-column client state (timestamps + dirty/retry/validated marks); its `classify` ladder decides whether a position is still wanted, and marks are consumed at the terminal answer (up-to-date / not-generated), not at send. `onNotGenerated` is a PERMANENT session-satisfy (no 0-stamp; only `markDirtyIfKnown` — a dirty broadcast — revives it); legacy cache 0-stamps from released clients classify as "no data" and re-declare -1. **Backing since 2026-08-19** (quadtree-client-state-plan.md): dense 8×8-chunk section leaves (one mask long per boolean state + `long[64]` ts + a derived `needs` mask maintained on every transition — needs ⇔ classify≠SATISFIED, fuzz-pinned) instead of eight hash structures — ~6-10× memory cut at distance 512, leaf-granular prune, and the scanner's ring fast path. Semantics pinned by `SectionStateFuzzTest`'s differential against `ReferenceColumnStateMap` (hash-backed reference: the v0.11.1 surfaces verbatim, the summary-era ops as co-designed independent-structure twins — do not "improve" the reference). Cache lifecycle: loads build leaf state on the IO thread (`buildLoaded`/`adoptLoaded` — the 100-300 ms A1 gate-apply hitch fix), saves DETACH the state by ownership transfer (`detachForSave` → `mergeSaveDetachedAsync` — destructive by contract, both callers clear/drop right after; the A2 dim-change copy fix); cache file stays v4. `applyTileValidation(tileX,tileZ,stampM,revokedOut)` is the region-summary consumer (§6), two-directional and provenance-scoped (final panel): for every STAMPED position in the 4×4-leaf tile, set `validated`+`summaryValidated` iff its stamp is STRICTLY above the margined tile stamp; a failing compare REVOKES only `summaryValidated` bits (per-column server proofs — `onReceived`/`onUpToDate`, which clear the summary bit — are never downgraded) and reports each revoked packed position to `revokedOut` so the caller reopens its ring. Mark-preserving (dirty/retry survive and outrank validated in classify), never creates leaves (hostile frames can't allocate), sentinels are the caller's job; pinned equivalent to per-column `onUpToDate` for mark-free stamped positions, differentially fuzzed against the reference twin (SectionStateFuzzTest op incl. revocation-set equality)
- `InFlightTracker` — the awaiting-answer set (positions of the last want-set minus answers since), replaced wholesale each scan. NOT a send-suppression structure: it exists only to gate status responses (BatchResponse carries no dimension) and to detect a dirty broadcast crossing an in-flight answer
- `ClientColumnProcessor` — off-render-thread decode of received column payloads before dispatch to consumers
- `ColumnCacheStore` — per-server per-dimension binary cache of column positions and timestamps (enables resync across sessions). Root resolved LAZILY by `resolveCacheRoot` (v0.11.0 stage D): fresh installs use `<gameDir>/.lss/cache` (the `.voxy` convention, unbranded for jar-swap continuity); an existing `config/lss/cache` DIRECTORY is adopted forever (no migration — user decision). The harness scripts clear/collect BOTH roots (soak.sh's cold-restart-resync restore deliberately creates the legacy root, forcing adoption for that run)
- `ResetCoordinator` + the `VoxyCompat` reset ladder — `/lss reset` (stage D): decode-drain → Voxy teardown/wipe/rebuild (wipe root from the LIVE instance cross-checked against the connection's derived path — a mismatch skips the wipe, the Flashback-replay guard; unresolvable render holder ABORTS; shutdown-throw skips the wipe) → `flushCache()` last. Wipe containment strictly inside `.voxy/saves/` or `<world>/voxy`, else fail-safe skip. No-manager fallback is confirm-gated (`/lss reset confirm`).
- `FarPlayerClientSupport` + `FarPlayerRenderer` + `FarPlayerMountLadder` — the client half of far players (Fabric, and the NeoForge twin on this line since v0.14.0). Support: capability composition (the prefs-carrier rule), prefs send-once-unless-changed, the SeeU-coexist gate, the reset re-subscribe. Renderer: `RemotePlayer` proxies via the line's render event (surfaces row 15 — the event NAME is not the invariant); handoff = vanilla's own cull predicate both directions; whole-pass crash latch. **Hardening 2026-09-04** (docs/planning/far-player-render-hardening-plan.md, both twins): proxy light is a sky-15 FLOOR over vanilla's block light (`farPlayersFullBright` = FULL_BRIGHT) — NEVER the client's stored light data, which reads zero in C2ME's no-tick ring beyond the server's entity-tracking radius and turned proxies black in daylight (the fresh cut's loaded/unloaded split was dead code: `ClientLevel.hasChunk` is unconditionally true); the DRAW calls only are frustum-culled (mount bookkeeping untouched; never `dispatcher.shouldRender` — distance term + Sable's injection); LSS draws its own depth-tested, sneak-hidden, `0.025×clamp(sqrt(d/64),1,8)`-scaled name tags (vanilla gates tags at 64 blocks on every line — `farPlayersNameTags` was inert) — as TWO draws, the second glyph-only on a plane lifted toward the camera by `clamp(d²·1.5e-5, 0.05, 24)` blocks (the font's background plate is a glyph effect a fraction of a depth step nearer than the glyphs, so a single depth-tested draw z-fights white/grey; live rig 2026-09-04), and ALSO over vanilla-tracked players past vanilla's 64-block cap under vanilla's own visibility ladder (the tag otherwise vanished in the 64..tracking-radius band where the real entity is vanilla's to draw); the model-parts byte is set (0x7E, 0x7F with an elytra) so the overlay skin layers render; the proxy advances the tick-only render inputs itself (walk cycle from position deltas with a `setSpeed(0)+update(0,1)` stop — this MC has no `stop()`; the FALL_FLYING shared flag + fall-fly ticks for the glide tilt; the swim amount via `AccessorLivingEntity`, both mixin configs) — `LivingEntity.tick()` never runs for a render-only entity; the pass ends its own shared batch with `endLastBatch()` (arg-less `endBatch()` stays forbidden); `/lss diag` gains the `FarPlayerRender:` line. Pinned by `FarPlayerRenderSourceContractTest`. Mounts: rider at its own wire position, edge-triggered persistent ride link, per-type degrade ladder (creation AND rendering throws latch the TYPE).
- `LSSClientNetworking` — packet handlers, dispatches sections to `LSSApi` consumers
- **The options page** (docs/planning/sodium-options-page-generations-plan.md, 2026-08-23): `xplat/config/menu/ClientOptionCatalog` is the SINGLE source of truth for the in-game settings (pages → groups → options as MC-free, Sodium-free records: ids, lang keys, defaults pinned equal to a fresh `LSSClientConfig`, bindings over a config instance, `enabledBy`, `SaveHook` incl. the far-player prefs push, `Tooltip.Condition`/`Visibility` enums over `MenuContext`) — add an option THERE + the lang file, never in a renderer. Renderers are thin walkers selected at RUNTIME by `SodiumGeneration` (a RESOURCE probe — never `Class.forName`, which would define the mixin target past the hook): `LSSConfigMenu` (fabric-only, Sodium 0.8+ public config API, present iff the line has a 0.8+ artifact — `ClientMenuEntrypointContractTest` pins entrypoint ⇔ file) and `LegacySodiumPage` (per-loader twins, Sodium 0.6/0.7's INTERNAL options API bound reflectively with zero compile dep — `Proxy` storages answering `hashCode`/`equals` because the legacy screen HashSets them; `SURFACE` is the member table `SodiumLegacySurfaceResolvesTest` checks against the real 0.6.13 bytecode by name+arity) injected by the `@Pseudo` string-targeted `SodiumLegacyOptionsHook` at `SodiumOptionsGUI.<init>` (its own non-required `lss-sodium-legacy.mixins.json`, both loaders; `compatibilityLevel` per line). `SodiumConfigScreens` (fabric) is the reflective generation switch behind ModMenu's Configure button (`MODERN_SURFACE` checked against the line's own 0.8 artifact by the same resolves-test; legacy: pre-init `currentPage` set, never `setPage` before `init()`); `LSSModMenuIntegration` is a one-line delegate. The far-player render-only options are `Visibility.RENDER_AVAILABLE`-gated on `FarPlayerRenderer.RENDER_AVAILABLE` (false on NeoForge v1 — only "Share My Position" shows there; but on THIS 1.21.1 line the NeoForge renderer is LIVE since v0.14.0, so all far-player options show on both loaders here). NeoForge renders the legacy page only (the 0.6.13 fork path); the 0.8 NeoForge walker is a recorded follow-up.

### Public API (`LSSApi`)

LOD rendering mods register a `VoxelColumnConsumer` via `LSSApi.registerColumnConsumer()` to receive `VoxelColumnData` (MC-native `LevelChunkSection` + `DataLayer` light data per section, with coordinates). Consumer list is `CopyOnWriteArrayList` for thread safety. The client sets `CAPABILITY_VOXEL_COLUMNS` in the handshake only if consumers are registered.

### Compatibility

`VoxyCompat` uses `MethodHandle` reflection to call Voxy's `rawIngest` API at runtime — zero compile-time dependency, only 2 core MethodHandles (`WorldIdentifier.of` + `VoxelIngestService.rawIngest`). MC types (`LevelChunkSection`, `DataLayer`) must be referenced via direct class literals (not `Class.forName()`) due to Fabric's runtime remapping. `ModCompat` checks `FabricLoader.isModLoaded()` before attempting.

`MoonriseReadCompat` (Fabric, docs/planning/moonrise-read-priority-design.md) is the Moonrise-Fabric read bridge, same zero-compile-dep pattern: resolves `MoonriseRegionFileIO`'s 7-arg `loadDataAsync` overload by SHAPE (`getMethods` scan matching `(ServerLevel, int, int, <enum>, BiConsumer, boolean, <enum>)` — the `RegionFileType`/`Priority` classes come from the matched method's own parameter types, because Moonrise-Fabric SHADES concurrentutil under `ca.spottedleaf.moonrise.libs.…` unlike Paper), then invokes at `Priority.LOW` with `intendingToBlock=false`, the exact Paper semantics. Gated on `isModLoaded("moonrise")`, resolved once per JVM (lazy holder; instance-scoped `build(modLoaded, lookup, warn)` for order-independent tests), every failure shape → null = the unchanged existing ladder, with a once-warned drift message when the mod is present but unresolvable.

`XaeroMapCompat` + `XaeroTileExtractor` (xplat, docs/planning/xaero-map-bridge-plan.md is normative — §15/§16 the field-test + sweep records) is the **Xaero's World Map bridge**: reflection-only (no mixins, ~65 members bound by exact `MethodType`; floor Xaero WM **1.42.0**, below it the bridge switches itself off with `state=unavailable`), default-off (`enableXaeroMapBridge`), writes delivered LOD tiles into the map from the main client thread by mirroring the decompiled `MapWriter.writeChunk` under Xaero's own locks and gate ladder (incl. Xaero's crash handler and its Load New Chunks / Update Chunks switches; surface layer only — waits while a cave layer renders), rebuilds the textures itself per tile chunk under `isResting` (NEVER the `setToUpdateBuffers` flag — the saver-crash class), holds `CAPABILITY_VOXEL_COLUMNS` (an Xaero-only client downloads the disc), and since the §12 backpressure round as amended by §12.8/§12.9 (hybrid-scan-plan.md, 2026-08-24 — the first live session refuted the original -1-on-blocked/refusal doctrine) **paces the LOD want-set to its writer's rate**: the consumer overrides `pendingIngestBacklog` with a halt-domain-scaled queue occupancy (halt at 75% — ahead of the drop point), and a gate-BLOCKED pump with a live watchdog KEEPS reporting off the queue — offers are never refused (the queue absorbs movement bursts and IS the pressure gauge; the sole pre-extraction drop is Xaero's own map-writing switches both off, counted `skipped_settings`), blocked-not-wedged overflow drops report for re-serve, and -1 survives only for kill-switch/inactive/watchdog-stale/wedged. The halt time-box (7 s, re-based on occupancy RECESSION from the in-window peak — never on a `written` trickle) wedges a non-draining writer to -1, and the wedge re-arms below 0.5 occupancy OR on a 10 s duty-cycle clock (never a one-way exit) — the map may pace the fill, never stop it. The §18 ledger heal is DELETED (the kept immediate reporter un-stamps stale-dimension/world-change drops; kill switch `enableXaeroMapBackpressure` composed under `enableIngestBackpressure`). `/lss diag` shows the `XaeroMap:` line (`bp=` off / -1(reason) / fraction with a `(blocked)` suffix while the pump is gate-blocked).

`AntiXrayCompat` (Fabric, docs/planning/antixray-compat-design.md) is the DrexHD AntiXray accommodation, same zero-compile-dep pattern: (1) the crash shim — binds AntiXray's obfuscation-context ScopedValues to null around both serialize choke points (their mixins' raw `.get()` throws unbound otherwise; the probe path additionally has generic containment in `RequestProcessingService.serializeProbeContained`), and (2) the engine probe — per-world adoption off the mod's controller for the x-ray masking feature: engine mode 1 (Hide controller) adopts its hidden list + max-block-height; modes 2/3 (Obfuscate/ObfuscateLayer) surface `ReplacementNoise` — their obfuscateGlobal is the hidden ∪ replacement UNION (bulk terrain), and adopting it flattened whole sections to their dominant non-masked state (the sculk/water black-LOD bug, found live 2026-07-27) — so the manager masks the LSS `xrayHiddenBlocks` list at the ENGINE's height instead (source label `antixray-mod+lss-list`) (`XrayMaskPolicy` in common, `XrayMaskFilter`/`XrayMaskManager` + Paper twins; masking hooks INSIDE `serializeColumn`/the NBT serializers so every serve path and the DirtyContentFilter hash see identical masked bytes; the static manager holder is published by the service lifecycle with an owner-guarded retract). All failure shapes degrade fail-safe (inactive shim = contained blank LODs, unreadable engine = LSS-config-keys masking) with once-bounded logging.

### Configuration

**Fabric:** GSON configs built on the shared `common/` base classes (`JsonConfig` loader + `ServerConfigBase`, which holds the server fields/defaults/clamps verbatim for both platforms). Two configs auto-created in `config/`:
- `lss-server-config.json` — full audit of every default and clamp in `docs/planning/config-defaults-and-clamps-review-2026-08-02.md`. Current shipped defaults (the README's table is the user-facing copy): bandwidth `mbPerSecondLimitPerPlayer` 25.0 / `mbPerSecondLimitGlobal` 75.0 (decimal MiB/s of RAW bytes — they bound client DECODE work, so wire compression does not loosen them; legacy `bytesPerSecondLimit*` keys are hidden readers honored forever, consumers MUST use `bytesPerSecondPerPlayer()/Global()`), `lodDistanceChunks` 512, generation caps 40/40, `dirtyBroadcastIntervalSeconds` 10 (**0 = disable client pushes**; the server-side invalidation drain still runs — parked NOT_GENERATED positions then heal only on reconnect), `maxConcurrentDiskReads` 0 = AUTO (store-armed → ceil(pool/2), store-less → pool; nonzero clamps 1..64; disable idiom = set ≥ pool), `diskReaderThreads` 0 = AUTO (Moonrise → min(8, cores/2), vanilla IOWorker → 3), per-dimension timestamp cache 0 = AUTO (derived from lodDistance at the D0 tile cost). Five expert booleans `@HiddenFromFile` (default true, honored from files that carry them, non-default values survive re-saves): `useBackgroundReadPriority`, `useBackgroundReadSplit`, `useSelectiveNbtParse`, `useNbtTranscode`, `useCompressedColumns`. Slow-link machinery (all default ON, all with `/lsslod set` or config kill switches): `lodYieldsToVanillaTransport` (skip the column flush while the netty channel is unwritable; starvation floor + relevance prune; loopback channels never go unwritable, so CI/soaks are provably unaffected), `enablePingBackstop` (keepalive-excess rate cut, `pingf=` diag), `enableSendPacing` (burst shaper: clamp(Q/10, share, 2×share) per tick — never below cap rate at 20 TPS; `paced=` diag). The deleted `outboundBufferCeilingKB` stays deleted (bounding queue depth cannot deliver latency — pace under capacity instead). Legacy-client shims `enableV16Compat`/`enableV18Compat`/`enableV19Compat` all default true. `missMemoTtlSeconds` 30 (0 = off). **LOD store**: `lodStore` off/on with a SPLIT default — compiled default `"off"` (absent key never arms on upgrade), brand-new installs generate `"on"` via the fresh-create hook; `"full"` is a permanent read alias; diag reports the running engine (`store=full`/`off`/`unavailable`). `lodStoreBackfill` true (inert while store off; not available on Paper), `lodStoreBackfillColumnsPerSecond` 500 (10..1000), `lodStoreMaxMB` 0 = uncapped (nonzero 64..1048576; logical-bytes accounting, looped incremental_vacuum — the v0.9.0 review's three compounding cap defects stay fixed), `lodStoreResweepSeconds` 0 shared / 300 Paper. Two independent guards: `enabled: false` opens no store and runs no backfill, and the backfill stops at a 2 GiB free-space floor. **The test harnesses pin the `lodStore` KEY explicitly** (all soak scenario configs, benchmark staging, gametest run-dir configs) so their baselines never shift with the default. X-ray: `xrayObfuscation` auto/on/off (auto adopts a detected engine's hidden list + height; `xrayHiddenBlocks`/`xrayMaxBlockHeight` are the fallback tier). **The service gate** (service-permission-gate-plan.md): `requireServicePermission` false — armed, LOD service requires BOTH `lss.use` AND `vss.use` (deny model, default-true everywhere: plugin.yml `default: true`, NeoForge default-true resolvers, `defaultValue=true` at every `LoaderServices.checkPermission` site — arming alone denies nobody; fail-open on every failure shape, a ROLLOUT lever not a security boundary); denial = the pinned DISABLED rung in the client's own dialect + the once-per-episode log + the `deniedHandshakes` memo; live rechecks every 200 ticks (CURRENT-dialect revocation with 2-sweep hysteresis + push-then-composite; grant sweep replays the stored handshake through the FULL production ladder whenever the memo is non-empty, so disarming drains everyone); Fabric reads fabric-permissions-api reflectively by shape (`FabricPermissionsBridge`), NeoForge registers native `PermissionNode` statics at gather, `/lsslod set` row 12 + the conditional `Gate:` diag line + `service.permission_denied`. **Far players**: `farPlayers` on/opt-in/off (default on; unknown values fail safe to off), `farPlayersUpdateIntervalTicks` 10, `farPlayersMaxDistanceBlocks` 2048 / min 0, `farPlayersSendSpectators` false, `farPlayersExclude` list; the `lss.`/`vss.farplayers.hidden` permission drops a target server-side on EVERY loader (Paper natively; Fabric/NeoForge through `LoaderServices.checkPermission` with default FALSE since 2026-09-04 — fail-VISIBLE by the seam's never-throw contract, unlike Paper's Folia-motivated fail-hidden `hiddenFor`; NeoForge registers the two nodes with default-false resolvers beside the service nodes), as does vanish (Paper `vanished` metadata; Fabric/NeoForge the reflective `MeliusVanishBridge` — absent = visible, present-but-throwing/drifted = HIDDEN, wired as a lambda reading `this.server` at call time because the service field initializer runs before the ctor sets it).
- `lss-client-config.json` — `receiveServerLods`, `lodDistanceChunks` override, the TWO-AXIS cache key (docs/planning/cache-alias-keying-and-reset-override-plan.md — `useWorldSubBuckets` default true: remote buckets gain a `.world-<16hex>` suffix from vanilla's obfuscated seed, derived FRESH per session build + dimension entry, once-ever bare-bucket adoption, 8-sibling cap; `cacheAddressAliases`: corroboration-guarded alias groups — applied only when Voxy's observed storage dir corroborates via voxy-extra's LoD Mirror, never while the Xaero bridge is armed; `/lss reset` sweeps group members + world siblings via `ColumnCacheStore.clearForServers`; `/lss reset voxy-force` is the ForceGrant-gated escape hatch — 60 s TTL, connection-bound, stage-2 samePath re-probe), the legacy-server shims (`enableV16ServerCompat` + `enableV16Generation`, both default true), `enableIngestBackpressure` (issue #71 kill switch), `enableAdaptiveScanCadence` (4 Hz fast re-scans; false = fixed 1 Hz), `enableScanPrefixRetention` (2026-08-18: keep the scan prefix across crossings/dirty re-opens, walking only reopened rings — the render-thread-hitch fix; false = legacy full re-walk per reset, the field A/B control arm), `enableQuadtreeScan` (2026-08-19: the section-leaf ring fast path — reset walks confirm satisfied rings leaf-wise; false = per-position walk verbatim, the field A/B lever; gates the WALK only, the section store itself is not switchable), `enableRegionScan` (2026-08-24: the HYBRID walk — near rings ≤ 64 in legacy ring order, far residue region-major; region-scan-plan.md + hybrid-scan-plan.md — false = the legacy ring-major arm; selected at manager CONSTRUCTION so a flip applies at the next join, deliberately no options-page row; NOTE the two legacy walk levers `enableScanPrefixRetention`/`enableQuadtreeScan` gate the LEGACY arm only — inert on the default region arm, whose one rollback lever is `enableRegionScan=false`), `lodColumnsPerSecondLimit` 0 = off (manual rate cap, clamps 10..100000; Sodium slider with curved stops), `enableAdaptiveTransferRate` (the client transfer governor — paces LOD downloads below link capacity on congested links, min-composed with the manual cap) + `enableJoinSlowStart` (sessions start at 64 KB/s and earn their rate; Sodium toggle), `enableRegionSummarySync` (region summaries — never request, never apply when false; the server twin is `enableRegionSummaries`), `unknownBlockFallback`/`crossVersionBlockFallbacks` (XVER identity fallbacks). **Far players (client)**: `farPlayersEnabled` (renderer master toggle — deliberately NOT a capability-bit term; the bit is the PREFS CARRIER so a disabled client still delivers its `farPlayersShareSelf` opt-out), name tags (drawn by LSS itself since 2026-09-04), `farPlayersFullBright` (default false — the sky-15 floor is the default light), render/animation distance caps, `farPlayersWithSeeU` (SeeU-coexist override); the Sodium page's save handler re-sends prefs so mid-session privacy flips apply immediately.

**Paper:** `PaperConfig` extends the same `ServerConfigBase` — identical fields, defaults, and clamps as Fabric. Config at `plugins/LodServerSupport/lss-server-config.json`, plus an `updateEvents` list of Bukkit event class names for dirty chunk detection and the Bukkit half of the service gate (`requireServicePermission` — cross-platform since the PR #244 rework; the design lives in the Configuration bullet above and docs/planning/service-permission-gate-plan.md). Bukkit-specific mechanics: the permission read is the live `Player.hasPermission` permissible (CONTAINED — a throwing backend serves with a once-warn), consulted inline on the thread handling the handshake (the region thread on Folia) and on the pump for the recheck sweeps (the PaperFarPlayerSnapshots precedent); BOTH brand spellings are declared in plugin.yml at `default: true` — an UNDECLARED node falls back to Bukkit's op default and blacks out every non-op, and the dual declaration keeps an admin's negative grant alive across an LSS<->VSS jar swap (`PluginYmlContractTest` + `release_check.py`'s dual-spelling pins guard both); the denied re-handshake/revocation composite is pump-marshaled via `enqueueRuntimeTask` (drained after the lifecycle mailbox), and the grant sweep's replay goes through the plugin's production receiver with the registrar's DEFERRED reply, so the Folia pre-registration gap stays closed. Paper `/reload` clears the memo (the state dies with the service) — formerly-denied players heal on rejoin there.

All config values are clamped to safe ranges (min and max) on load.

## Benchmarking

Automated benchmark system for measuring server pipeline performance. Gated behind `-Dlss.benchmark=true` — never activates in production.

### Quick Start

```bash
./scripts/benchmark.sh fresh 60       # Fresh world, 60s — tests generation + serialization
./scripts/benchmark.sh no-cache 60    # Pre-built world — tests disk-read + serialization
```

Run `fresh` first to generate a base world, then `no-cache` reuses it.

### Optimization Workflow

1. Run baseline: `./scripts/benchmark.sh fresh 60`
2. Make optimization changes
3. Run again: `./scripts/benchmark.sh fresh 60`
4. Compare `benchmark-results/server.json` — key metrics: `throughput.sections_per_second`, `throughput.bytes_per_second`, `disk_reader.avg_read_time_ms`, `jvm.gc_time_ms`
5. Client metrics in `benchmark-results/client.json` — key: `avg_rtt_ms`, `columns_received`, `total_not_generated`

### Output Files

- `benchmark-results/server.json` — server throughput, sources, disk reader, generation, backpressure, bandwidth, JVM stats
- `benchmark-results/client.json` — columns received, bytes, send cycles, RTT, bandwidth rate
- `benchmark-results/*.jfr` — Java Flight Recorder profiles for both server and client

### How It Works

The shell script builds the mod, starts a dedicated server (`runBenchmarkServer`) and client (`runBenchmarkClient`) as separate Gradle tasks. The client auto-joins via `--quickPlayMultiplayer`. After the configured duration, the server exports metrics and halts; the client exports on disconnect. Duration is configurable via `-Pbenchmark.duration=N` on the server Gradle task.

### Key Files

- `fabric/src/main/java/dev/vox/lss/BenchmarkBridge.java` — reflective gate from the entrypoints into the dev-only harness (the `dev.vox.lss.benchmark` package is excluded from release jars; production no-ops)
- `fabric/src/main/java/dev/vox/lss/benchmark/BenchmarkHook.java` — tick counter, auto-shutdown, metric dump
- `xplat/src/main/java/dev/vox/lss/benchmark/BenchmarkMetricsExporter.java` — JSON serialization of all diagnostic + JVM data
- `scripts/benchmark.sh` — orchestrator (build → start server → start client → wait → collect); sources `scripts/lib/mc-run.sh` for the shared server/client lifecycle helpers

### Scenarios Explained

| Scenario | World State | Tests |
|----------|------------|-------|
| `fresh` | Empty world, chunks generated on demand | Generation + serialization pipeline |
| `no-cache` | Pre-built world | Disk-read + serialization pipeline |

### Interpreting Results

**Server `server.json`:**
- `throughput.sections_per_second` — primary throughput metric
- `sources` — where data came from: `in_memory` (loaded chunks), `disk_read`, `generation`
- `disk_reader.avg_read_time_ms` — disk I/O latency
- `backpressure.superseded` / `range_filtered` — asks silently dropped by a newer want-set declaration, transient generation pressure (gen slot full / capacity / timeout), a generation-ordering refusal (pacing/spread — these dominate the volume during backfill; see `gen_order_gated`), or the distance filter; healed by re-declaration. Nonzero is normal under v17 — `superseded` rises with any backlog the periodic declare outruns. The disk-miss flavors also count `service.miss_dropped` (soak law A5's term)
- `backpressure.queue_full` — send-queue overflow drops (a real loss signal, unlike the two above)
- `jvm.gc_time_ms` — GC overhead during benchmark

**Client `client.json`:**
- `avg_rtt_ms` — round-trip time for chunk requests
- `total_not_generated` — chunks the server permanently couldn't serve; ~0 on a gen-enabled server (transient pressure never answers NOT_GENERATED anymore)

## Soak Testing

Scenario-driven correctness harness: a real dedicated server + headless client execute a scripted timeline, both sides append JSONL diagnostic snapshots, and a Python checker enforces conservation invariants (request/delivery/generation/disk accounting) plus per-scenario expectations. Gated behind `-Dlss.soak.scenario=<path>` (server) and `-Dlss.soak=true` (client) — never activates in production. Design spec: `docs/planning/soak-test-design.md`.

### Quick Start

```bash
./scripts/soak.sh fresh-backfill    # Fresh superflat world — generation backfill; saves soak-worlds/base
./scripts/soak.sh warm-rejoin       # Two client runs — warm cache resync after a kick
./scripts/soak.sh dimension-trip    # Overworld → End → Overworld dimension boundaries
./scripts/soak.sh dirty-broadcast   # setblock → dirty broadcast → client re-request
./scripts/soak.sh all               # All 23 Fabric scenarios in order; stops at first failure
SOAK_PLATFORM=paper ./scripts/soak.sh all   # Paper port: fresh-backfill, warm-rejoin, dimension-trip, warm-rejoin-summary, paper-dirty-falling-block, store-second-join, paper-store-unfired-event
SOAK_PLATFORM=folia ./scripts/soak.sh all   # (NOT runnable on this line — no 1.21.1 Folia build) Folia: same six (minus unfired-event — the resweep gate is unvalidated there) against a real Folia server (:paper:runFolia)
```

Further scenarios (same invocation): rate-limit-storm, disk-saturation, disk-read-gate (v0.11.0 stage B, re-piped for Amendment 2 retention — K=1 under a threads:2 flood over the prebuilt annulus; premise `disk.gate_stops > 0`, `disk.saturated == 0`, `superseded >= 100`; `disk.gated` unpinned race armor), generation-disabled, generation-capacity-stress, bandwidth-throttle, cold-restart-resync, enabled-false, teleport-prune, dirty-range-filter, dirty-during-backfill, dirty-while-offline, clearcache-mid-session, dimension-rejoin-warm, store-second-join (lodStore=full; absorbed the old `-full` twin when `memory` was retired), store-save-storm, hybrid-boundary (the hybrid walk's phase-boundary gate — lod 72 fresh fill with a far-corner tp, disc-completeness is the seam-hole discriminator: a ring-65 hole FALSE-CONVERGES scan.confirmed, only the area floor reds), warm-rejoin-summary + dirty-while-offline-summary (region-summary-sync-plan.md §8 — mid-tile player at chunk (16,16), dist-24 disc over 9 tiles; run 1 settles every header with a t130 save-all (Paper's distributed autosave otherwise trickles gen saves past the re-stamp window) then a 160s clearcache RE-STAMPS the disc so cached stamps clear the freshness margin (serve-then-save stamps never can — the scenario-design discovery); run 2 validates the bulk off one frame, the player tile stays stale (kick-save), and the `-summary` dirty variant's offline edit in tile (1,-1) is the FALSE-CLEAN canary: probe 36:-4 must rise while control -4:36 stays exactly equal — both probe legs race-immune). Chained extras (stamp_heal is store-OFF — all four of its configs pin `lodStore: "off"`; the rest are store-specific): `stamp_heal.sh` (stamped-up_to_date heal chain: `stamp-heal-prime` → carried world+cache → `stamp-heal-rejoin`; Fabric-only, chain-only like the evicted chain), `store_offline_edit.sh` (three chained phases via SOAK_WORLD_FROM + cross-phase probe-hash verdict), `store_save_storm.sh` (paired process-CPU), `store_gate.sh warm|cold` (the §0 A/B), `SOAK_LODSTORE_OVERRIDE=full` (burn-in lever merging the store into every scenario), `summary_evicted.sh` (the P1 header rung's live gate: warm-rejoin-summary → carried world → evicted-tscache-rejoin, which boots a fresh server with `world/data/lss-timestamps.bin` DELETED and floors `disk.header_hits` — the empty-tscache resync must resolve via region headers, not a full re-download).

`rate-limit-storm` and `disk-saturation` keep their historical names but v17 has no
rate limiting: `disk-saturation` pins the supersession proof (`disk.saturated == 0`
AND `service.superseded >= 100` — zero saturation alone would prove nothing);
`rate-limit-storm` pins bounded transient-drop churn (superseded ceiling 1500);
`generation-capacity-stress` pins the permanence guarantee negatively
(`not_generated == 0` on a gen-enabled server, `superseded >= 100`).
**The general rule: under this model, supersession is produced by slow *service* relative to the want-set** — the want-set is a constant, so a scenario that means to create contention must slow the service (disk threads / generation caps). A failure in these scenarios is a premise question first (did the config stop creating contention? did the headroom gate leak?) and a threshold question only after.

Scenarios needing a base world auto-run `fresh-backfill` first. warm-rejoin, dirty-while-offline, and dimension-rejoin-warm use two client runs. Exit code = checker exit code.

### Output Files

- `soak-results/<scenario>-<timestamp>/server.jsonl` — server snapshot/command/join/end event rows. Snapshots carry the data-capture fields: `service.{superseded,range_filtered,re_resolved,grace_skipped,miss_dropped}`, `disk.memo_hits` (miss-memo rung hits — law A5's virtual not-founds), `players[].backlog`, `*_hw` high-water gauges (per-tick sampled), `dedup.groups`, `jvm.*`, `tscache.*`, `dirty.{marked,suppressed}_total`, `mspt_avg_window`; command rows carry `ok`.
- `soak-results/<scenario>-<timestamp>/client-run<N>.jsonl` — client snapshot/disconnect rows per run (incl. `ingest_failures`, `effective_lod`, `rtt.{p50,p95}_ms`)
- `soak-results/<scenario>-<timestamp>/verdict.json` — checker verdict (conservation laws + named checks)
- `soak-results/<scenario>-<timestamp>/report.md` — `soak_report.py` anomaly digest (auto-written; a lens, not a gate)

### How It Works

`soak.sh` stages world/cache/config per scenario, pre-validates the timeline (`check_soak.py --validate`), then starts `runSoakServer` and `runSoakClient` (no JFR). `SoakScenarioDriver` fires timeline commands at join-anchored offsets, snapshots every 5 s, and halts the server at scenario end; the soak client synchronously flushes its column cache before exiting. The checker evaluates deltas between verified-quiescent snapshots and writes `verdict.json`; any violation exits nonzero. `SOAK_PLATFORM=paper` runs the identical scenario timelines against a real Paper server (`:paper:runSoakServer`, a run-paper task fed by the dev-only `soakShadowJar` that retains the soak package) with the unchanged Fabric soak client and checker; Paper keeps its own base world at `soak-worlds/base-paper`. `SOAK_PLATFORM=folia` does the same against a real Folia server (`:paper:runFolia`, run-paper's Folia download task with `pluginsMode = INHERIT_NONE` — the default plugin-jar detection would add the soak-stripped release jar alongside the soak jar; base world `soak-worlds/base-folia`): the driver maps `save-all` to an acknowledged no-op (`"mapped": true` command-row key — Folia unregisters the command) and the staging writes a `bukkit.yml` with a 100-tick autosave so chunks still flush mid-run. Per-scenario client instrumentation: `-Psoak.probes=x:z,...` adds per-position timestamp probes to client snapshots; `-Psoak.clientActionAt=SECONDS:ACTION` fires one scripted client action (e.g. `60:clearcache`). warm-rejoin, dirty-while-offline, and dimension-rejoin-warm use two client runs.

### Key Files

- `scripts/soak.sh` — orchestrator (stage → validate → run → collect → check)
- `scripts/soak-scenarios/<name>.json` + `<name>-config.json` — driver timeline + sparse server-config overrides
- `scripts/check_soak.py` — stdlib Python invariant checker (`--validate` pre-flight, post-run laws, per-run completion gates — a missing server `end` row OR a missing client `disconnect` row means that JVM died mid-run, `client_run_completion_violations` — `--selftest` 270 in-memory pass/catch cases incl. all four oldest named checks, the disconnect gate, the quiescence client mirror, and the xray config-key type branches). **The harness is v17-only:** `players[].backlog` is a required schema field and `service.superseded`/`range_filtered` are required + monotonic, so it will correctly reject any pre-v17 recording — re-record rather than debug.
- `scripts/soak_report.py` — stdlib post-run anomaly digest (spikes/stalls, concerning-vs-mechanism counters, high-water marks, cadence/TPS, law margins, cross-identity audits); a lens, never a gate (`--strict` to exit nonzero on any anomaly; `--compare`, `--selftest`)
- `scripts/check_move_trace.py` — stdlib validator for the move-desync tracer's JSONL (`--validate FILE...`, `--selftest` — 36 pass/catch cases over the shared `scripts/testdata/move-trace-rows.jsonl` fixture the Tier 1 goldens write; run it on collected traces BEFORE analysis)
- `scripts/release_check.py` — release-jar safety gate (no dev-only benchmark/soak packages ship, incl. inside nested Jar-in-Jar entries and dev/vox/lss/common namespaces; stale-jar ambiguity guard + `--version` pinning; version expansion; mappings-namespace manifest; glob hygiene). Wired into `.github/workflows/build.yml` alongside the three `--selftest` runs.
- `scripts/lib/mc-run.sh` — shared server/client lifecycle helpers (sourced by soak.sh and benchmark.sh)
- `fabric/src/main/java/dev/vox/lss/benchmark/SoakScenarioDriver.java` — server-side timeline executor + JSONL snapshots
- `paper/src/main/java/dev/vox/lss/paper/soak/PaperSoakScenarioDriver.java` — Paper twin of the Fabric driver (Bukkit join anchors + 1-tick scheduler, same JSONL contract)
- `paper/src/main/java/dev/vox/lss/paper/soak/PaperSoakMetricsExporter.java` — Paper twin of the server-side exporter (same snapshot schema)
- `paper/src/main/java/dev/vox/lss/paper/PaperSoakBridge.java` — reflective gate; the soak package is excluded from the release shadowJar
- `fabric/src/main/java/dev/vox/lss/BenchmarkBridge.java` — reflective gate to the dev-only harness package (excluded from release jars)

## Key Patterns

- **Java 25** target for Fabric and Paper (`options.release = 25`), **Java 21** for common. Uses `var`, records, sealed-pattern `instanceof`, switch expressions.
- **Payloads:** Fabric uses `StreamCodec` with lambda encode/decode registered in `LSSNetworking`; Paper uses raw `FriendlyByteBuf` in `PaperPayloadHandler`. Both produce identical wire bytes.
- **Raw chunk shipping:** Server serializes MC-native `LevelChunkSection` data (block states + biomes via `section.write(buf)`) plus light `DataLayer` nibbles and sends. MC's built-in zlib network compression handles packet compression. No server-side voxelization or caching — client receives MC objects directly.
- **Off-thread processing:** Main server thread serializes loaded chunks into `LoadedColumnData` (pre-serialized bytes); a dedicated processing thread prepares payloads and schedules sends via `OffThreadProcessor`. `ColumnTimestampCache` provides in-memory up-to-date checks.
- **Folia (Paper module, experimental):** the service tick runs on the `GlobalRegionScheduler` (main thread on Paper, the single global-region thread on Folia — same pump semantics both ways); handshake registration and PlayerQuit arrive on region threads and go through a lifecycle mailbox drained at the top of `tick()` (the registering handshake's SessionConfig reply is deferred into that drain — never sent from the region thread — so the client cannot declare before its state exists); generated chunks are serialized inside the Moonrise `scheduleChunkLoad` completion (the owning region thread — the chunk can unload before a hop to the global tick). Loaded-chunk probing on Folia runs on the chunk's owning region thread (the player's `EntityScheduler`, gated by `Bukkit.isOwnedByCurrentRegion`) rather than the global pump, so palettes are read on their owner; a one-tick hold-release at batch granularity aligns each declared batch with its region-published probe results in the same snapshot (release-then-take, never take-then-release — see `PaperRequestProcessingService`). `FoliaWiringContractTest` scans every production class's constant pool (nested classes included) so no `BukkitRunnable`/`BukkitScheduler` reference creeps back in (they throw on Folia). `/reload` is unsupported on Folia. LSS disk reads go through Moonrise (`loadDataAsync` at `Priority.LOW`, see `PaperChunkDiskReader`) — called from the LSS reader pool, off any region thread, which is safe because region files are not regionised. Release notes for Folia-affecting changes should mention the experimental status.
- **Thread safety** via `ConcurrentHashMap`, `AtomicLong`/`AtomicInteger`, `volatile`, `CopyOnWriteArrayList`
- **No compile-time optional deps** — all mod compat uses `MethodHandle` bridges (MC types must use direct class literals, not `Class.forName()`, due to Fabric remapping)
- **Mappings:** Mojang official (not Yarn). Paper uses Mojang mappings natively via paperweight-userdev.
- **Package:** `dev.vox.lss` (Fabric), `dev.vox.lss.paper` (Paper), `dev.vox.lss.common` (shared)

## Releasing

Releases are triggered by pushing an **annotated tag** (`git tag -a`). The tag annotation message becomes the release notes on both GitHub and Modrinth. The CI workflow extracts it automatically.

### Tagging a Release

`main` is branch-protected (changes must land via PR; tags are **not** protected). Full flow:

1. Review commits since the last tag: `git log $(git describe --tags --abbrev=0)..HEAD --oneline`
2. **Pre-flight the exact release build locally** before tagging — the tag triggers an irreversible GitHub + Modrinth publish, so it must be green first:
   `CI=true ./gradlew :fabric:build :paper:test :paper:shadowJar :neoforge:build -Pmod_version=<version> && python3 scripts/release_check.py --version <version>`
   (`release_check.py` must print `OK`; `--version` pins the check to the jars just built — stale jars in build/libs otherwise fail the run; `:fabric:build` runs Tier 1 + Tier 2, `:paper:test` gates the Paper jar, `:neoforge:build` runs the contract suite + builds the six-family jar set `release_check.py` hard-requires — this is the ONLY line that ships NeoForge (`LINE_SHIP_NEOFORGE=true`). There is NO Tier 3 on this line — build.yml has no client-gametest job.)
3. Get the release commit onto `main` via PR (protected branch): push the release branch, `gh pr create --base main`, then `gh pr merge --merge`. Use **`--merge`** (a merge commit) — `--squash`/`--rebase` rewrite SHAs and orphan the tag.
4. Write release notes to a file (format below) and create the annotated tag with **`--cleanup=verbatim`** so the `###` headers survive:
   `git tag -a v<version> -F <notes-file> --cleanup=verbatim`
   Verify before pushing: `git for-each-ref --format='%(contents)' refs/tags/v<version>` (are the headers present?).
5. Push the tag — this triggers `release.yml`: `git push origin v<version>`
6. Watch CI (`gh run watch <id> --exit-status`); after it publishes, **verify the rendered notes** on GitHub (`gh release view v<version>`) and Modrinth — see Pitfalls.

### Release Pitfalls (all hit v0.4.0 — see also memory `release-tag-notes-gotchas`)

- **`###` headers stripped from the tag message.** `git tag` defaults to `--cleanup=strip`, which deletes `#`-leading lines as comments. Always pass `--cleanup=verbatim`.
- **CI can publish a commit-dump instead of the annotation.** `actions/checkout` leaves a *lightweight* tag on tag-triggered runs, so the extract step's `objecttype` reads `commit` and falls back to `git log` commit subjects. Fixed in `release.yml` by force-fetching the annotated tag (`git fetch --force origin "refs/tags/$TAG:refs/tags/$TAG"`); if the notes still render wrong, fix GitHub instantly with `gh release edit v<version> --notes-file <file>`.
- **Modrinth changelog can't be fixed locally.** `MODRINTH_TOKEN` is a CI-only secret, so a wrong Modrinth changelog must be edited by hand on the web (`lKiXKLvv`; the `84zcagOb`/voxy-server-side listing stopped receiving versions at v0.8.0 — its v0.7.x changelogs are still hand-editable there). Keep a short, player-focused variant of the notes ready for this.
- **Never `gh run rerun` a partially-published release.** A re-run rebuilds byte-different jars, silently replaces the GitHub release assets, and creates DUPLICATE Modrinth versions (nothing rejects a second `v<X>+fabric+mc<Y>`). Red BEFORE the gh-release step = nothing published, full re-run safe. Red AFTER any publish step (the likely case: the voxy step 403s on missing membership) = recover by hand-uploading the GitHub-attached jars to the channel that missed.
- **Do NOT smoke-test the pipeline with a throwaway pre-release tag** — `release.yml` triggers on every `v*` tag and would irreversibly publish the LSS jars. (The old "verify 84zcagOb membership before tagging" step is retired with VSS publishing — disabled since v0.8.0.)
- **A gametest can flake on CI** — see **Known Test Flakes & Environmental Failures** (in the Test Commands section) for the full catalog. Since 2026-07 `build.yml` retries the gametest tiers once automatically (retries surface as `::warning::` annotations; first-attempt evidence lands in the `gametest-flake-evidence-*` artifacts), so a red job already survived two attempts — diagnose from the artifacts before `gh run rerun <id> --failed`.

### Release Notes Format

Write for **Minecraft server admins and mod users**, not developers. Use markdown with category headers — omit empty categories:

```
### Bug Fixes

- **Short summary** — What changed and why it matters to users.

### New Features

- **Short summary** — What it does and how users benefit.

### Configuration

- **Short summary** — New/changed config options with defaults.

### Performance

- **Short summary** — What improved and the user-visible impact.
```

Rules:
- Each item: bold summary + dash + 1-2 sentence explanation
- Mention Fabric or Paper when a change is platform-specific; omit qualifier when it affects both
- Folia-affecting items must mention Folia support's **experimental** status (the label stays
  until concurrent multi-region soak validation — see the spec's exit criterion)
- Skip internal-only changes (CI, README, refactoring) unless they have user-visible impact
- Use present tense ("Fixes X" not "Fixed X" — GitHub/Modrinth display these as current release notes)
- No version heading — the tag name is displayed as the title automatically

### Example

```bash
git tag -a v0.2.4 -m "$(cat <<'EOF'
### Bug Fixes

- **Fixes infinite generation loop in The End** — All-air chunks were treated as missing, causing endless re-generation in void dimensions.

### New Features

- **Purpur server support** — Paper builds now work on Purpur servers.

### Configuration

- **Configurable timestamp cache size** — New `perDimensionTimestampCacheSizeMB` option (default 32 MB) controls memory used for chunk freshness tracking.
EOF
)"
```
