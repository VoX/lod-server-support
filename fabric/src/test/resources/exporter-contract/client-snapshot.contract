# Client snapshot schema contract — flattened "dotted.key=javatype" lines, sorted.
# Pins BenchmarkMetricsExporter.buildClientSnapshot with a live LodRequestManager and
# probes armed as exactly "7:-3". ExporterContractTest requires EXACT equality, and the
# manager-null (enabled=false) zero-fill branch must emit the IDENTICAL key set.
# Editing this file is a wire-adjacent schema change: scripts/check_soak.py KNOWN keys
# and docs/soak-test-design.md (PKG-CHK) must move in lockstep.
columns.dirty=int
columns.empty=int
columns.known=int
columns.satisfied=int
dimension=string
dropped=long
effective_lod=int
ingest_failures=long
probes.7:-3=long
queued=int
queued_bytes=long
received_bytes=long
received_columns=long
requested_total=long
responses.columns=long
responses.not_generated=long
responses.up_to_date=long
rtt.p50_ms=double
rtt.p95_ms=double
scan.audit_heals=long
scan.budget=int
scan.confirmed=int
scan.fast=long
scan.missing_vanilla=int
scan.near_rings=int
scan.quad_ring_skips=long
scan.queued=int
scan.rate_gated=long
scan.region_skips=long
scan.region_span=int
scan.reopened=int
scan.ring=int
scan.valve_trips=long
send_cycles=long
server_enabled=boolean
session_version=int
summary.columns_validated=long
summary.stamps_applied=long
summary.stamps_ignored=long
summary.tiles_clean=long
summary.tiles_no_region=long
summary.tiles_stale=long
summary.tiles_unknown=long
tracker_in_flight=int
wire_received_bytes=long
