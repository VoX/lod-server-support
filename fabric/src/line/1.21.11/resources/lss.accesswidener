accessWidener v2 named
# Headless section serialization (disk-read serve path, 2026-07-29 profile): the palette
# histogram that replaces LevelChunkSection's recalcBlockCounts needs the container's
# palette + storage pair. Vanilla keeps them behind a private volatile field on a private
# record; Paper's Moonrise patch makes both public, so the Paper twin needs no widening.
accessible class net/minecraft/world/level/chunk/PalettedContainer$Data
accessible field net/minecraft/world/level/chunk/PalettedContainer data Lnet/minecraft/world/level/chunk/PalettedContainer$Data;
